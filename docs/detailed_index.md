# Detailed code index — apecx_integration

> Generated by `scripts/gen_index.py` — do NOT edit by hand. Re-run the script after code changes; a pre-commit gate (`code-index-fresh`) blocks a commit whose index is stale.

Per file: classes (with bases + methods) and top-level functions, each with a compact
signature + docstring line 1. Consult on demand (grep for a symbol), not front-to-back.

## `src/apecx_integration/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/_alembic/migrations/env.py`
_Alembic env — imports Base.metadata from the Control Plane ORM layer._

- `def run_migrations_offline()`
- `def run_migrations_online()`

## `src/apecx_integration/_alembic/migrations/versions/0001_initial.py`
_initial_

- `def upgrade()` — Upgrade schema.
- `def downgrade()` — Downgrade schema.

## `src/apecx_integration/_alembic/migrations/versions/0002_unique_run_started_per_run.py`
_unique RUN_STARTED per run — concurrent /workflows/execute guard_

- `def upgrade()`
- `def downgrade()`

## `src/apecx_integration/_alembic/migrations/versions/0003_unique_active_null_scope_synonym.py`
_unique active verified_synonym per (source, query, target) when scope IS NULL_

- `def upgrade()`
- `def downgrade()`

## `src/apecx_integration/_alembic/migrations/versions/0004_allocation_estimate_created_at.py`
_allocation_estimate.created_at — give /hpc/confirm a real ordering key_

- `def upgrade()`
- `def downgrade()`

## `src/apecx_integration/_alembic/migrations/versions/0005_approval_created_at.py`
_approval.created_at — give /approvals/pending a real ordering key_

- `def upgrade()`
- `def downgrade()`

## `src/apecx_integration/_alembic/migrations/versions/0006_step_created_at.py`
_step.created_at — give /runs/status a real ordering key for PENDING steps_

- `def upgrade()`
- `def downgrade()`

## `src/apecx_integration/_bounded_cache.py`
_A tiny FIFO-bounded mapping for process-lifetime memo caches._

- class `BoundedDict(OrderedDict)` — ``dict`` that keeps at most ``maxsize`` entries, evicting the oldest-inserted on overflow.
  - `def __init__(self, maxsize: int=512)`
  - `def __setitem__(self, key, value)`

## `src/apecx_integration/agents/__init__.py`
_apecx_integration.agents — port of agent code formerly housed in_

_(no module-level classes or functions)_

## `src/apecx_integration/agents/_llm_config.py`
_Single source of truth for the synthesis-path LLM model + a loud preflight._

- `def resolve_llm_timeout()` — Return the LLM client request timeout: ``APECX_LLM_TIMEOUT`` env > default (300s).
- `def resolve_llm_model()` — Return the synthesis-path model name: ``APECX_LLM_MODEL`` env > default.
- `def resolve_llm_base_url()` — Return the LLM base URL: ``APECX_LLM_BASE_URL`` env > default.
- `def _probe_model(model: str, base_url: str)` — Probe the endpoint's model list. Returns ``(reachable, model_pulled)``.
- `def preflight_llm_model(model: str | None=None, base_url: str | None=None)` — Fail loud, early, and clearly when the synthesis model is not pulled.

## `src/apecx_integration/agents/_llm_factory.py`
_Shared LLM client factory for apecx_integration agents._

- `def build_chat_llm(temperature: float=0.0, max_tokens: int=1024, **overrides: Any)` — Build a LangChain ``ChatOpenAI`` against the configured endpoint.

## `src/apecx_integration/agents/domain_rag/__init__.py`
_Domain-specific FAISS RAG index for the apecx pipeline._

_(no module-level classes or functions)_

## `src/apecx_integration/agents/domain_rag/index.py`
_Domain RAG index — load a pre-built FAISS IndexFlatIP + metadata._

- `def _import_rag_libs()` — Return ``(SentenceTransformer, faiss, numpy)``, importing them in the
- class `DomainRagIndex` — Pre-built domain RAG index loader + searcher.
  - `def __init__(self, index_dir: Path | None=None, *, model_name: str=_DEFAULT_MODEL, device: str=_DEFAULT_DEVICE)`
  - `def index_dir(self)`
  - `def is_available(self)` — Cheap stat-only probe: True iff both the FAISS binary and
  - `def search(self, query: str, k: int=5)` — Return up to ``k`` nearest chunks for ``query``.
  - `def _ensure_loaded(self)`
  - `def _log_disabled_once(self, reason: str='index')` — Emit the loud first-time "RAG disabled" notification.
  - `def _load_model(self)`
  - `def _encode(self, texts: list[str])`

## `src/apecx_integration/agents/functional/__init__.py`
_Residue-level functional/immunological annotation clients (E3-3)._

_(no module-level classes or functions)_

## `src/apecx_integration/agents/functional/_cache.py`
_On-disk JSON cache for the functional-annotation clients (E3-3, CC-4)._

- `def cache_path(subdir: str, key: str)` — Return the JSON cache file path for ``key`` under ``subdir`` (created on demand).
- `def read_json(path: Path, *, ttl_seconds: float | None=None)` — Read a cached JSON payload, or ``None`` when absent / expired / corrupt.
- `def write_json(path: Path, data: Any)` — Atomically write ``data`` as JSON (tmp file + replace). Never raises on I/O error.

## `src/apecx_integration/agents/functional/_http.py`
_Shared async HTTP-with-retry base for the functional-annotation clients._

- class `HttpClientError(RuntimeError)` — Raised when a request fails after all retries (carries the final HTTP status).
- class `AsyncHttpClient` — Async httpx wrapper with retry/backoff and context-manager lifecycle.
  - `def __init__(self, *, base_url: str, timeout: float=DEFAULT_TIMEOUT_SECONDS, retry_attempts: int=DEFAULT_RETRY_ATTEMPTS, backoff_base: float=DEFAULT_BACKOFF_BASE_SECONDS, client: httpx.AsyncClient | None=None)`
  - `async def __aenter__(self)`
  - `async def __aexit__(self, *args: object)`
  - `async def close(self)`
  - `async def _get_json(self, path: str, *, params: dict[str, Any] | None=None)` — GET ``base_url + path`` with retry; return parsed JSON.
  - `async def _get_json_and_headers(self, path: str, *, params: dict[str, Any] | None=None)` — Like :meth:`_get_json` but also returns the response headers (lower-cased keys).
  - `async def _backoff(self, attempt: int)`

## `src/apecx_integration/agents/functional/iedb_client.py`
_IEDB client — known B/T-cell epitopes for an antigen (E3-3.3, bonus)._

- `def containment_param(accession: str)` — Return the PostgREST array-containment filter value for ``accession``.
- class `IedbClient(AsyncHttpClient)` — Async client for the IEDB query-api epitope_search endpoint.
  - `def __init__(self, *, base_url: str=DEFAULT_BASE_URL, ttl_seconds: float=_DEFAULT_TTL_SECONDS, **kwargs: Any)`
  - `async def search_epitopes(self, accession: str)` — Return the epitopes whose parent source antigen is ``UNIPROT:<accession>``.
  - `def _parse(rows: Any)`

## `src/apecx_integration/agents/functional/residue_annotation.py`
_Residue-level annotation orchestration + numbering bridge + cross-check (E3-3.4)._

- `def feature_covers(feature: dict[str, Any], unp_pos: int)` — True when ``unp_pos`` participates in ``feature``.
- `def locate_epitope_spans(uniprot_seq: str, epitope_seq: str)` — Return every (1-based start, end) span where ``epitope_seq`` occurs in ``uniprot_seq``.
- `async def gather_annotation_context(pdb_id: str, chain: str, *, sifts: SiftsClient, uniprot: UniProtClient, iedb: IedbClient)` — Fetch SIFTS + UniProt + IEDB annotation for ``pdb_id`` chain ``chain``.
- `def cross_check_residues(candidate_resis: list[int], ctx: dict[str, Any])` — Cross-check candidate AUTHOR residues against the annotation context.
- `def _degrade(note: str)`

## `src/apecx_integration/agents/functional/sifts_client.py`
_SIFTS client — PDB→UniProt accession + author-numbering residue bridge (E3-3.1)._

- class `SiftsClient(AsyncHttpClient)` — Async client for the PDBe SIFTS UniProt-mappings API.
  - `def __init__(self, *, base_url: str=DEFAULT_BASE_URL, **kwargs: Any)`
  - `async def get_mappings(self, pdb_id: str)` — Return ``{uniprot_accession: [segment, ...]}`` for ``pdb_id``.
  - `def _parse(body: dict[str, Any], key: str)`
- `def chain_segments(mappings: dict[str, list[dict[str, Any]]], chain: str)` — Flatten ``mappings`` to the segments covering ``chain``, each tagged with its
- `def bridge_residue(segments: list[dict[str, Any]], author_resi: int)` — Map an AUTHOR residue number to ``(accession, unp_pos)`` using the covering segment.

## `src/apecx_integration/agents/functional/uniprot_client.py`
_UniProt client — residue-level sequence features + canonical sequence (E3-3.2)._

- class `UniProtClient(AsyncHttpClient)` — Async client for the UniProtKB REST API (residue features + sequence).
  - `def __init__(self, *, base_url: str=DEFAULT_BASE_URL, **kwargs: Any)`
  - `async def get_entry(self, accession: str)` — Return ``{accession, release, query_date, sequence, features:[...]}`` or ``None``.
  - `def _parse(body: dict[str, Any], accession: str, headers: dict[str, str] | None=None)`

## `src/apecx_integration/agents/globus_search/__init__.py`
_Globus Search index client for the APECx harvested-corpus._

_(no module-level classes or functions)_

## `src/apecx_integration/agents/globus_search/_datacite.py`
_DataCite-aware field extraction for Globus harvested-corpus records._

- `def datacite_title(content: Any)` — Return the human title of a Globus record, or ``None``.
- `def datacite_description(content: Any)` — Return the abstract/description of a Globus record, or ``None``.
- `def datacite_subjects(content: Any, limit: int=6)` — Return the DataCite ``subjects[].subject`` keyword list (deduped, capped).
- `def datacite_identifiers(content: Any)` — Return the object identifiers of a Globus record, grouped by type.
- `def datacite_primary_id(content: Any)` — Return the best single citation token for a record (e.g. ``"PDB:7H6J"``,
- `def datacite_taxon_iris(content: Any)` — Return the DataCite ``subjects[].valueUri`` taxon IRIs (deduped, order preserved).
- `def datacite_organisms(content: Any)` — Return the deposited organism scientific names of a PDB record (deduped).

## `src/apecx_integration/agents/globus_search/client.py`
_Globus Search index client — read-only access to the APECx corpus._

- class `GlobusSearchUnavailableError(RuntimeError)` — Raised when the Globus Search query cannot be completed.
- `def _resolve_index_uuid()`
- `def _is_disabled()`
- `def search(query: str, *, max_results: int=20, offset: int=0, filters: list[dict[str, Any]] | None=None)` — Query the APECx Globus Search index.
- `def _extract_hits(result: Any)` — Normalize one Globus ``post_search`` page into hit dicts.
- `def facet(field_name: str, query: str, *, filters: list[dict[str, Any]] | None=None, size: int=100)` — Enumerate the distinct values of one indexed field (a Globus terms facet).

## `src/apecx_integration/agents/globus_search/structural_query.py`
_Taxon-precise structural search over the aggregate Globus index (e74bf12a)._

- class `SpeciesResolution` — Resolved species scoping for a structural query.
- class `StructuralSearchResult` — One source's taxon-precise structural hits plus any NAMED degrade note.
- `def resolve_species_terms(query: str, taxon_id: int | str | None=None, species_name: str | None=None)` — Resolve a query (+ optional NCBI taxon_id / canonical species name) to scoping terms.
- `def _facet_fallback_term(query: str)` — Pick the most-specific query token that the PDB scientific_name facet recognizes.
- `def _structural_keyword_tokens(query: str, terms: list[str])` — The query's protein/structural residual: query words minus species words.
- `def enumerate_organisms(species_term: str | list[str], *, publisher: str='RCSB PDB')` — Facet pre-pass: every ``scientific_name`` spelling matching a species term.
- `def search_one_source(query: str, source: str, publisher: str, *, taxon_id: int | str | None=None, species_name: str | None=None, max_results: int=0)` — Run a taxon-precise structural query for one source (``"pdb"`` / ``"emdb"``).
- `def _tag(hits: list[dict[str, Any]], source: str)`

## `src/apecx_integration/agents/globus_search/taxonomy_resolver.py`
_Extract candidate virus name(s) from free query text (for downstream taxon resolution)._

- `def extract_virus_names(query: str)` — Pull candidate virus name(s) from free query text, most-specific first.
- `def decompose_query_terms(query: str)` — Ordered ``(candidate_term, recovered_suffix)`` for resolving an arbitrary/combined query.

## `src/apecx_integration/agents/rag_synthesis/__init__.py`
_rag_synthesis — LLM synthesis with retrieved RAG chunks +_

_(no module-level classes or functions)_

## `src/apecx_integration/agents/rag_synthesis/harvester_adapter.py`
_Bridge from apecx-harvesters ``DataCite`` records to the flat_

- `def datacite_to_publication(record: DataCite)` — Convert a DataCite record to a synthesizer publication dict.

## `src/apecx_integration/agents/rag_synthesis/synthesizer.py`
_LLM synthesis with retrieved RAG chunks + structured DB context._

- class `SynthesisConfig(BaseModel)` — Tunable synthesis behavior.
- `def _load_default_config()` — Load the bundled synthesis config from disk. Loaded lazily so
- `def _reject_or_skip(msg: str, *, strict: bool, kind: str, idx: int)` — Centralize the strict-vs-lenient policy. In strict mode, raise;
- `def _render_rag_chunks(chunks: Iterable[dict[str, Any]], cap: int, *, strict: bool)` — Render retrieved RAG chunks for inclusion in the user prompt.
- `def _render_bvbrc_genomes(genomes: Iterable[dict[str, Any]], cap: int, *, strict: bool)` — Render BV-BRC genome rows. Returns ``(rendered, allowed_tokens)``.
- `def _render_violin_mappings(mappings: Iterable[dict[str, Any]], cap: int, *, strict: bool)` — Render VIOLIN cached mappings. Returns ``(rendered, allowed_tokens)``.
- `def _render_publications(pubs: Iterable[dict[str, Any]], cap: int, *, strict: bool, abstract_max_chars: int=300)` — Render harvester publication metadata.
- `def _render_globus_results(hits: Iterable[dict[str, Any]], cap: int, *, strict: bool, abstract_max_chars: int=300)` — Render Globus Search hits from the APECx harvested-corpus index.
- `def _extract_distinct_citations(text: str, patterns: list[str])` — Return the set of distinct citation tokens in ``text``.
- `def synthesize_response(query: str, *, rag_chunks: Iterable[dict[str, Any]] | None=None, bvbrc_genomes: Iterable[dict[str, Any]] | None=None, violin_mappings: Iterable[dict[str, Any]] | None=None, publications: Iterable[dict[str, Any]] | None=None, globus_results: Iterable[dict[str, Any]] | None=None, llm: Any=None, config: SynthesisConfig | None=None, system_prompt_override: str | None=None)` — Synthesize a free-form Markdown response with inline citations.

## `src/apecx_integration/agents/violin_bvbrc/__init__.py`
_violin_bvbrc — VIOLIN/BV-BRC entity extraction + synonym search._

_(no module-level classes or functions)_

## `src/apecx_integration/agents/violin_bvbrc/_csv_agent_shell.py`
_Dormant CSV-agent paths extracted from violin_bvbrc/agent.py (G79)._

- `def initialize_csv_agent(verbose: bool=False)` — Initialize a CSV agent with access to all VIOLIN database tables.
- `def initialize_bvbrc_agent(verbose: bool=False)` — Initialize a CSV agent specifically for the BV-BRC genome data.
- `def post_process_response(response: str, entity_data: dict[str, Any]=None)` — Post-process the agent's response to enhance readability and add context.
- `def process_query(user_query: str, verbose: bool=False)` — Process a natural language query against the VIOLIN and BV-BRC databases.
- `def process_bvbrc_query(user_query: str, genome_entities: list[dict[str, Any]], pathogen_entities: list[dict[str, Any]]=None, verbose: bool=False)` — Process a query specifically for BV-BRC genome data.
- `def main()` — Main function to run the BV-BRC and VIOLIN database natural language interface.

## `src/apecx_integration/agents/violin_bvbrc/agent.py`
_BV-BRC and VIOLIN Database Natural Language Interface_

- `def _default_data_dir()` — Default data directory: the apecx-db-integration repo root,
- `def load_dataframes()` — Read the CSV tables from ``APECX_DB_DATA_DIR`` (or the default).
- `def _get_dfs()` — Internal: cached access to the VIOLIN + BV-BRC dataframes.
- `def __getattr__(name: str)` — PEP-562 module-level ``__getattr__`` so external callers that do
- `def get_agent_statistics()` — Get basic statistics about the database tables.
- `def get_llm_for_entity_extraction()` — Get an LLM instance optimized for entity extraction.
- `def extract_entities_llm(query: str)` — Extract potential entities from a user query using an LLM.
- `def get_candidate_terms(dfs: dict[str, pd.DataFrame]=None)` — Extract all potential candidate terms from the database that could be matches for entities.
- `def rank_terms_with_llm(entity: dict[str, Any], candidates: dict[str, list[str]])` — Use LLM to rank candidate terms based on how likely they refer to the same entity.
- `def filter_candidates_by_similarity(query: str, candidates: list[str], max_candidates: int=100)` — Filter candidate terms using simple string similarity to reduce the number of candidates.
- `def llm_find_synonyms(entity_dict: dict[str, Any], dfs: dict[str, pd.DataFrame]=None)` — Find synonyms for an entity using LLM ranking.
- `def enrich_query_with_llm_synonyms(query: str)` — Enhance a query by adding synonyms for detected entities using LLM-based ranking.
- `def consolidated_synonym_search(query: str, dfs: dict[str, pd.DataFrame]=None, include_relevant_data: bool=False)` — Consolidate synonym search into a single LLM query that processes all entities at once.
- `def enrich_matches_with_database_data(matches: list[dict[str, Any]], dfs: dict[str, pd.DataFrame])` — Enriches the synonym matches with relevant data from the database tables.

## `src/apecx_integration/cli/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/cli/_globus_data_transfer.py`
_Apecx-side glue for the Globus data transfer (G82 2026-05-16; G127 2026-05-21)._

- `def _effective_auth_mode()` — The auth mode the resolver will use. NATIVE is the default; the
- `def _resolve_native_client_id()` — The native-app client_id: $APECX_GLOBUS_NATIVE_CLIENT_ID or the built-in
- `def _config_dest_endpoint()` — Destination endpoint persisted by ``apecx-globus-setup`` (or None).
- `def _backfill_dest_endpoint_env()` — If ``APECX_GLOBUS_DEST_ENDPOINT_ID`` is unset, populate it from the
- class `GlobusPrereqStatus` — Why the Globus-first path is or isn't usable on this machine.
  - `def reason(self)` — Human-readable, single-line summary of why we're / aren't
- class `GlobusTransferResult` — Outcome of an ``attempt_globus_data_transfer`` call.
- `def check_globus_prerequisites()` — Cheap preflight: are all the env vars / SDK / credentials present?
- `def _keyring_credentials_present()` — True iff confidential-client credentials are in the OS keyring.
- `def build_transfer_items(data_dir: Path, *, datasets: tuple[str, ...] | set[str] | None=None)` — Build the {source_path, dest_path} list for the dataset files.
- `def _wrapper_yaml_path()` — Resolve the wrapper YAML's filesystem path.
- `def _workflow_yaml_path()` — Resolve the verify->transfer workflow YAML (G127).
- `def attempt_globus_data_transfer(data_dir: Path, *, poll_timeout_seconds: float=600.0, datasets: tuple[str, ...] | set[str] | None=None)` — Try to transfer the dataset(s) via Globus. Return a structured result.
- `def _resolve_auth_env()` — Populate ``APECX_GLOBUS_RESOLVED_*`` env vars based on auth_mode.
- `async def _attempt_globus_data_transfer_async(*, data_dir: Path, poll_timeout_seconds: float, datasets: tuple[str, ...] | set[str] | None=None)` — Async core. Drive the verify->transfer nanobrain workflow (G127).

## `src/apecx_integration/cli/dashboard.py`
_apecx-dashboard — live terminal view of apecx-mcp infrastructure health (W3.2)._

- `def _fetch_status(base_url: str, timeout: float=5.0)`
- `def _dot(b: dict[str, Any])`
- `def render(data: dict[str, Any])`
- `def main(argv: list[str] | None=None)`

## `src/apecx_integration/cli/globus_config.py`
_Persistent Globus configuration — ``~/.apecx/globus_config.json``._

- `def config_path()` — Resolve the config file path (``$APECX_GLOBUS_CONFIG_PATH`` override).
- `def _empty()`
- `def _validate_dir_entry(entry: Any, *, where: str)`
- `def _default_subdir(remote_path: str)` — Derive a destination subdir from the remote path's last segment.
- `def load()` — Load + validate the config. Returns defaults when the file is absent.
- `def save(cfg: dict[str, Any])` — Validate + atomically write the config; returns the path written.
- `def get_dest_endpoint()`
- `def set_dest_endpoint(endpoint_id: str)`
- `def get_extra_source_dirs()`
- `def add_source_dir(remote_path: str, dest_subdir: str | None=None)` — Append a recursive source directory. Idempotent on remote_path.

## `src/apecx_integration/cli/globus_setup.py`
_apecx-globus-setup — Globus credential store + endpoint setup/test CLI (G31)._

- `def _print_header(title: str)`
- `def _pass(label: str, detail: str='')`
- `def _fail(label: str, detail: str='')`
- `def _repo_root()` — Repo root = three parents up from this file (src/apecx_integration/cli/).
- `def _cmd_store()` — Interactively prompt for credentials and store them securely.
- `def _cmd_status()` — Print a human-readable, secret-free credential-store status.
- `def _cmd_clear()` — Delete stored Globus credentials. Idempotent.
- `def _cmd_endpoint_config(project: str | None, output: str)` — Render the Aurora endpoint-config template with the ALCF project filled in.
- `def _cmd_login(client_id: str | None)` — Run the Globus device-code login flow + persist tokens.
- `def _import_globus_sdk()` — Lazy import + FAIL-LOUD if missing.
- `def _resolve_credentials()` — Resolve client_id/secret for the ``test`` subcommand.
- `def _cmd_test(endpoint_id_arg: str | None, round_trip: bool)` — Test Globus endpoint configuration end-to-end.
- `def _round_trip_dispatch(endpoint_id: str, client_id: str, client_secret: str)` — Dispatch the trivial_echo_step fixture to the endpoint and verify the echo.
- `def _cmd_test_transfer(*, list_only: bool, source_path_override: str | None)` — Verify the data-transfer Globus path end-to-end (G84+, 2026-05-16).
- `def _cmd_add_dir(remote_path: str, dest_subdir: str | None)` — Register an extra source directory (fetched recursively at transfer).
- `def _cmd_setup()` — One-shot full configuration (the no-argument default).
- `def _build_parser()`
- `def main(argv: list[str] | None=None)` — Entry point. Returns an int exit code (0 = success, non-zero = failure).

## `src/apecx_integration/cli/lookup.py`
_``apecx-lookup`` — CLI wrapper around the synonym-dictionary lookup pipeline._

- `def _build_parser()`
- `def _resolve_dict_path(arg_path: Path | None)` — Pick the dictionary path: explicit flag > env var > default.
- `def _render_text(result: LookupResult)` — Human-readable rendering. Multi-line block; concise.
- `def _render_json(result: LookupResult)` — Machine-readable rendering of the full LookupResult.
- `def main(argv: list[str] | None=None)`

## `src/apecx_integration/cli/regression_metrics.py`
_``apecx-regression-metrics`` — CLI wrapper around_

- `def _parse_since(arg: str | None)`
- `def _build_parser()`
- `def _filter_rule_id(report_dict: dict, rule_id: str)` — Narrow the by_rule_id breakdown in-place.
- `def main(argv: list[str] | None=None)`

## `src/apecx_integration/cli/routing_rule.py`
_Canonical APECx tool-routing rule + install-time placement._

- `def user_global_claude_md_path()` — The user-global local-agent / Claude Code memory file (~/.claude/CLAUDE.md).
- `def render_block()` — The full marker-delimited block written into a CLAUDE.md.
- `def upsert_routing_block(path: Path | None=None)` — Insert or refresh the routing block in ``path`` (default ~/.claude/CLAUDE.md).
- `def manual_paste_guidance()` — Operator instructions for the account-side channel an installer CANNOT write.

## `src/apecx_integration/cli/setup.py`
_apecx-setup orchestrator (2026-05-09; RAG made opt-in 2026-05-16, G81)._

- class `StepResult`
  - `def emoji(self)`
- `def _print_header(title: str)`
- `def _print_summary(results: list[StepResult])`
- `def _step_globus(*, interactive: bool=True)` — Globus install + configuration preflight (G84, 2026-05-16).
- `def _step_data(*, interactive: bool=True)` — Acquire the OPTIONAL local VIOLIN + BV-BRC datasets via Globus.
- `def _spec_to_run_args(spec, *, bind_host: str='127.0.0.1')` — Translate a ContainerSpec into ``-p BIND:H:C / -e K=V / -v SRC:DST`` argv.
- `def _docker_available()`
- `def _container_running(name: str)`
- `def _container_exists(name: str)`
- `def _step_dict(*, interactive: bool=True)` — Pre-build the synonym dictionary at the canonical location.
- `def _step_infra()`
- `def _ollama_url()`
- `def _ollama_model()` — Resolve the Ollama model the installer pulls (E3-6 — single source of truth).
- `def _ollama_daemon_reachable(timeout: float=2.0)` — Probe Ollama's /api/tags endpoint.
- `def _ollama_reachable(timeout_s: float=5.0)` — True if something serves Ollama's REST API at the configured URL (container OR host).
- `def _ollama_installed_models(timeout_s: float=5.0)`
- `def _pull_ollama_model_http(model: str, *, timeout_s: float=1800.0)` — Pull an Ollama model via the REST API (POST /api/pull) — container-aware: works against the
- `def _ensure_ollama_serving(*, interactive: bool=True)` — Ensure Ollama is serving at the configured URL: ADOPT an already-running host/container Ollama
- `def _probe_llm()` — Resolve whether an LLM endpoint is usable, and how — local OR remote.
- `def _prompt_yes(question: str, default: bool=True)` — Prompt user for y/N. Returns the default on empty input.
- `def _offer_install_ollama(*, interactive: bool)` — Offer to install Ollama via the platform's documented installer.
- `def _offer_start_ollama_daemon(*, interactive: bool)` — Start the Ollama daemon in the background.
- `def _step_llm(*, interactive: bool=True)`
- `def _step_rag()`
- `def _step_pymol()` — Build the version-pinned headless PyMOL image (E3-7, 2026-06-13).
- `def _step_verify()`
- `def _step_capabilities()` — Show what the install can do now + what infra unlocks — via the shared aggregator.
- `def _step_routing(interactive: bool=True)` — Install the APECx tool-routing rule so the client LLM reaches for APECx first.
- `def chatgpt_connector_guidance(port: int=_CHATGPT_PORT)` — Operator instructions to connect ChatGPT to apecx-mcp — minimal + exact.
- `def _step_chatgpt(interactive: bool=True)` — Print how to connect ChatGPT to apecx-mcp (instructional — no file to auto-write).
- `def _run_all(*, interactive: bool=True, with_rag: bool=False, with_pymol: bool=False)` — Run the canonical install chain.
- `def main(argv: list[str] | None=None)`

## `src/apecx_integration/cli/setup_data.py`
_apecx-setup data — shared data-acquisition helpers._

- `def prompt_for_data_dir(*, interactive: bool=True)` — Choose the local data directory; guard against clobbering existing CSVs.
- `def _default_claude_config_path()` — The standard Claude Desktop config location for this OS.
- `def _find_apecx_mcp_binary()` — Locate the ``apecx-mcp`` binary so a new config block can point at it.
- `def _build_apecx_server_block(data_dir: Path, llm_env: dict | None=None)` — Construct the full ``mcpServers.apecx`` block for a fresh install.
- `def _prompt_for_llm_config()` — Prompt for the three LLM env vars; Enter accepts each default.
- `def _load_or_init_config(config_path: Path)` — Load the Claude Desktop config, or return ``{}`` if missing.
- `def _apecx_block_state(config: dict, config_path: Path)` — Return the existing ``mcpServers.apecx`` block, or None if absent.
- `def _update_claude_config(config_path: Path, data_dir: Path, llm_env: dict | None=None)` — Patch the Claude Desktop config to set APECX_DATA_ROOT.
- `def _prompt_yes_no(question: str, default: bool)` — Default-aware y/n prompt; empty input picks the default.
- `def _resolve_config_target(default_path: Path)` — Confirm the standard config path or prompt for a manual path.
- `def _format_apecx_block_preview(block: dict)` — Pretty-print the proposed apecx block for the first-time prompt.
- `def _maybe_update_claude_config(data_dir: Path)` — Find the Claude Desktop config and offer to patch / install the apecx block.
- `def _reconfigure_llm_in_config(config_path: Path)` — Re-prompt for LLM env vars in an existing apecx config block.
- `def _run_reconfigure_llm()` — Entry point for ``apecx-setup --reconfigure-llm``.
- `def report_post_transfer_layout(data_dir: Path)` — Print which expected files are present under ``data_dir``; return the
- `def main(argv: list[str] | None=None)` — Thin dispatcher kept for the ``--reconfigure-llm`` flow.

## `src/apecx_integration/composition/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/composition/_composer_candidates.py`
_Candidate-rendering + step-IO inspection helpers extracted from composer.py (G78)._

- `def _load_step_io_names(absolute_yaml_path: str | None)` — Read a wrapper YAML and return its input/output data unit names.
- `def _render_candidates_spec(hits: list[SearchHit])` — Render retrieval hits in the compact spec-mode format.
- `def _render_candidates(hits: list[SearchHit])` — Render retrieval hits as a compact, LLM-consumable block.

## `src/apecx_integration/composition/_composer_llm_factory.py`
_LLM factory + env-var override helpers extracted from composer.py (G78)._

- `def _default_llm_factory(**kwargs: Any)` — Default LLM client builder — imports lazily so a missing
- `def _apply_llm_env_overrides(raw: dict[str, Any])` — Honor the ``APECX_LLM_*`` env-var contract the composer_config

## `src/apecx_integration/composition/_composer_parsing.py`
_Parsing + retry-feedback helpers extracted from composer.py (G78)._

- `def _is_repairable_parse_error(exc: ComposerResponseError)` — True iff the parse failure has a shape the LLM can correct
- `def _format_parse_feedback(exc: ComposerResponseError)` — User-turn message for the parse-error retry path.
- `def _is_class_not_found_error(exc: ComposerResponseError)` — True iff the spec realized-but-referenced a step class the catalog does not
- `def _format_class_not_found_feedback(exc: ComposerResponseError, valid_class_names: list[str])` — Retry feedback for a hallucinated class-name error. Unlike ``_format_parse_feedback``
- `def _parse_response(content: str)` — Extract the single ``yaml`` fenced block and the optional

## `src/apecx_integration/composition/_composer_rerank.py`
_Post-retrieval re-rank helpers extracted from composer.py (G78)._

- `def _prompt_tokens(prompt: str)` — Extract lowercase alphanumeric tokens from a prompt for
- `def _class_name_of(class_path: str)`
- `def _rerank_by_class_name_match(hits: list[SearchHit], prompt: str)` — Boost hits whose class_name shares a token with the prompt.

## `src/apecx_integration/composition/_errors.py`
_Composer error hierarchy._

- class `ComposerConfigurationError(ValueError)` — Raised when a composer config is structurally wrong.
- class `ComposerResponseError(ValueError)` — Raised when the LLM response can't be parsed into a workflow.

## `src/apecx_integration/composition/approval_policy.py`
_T06 approval-policy gate._

- class `ApprovalAction(StrEnum)`
- class `ApprovalDecision` — Result of ``ApprovalPolicy.evaluate``.
  - `def blocks(self)`
  - `def strongest_required_action(self)`
  - `def pause_reason(self)` — One-sentence rationale for why the workflow is paused.
- class `ApprovalPolicy` — Category → action map, loaded from YAML.
  - `def __init__(self, *, mapping: Mapping[StepCategory, ApprovalAction])`
  - `def load(cls, path: Path)`
  - `def action_for(self, category: StepCategory)`
  - `def evaluate(self, categorized: CategorizedWorkflow, *, reviewer_approved: bool | None=None)` — Map step categories → an approval decision.

## `src/apecx_integration/composition/artifact_store.py`
_Content-addressed artifact store (T11)._

- class `ArtifactNotFound(LookupError)` — Raised by :meth:`ArtifactStore.load_content` when the artifact
- class `GenerationMetadata`
- class `ArtifactStore`
  - `def __init__(self, session_factory: sessionmaker[Session], recorder: ProvenanceRecorder, *, root: Path | None=None, git_repo_env: str='GENERATED_ARTIFACTS_REPO_PATH')`
  - `def store(self, *, content: bytes, kind: ArtifactKind, run_id: UUID, mime_type: str, step_id: UUID | None=None, generated_metadata: GenerationMetadata | None=None)`
  - `def load_content(self, artifact_id: UUID)` — Read the on-disk bytes for an artifact. Verifies the SHA-256
  - `def _maybe_git_commit(self, artifact_id: UUID, kind: ArtifactKind, content: bytes, metadata: GenerationMetadata | None)`

## `src/apecx_integration/composition/class_path_resolver.py`
_Catalog-grounded class-path resolver (CPR, 2026-05-11)._

- class `ResolutionKind(StrEnum)` — How the resolver classifies an emitted class path.
- class `ClassPathResolution` — One resolver verdict.
- `def _leaf(class_path: str)` — Last dotted segment of a class path. Empty string for malformed.
- `def resolve_class_path(emitted: str, catalog_class_paths: set[str])` — Classify an emitted class path against a catalog set.
- `def _near_leaf_matches(emitted_leaf: str, catalog_class_paths: set[str], *, limit: int=3)` — Catalog paths whose leaf shares a substring with the emitted leaf.
- class `ClassPathRepair` — One auto-correction applied to a parsed workflow.
- `def repair_workflow_class_paths(workflow_dict: dict, catalog_class_paths: set[str])` — Walk ``workflow_dict["steps"]`` and auto-correct any
- `def hint_for_step_violation(emitted: str, catalog_class_paths: set[str])` — Format a "Did you mean X?" hint for the validator's

## `src/apecx_integration/composition/component_catalog.py`
_Component catalog — the composer's source-of-truth for "what library_

- class `CatalogComponent` — One library component surfaced to the composer.
- class `SearchHit` — One ranked retrieval result.
- class `ComponentCatalog` — In-memory catalog loaded from one or more manifest YAML files.
  - `def from_manifests(cls, paths: list[Path])` — Load + flatten components from one or more manifest YAMLs.
  - `def search(self, query: str, k: int=10)` — Substring-match ``query`` against every component's
  - `def __len__(self)`
- `def _load_manifest(path: Path)` — Parse one manifest YAML into a list of CatalogComponent.
- `def _strip_multiline(s: Any)` — YAML's ``>`` folded scalars leave trailing newlines + internal
- `def _tokenize(query: str)` — Lowercase + split on non-alphanumeric. No stemming, no stopword
- `def catalog_search_roots(composer_config_path: Path | None=None)` — Resolved directories a composed workflow's relative ``config:`` refs

## `src/apecx_integration/composition/composer.py`
_LLM composer — Phase 2 (T-COMP per docs/composer_task_spec.md)._

- class `_RetryableValidationError(Exception)` — Internal wrapper around ``WorkflowValidationError`` for the
  - `def __init__(self, workflow_validation_error)`
- class `Composer` — LLM-backed workflow composer (T-COMP).
  - `def __init__(self, config: ComposerConfig, *, llm_factory=None, artifact_store=None)` — Args:
  - `def _load_rag_index_or_degrade(self, config: ComposerConfig)` — Load the FAISS ``ComponentIndex`` at ``config.rag_index_dir``, or
  - `def llm_for_role(self, role: str, **overrides: Any)` — Build a chat LLM bound to the model assigned to ``role``.
  - `def from_config(cls, config_path: str | Path)` — Load a ``ComposerConfig`` from YAML and build the composer.
  - `def config(self)`
  - `def prompts(self)`
  - `def catalog(self)`
  - `def _suggest_for_violation(self, source: str, *, k: int=3)` — Pick ``k`` components from the active retrieval backend that
  - `def _retrieve(self, prompt: str, k: int)` — Route retrieval to RAG or linear-scan.
  - `async def compose(self, prompt: str, context: dict[str, Any] | None=None)` — Compose a workflow YAML from a natural-language prompt.
  - `def _invoke_and_parse(self, llm: Any, messages: list[Any])` — One LLM round-trip: invoke → parse → safe_load.
  - `def _parse_spec_response(self, raw_content: str)` — SPEC2: parse a JSON MinimalWorkflowSpec → workflow_dict.
  - `def _log_llm_failure(self, *, exc: BaseException, elapsed_ms: int, message_count: int, total_chars: int)` — Surface full LLM-call diagnostics on failure (B2).
  - `def _validate_or_raise(self, workflow_dict: dict[str, Any], yaml_text: str, yaml_paths: dict[str, str], *, catalog_class_paths: set[str] | None=None, novel_python: dict[str, str] | None=None)` — Run framework-rule validation on a parsed workflow.
  - `def _persist_or_synthesize(self, *, yaml_bytes: bytes, prompt: str, context: dict[str, Any] | None, composition_summary: CompositionSummary, novel_python: dict[str, str], llm_model_version_hash: str)` — Either persist via ArtifactStore and return its Artifact UUID,
  - `def _build_system_prompt(self)` — Build the system prompt for the active composer mode.
  - `def _build_user_prompt(self, prompt: str, hits: list[SearchHit], context: dict[str, Any] | None)` — Compose the user-facing message: library candidates +
  - `def _load_prompts(cls, prompt_dir: Path)`
  - `def prompt_budgets(self)` — Read-only view of the per-prompt size accounting computed
  - `def _build_prompt_budgets(prompts: dict[str, str], *, soft_cap_kb: float, hard_cap_kb: float)` — Compute a ``PromptBudget`` for every loaded prompt.
  - `def _enforce_prompt_budgets(budgets: dict[str, PromptBudget])` — Apply soft/hard caps to ``system.md`` specifically.
- `def _persisted_composition_summary(summary: CompositionSummary, novel_python: dict[str, str])` — Serialize a CompositionSummary to the dict persisted on the GeneratedArtifact.

## `src/apecx_integration/composition/composer_schemas.py`
_Shapes the LLM composer produces + config shape it consumes._

- class `ModelRoleConfig(BaseModel)` — Per-role LLM endpoint binding (BENCH-P0, 2026-05-12).
- class `ComposerConfig(BaseModel)` — Composer configuration loaded from YAML by ``Composer.from_config``.
- class `CompositionSummary` — Diff-UX payload (feeds T06).
  - `def reuse_ratio(self)` — Adoption signal — fraction of steps drawn from the library.
  - `def is_reuse_dominated(self, threshold: float=0.8)` — Adoption-signal predicate: ``reuse_ratio >= threshold``.
- class `ComposedWorkflow` — What ``Composer.compose()`` will return once Phase 2 lands.
- class `PromptBudget` — Per-prompt size accounting + cap predicates.
  - `def fraction_of_hard_cap(self)` — ``size_bytes / hard_cap_bytes`` — the headline utilization
  - `def is_within_soft_cap(self)` — True when the prompt fits inside the soft cap. False
  - `def is_within_hard_cap(self)` — True when the prompt fits inside the hard cap. False

## `src/apecx_integration/composition/contract_coverage.py`
_Contract coverage analysis over the workflow corpus (Project A Step 3a)._

- `def _resolve(ref: str, base: Path, roots: list[str])` — Resolve a relative ``config:`` ref to a file, base_path first then catalog roots.
- `def _load(p: Path)`
- `def _endpoint_contract(ref: str, side: str, wf: dict, base: Path, roots: list[str])` — The declared contract for a link endpoint, or None when undeclared/unresolvable.
- `def _direct_links(wf: dict)` — (source, target) for each DirectLink with both refs present.
- `def workflow_yamls(wf_dir: Path=_WF_DIR)` — Top-level workflow YAMLs (those declaring both steps and links) under ``wf_dir``.
- `def undeclared_boundaries(roots: list[str] | None=None, wf_dir: Path=_WF_DIR)` — (workflow_rel_path, source, target) for every DirectLink boundary that is NOT
- `def count_undeclared(roots: list[str] | None=None, wf_dir: Path=_WF_DIR)`

## `src/apecx_integration/composition/decomposition/__init__.py`
_Local bounded task decomposition (EO-20): match-a-workflow-first, else decompose._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/decomposition/dispatchers.py`
_Workflow dispatcher for local decomposition (EO-20 dispatcher impl)._

- class `RunWorkflowDispatcher` — Dispatch a matched workflow by name. ``workflow_loader(name)`` returns a runnable
  - `def __init__(self, workflow_loader: Callable[[str], Any], *, input_envelope_resolver: Callable[[str], str | None] | None=None, **run_kwargs: Any)`
  - `async def dispatch(self, workflow_name: str, task: Task)`

## `src/apecx_integration/composition/decomposition/factory.py`
_Assemble a production LocalDecomposer from the real workflow catalog (EO capstone item 1)._

- `def assemble_local_decomposer(*, mode: str | None=None, match_threshold: float=0.0, **run_kwargs: Any)` — Build a LocalDecomposer bound to the live catalog + the real LLM decomposer.

## `src/apecx_integration/composition/decomposition/llm_decomposer.py`
_LLM-driven task decomposer (EO-20 decomposer impl) — the local-LLM boundary._

- `def _parse_decomposition(content: str)`
- class `LLMTaskDecomposer`
  - `def __init__(self, *, prompt_path: str | Path | None=None, llm_factory: Callable[..., Any]=build_chat_llm, max_subtasks: int=5)`
  - `async def decompose(self, task: Task)`

## `src/apecx_integration/composition/decomposition/local_decomposer.py`
_Local bounded decomposition (EO-20) — the local-LLM discretion layer._

- class `Task`
- class `MatchResult`
- class `WorkflowMatcher(Protocol)`
  - `async def match(self, task: Task)`
- class `TaskDecomposer(Protocol)`
  - `async def decompose(self, task: Task)`
- class `WorkflowDispatcher(Protocol)`
  - `async def dispatch(self, workflow_name: str, task: Task)`
- class `LocalDecomposer`
  - `def __init__(self, matcher: WorkflowMatcher, decomposer: TaskDecomposer, dispatcher: WorkflowDispatcher, *, max_depth: int=3, max_dispatches: int=20, match_threshold: float=0.0, mode: str='auto_solver', inputs_resolver: Any=None)`
  - `async def solve(self, task: Task, *, _depth: int=0)`
  - `async def _plan(self, task: Task)` — plan_returner — match (+ decompose if needed) and return a decomposition_choice control
  - `def _workflow_need(self, name: str, task: Task)`
  - `def _integrate(self, task: Task, subtasks: list[Task], results: list[WorkflowResult])`

## `src/apecx_integration/composition/decomposition/matchers.py`
_Workflow matchers for local decomposition (EO-20 matcher impl)._

- `def _tokenize(text: str)`
- class `KeywordWorkflowMatcher` — Jaccard token-overlap matcher over a ``{workflow_name: description}`` catalog.
  - `def __init__(self, catalog: dict[str, str])`
  - `async def match(self, task: Task)`

## `src/apecx_integration/composition/decomposition/modes.py`
_Decomposer mode flag (RoC-3a)._

- `def resolve_decomposer_mode(explicit: str | None=None)` — Resolve the mode from an explicit value or ``$APECX_EO_DECOMPOSER_MODE``.

## `src/apecx_integration/composition/differ.py`
_T06 categorization — what's composed vs. what's novel in a generated workflow._

- class `StepCategory(StrEnum)` — AP §5.6 step categorization.
- class `StepCategorization` — One row in the diff summary — per step in the workflow.
- class `CategorizedWorkflow` — The diff-UX payload for a composed workflow.
  - `def summary_sentence(self)` — AP §5.6 human-readable summary.
  - `def count(self, category: StepCategory)`
  - `def novel_step_ids(self)`
- `def categorize_workflow(*, workflow_dict: dict[str, Any], novel_python: dict[str, str], retrieved_class_paths: set[str], catalog_yaml_paths: dict[str, str] | None=None)` — Run the AP §5.6 categorization heuristic.
- `def _classify_step(*, step_id: str, step_class: str, step_config: Any, novel_ids: set[str], retrieved_class_paths: set[str], yaml_paths: dict[str, str])` — Return ``(category, reason, retrieval_gap)`` for one step.
- `def _import_outcome(class_path: str)` — Classify what happens when we try to import ``class_path``.
- `def _config_references_novel(config: Any, novel_ids: set[str])` — Does ``config`` mention any novel step_id?

## `src/apecx_integration/composition/docker_sandbox.py`
_T13b — runtime Docker sandbox (scaffold)._

- class `SandboxConfig` — Tunable isolation parameters.
- class `SandboxResult` — Outcome of a single sandboxed execution.
- class `SandboxError(Exception)` — Base class for sandbox errors.
- class `SandboxNotAvailableError(SandboxError)` — Raised when the sandbox cannot be invoked.
- `def build_docker_sandbox_command(command: Sequence[str], *, input_host_path: Path | None, output_host_path: Path | None=None, container_name: str | None=None, config: SandboxConfig | None=None)` — Construct the ``docker run`` argv for a hardened sandbox invocation.
- `def _docker_on_path()`
- class `DockerSandboxRunner` — Runs commands inside a hardened Docker sandbox.
  - `def __init__(self, config: SandboxConfig | None=None)`
  - `def config(self)`
  - `def _check_available(self)`
  - `async def run(self, command: Sequence[str], *, input_host_path: Path | None=None, container_name: str | None=None)` — Execute ``command`` inside the sandbox.

## `src/apecx_integration/composition/env_manifest.py`
_build_env_manifest — multi-repo provenance stamp for a composed workflow._

- `def _editable_src(pkg: str)` — The on-disk source dir of an editable install, via PEP 610 direct_url.json.
- `def _git_state(src: Path)` — {sha, dirty, src, vcs} for a checkout. vcs=False when src is not a git repo.
- `def _compute_reproducible(repos: dict)` — Reproducible iff every repo has a clean SHA. A missing SHA (no VCS) or ANY dirty
- `def _pkg_version(pkg: str)`
- `def build_env_manifest(*, llm_model: str | None=None, llm_base_url: str | None=None)` — Build the provenance manifest. ``llm_*`` default to the ``APECX_LLM_*`` env vars.

## `src/apecx_integration/composition/handles/__init__.py`
_Handle store for chaining structured payloads between workflows (EO-11)._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/handles/store.py`
_Handle store — stash a structured ``DataShape``, get back an opaque handle (EO-11)._

- class `HandleNotFound(KeyError)` — Raised by :meth:`HandleStore.get` on an unknown handle.
- class `HandleBackend(Protocol)` — Storage backend contract. v1 is in-memory; ProxyStore is a future impl.
  - `def put(self, payload: dict)`
  - `def get(self, handle: str)`
  - `def delete(self, handle: str)`
  - `def clear(self)`
- class `InMemoryBackend` — Process-lifetime, thread-safe dict backend.
  - `def __init__(self, max_handles: int=_DEFAULT_MAX_HANDLES)`
  - `def put(self, payload: dict)`
  - `def get(self, handle: str)`
  - `def delete(self, handle: str)`
  - `def clear(self)`
- class `HandleStore` — Typed facade: put a ``DataShape`` in, get the same typed shape back.
  - `def __init__(self, backend: HandleBackend | None=None)`
  - `def put(self, shape: DataShape)`
  - `def get(self, handle: str)`
  - `def delete(self, handle: str)`
  - `def clear(self)`
- `def default_handle_store()` — Process-wide store shared across MCP tool calls within one server lifetime.

## `src/apecx_integration/composition/inspection/__init__.py`
_Static workflow inspection — recursive YAML-tree resolution (EO-02)._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/inspection/workflow_inspector.py`
_Recursive static inspection of a nanobrain workflow YAML (EO-02)._

- class `LinkInspection(BaseModel)`
- class `StepInspection(BaseModel)`
- class `WorkflowInspection(BaseModel)`
- `def _du_names(section: Any)`
- `def _resolve_step_config(step_cfg: dict[str, Any], base_dir: Path)` — Return ``(relative_config_path, loaded_dict)`` for a step's ``config:``.
- `def _inspect_dict(raw: dict[str, Any], base_dir: Path, depth: int, max_depth: int)`
- `def inspect_workflow(workflow_yaml: str | Path, *, max_depth: int=3)` — Recursively inspect a workflow YAML into a structured tree (static, no instantiation).

## `src/apecx_integration/composition/lightweight/__init__.py`
_Programmatic (lightweight) variants of the code-writing workflows._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/lightweight/code_reflection_lightweight.py`
_Programmatic (lightweight) variant of code_reflection_workflow.yml._

- `def build_code_reflection_workflow(*, workflow_name: str='code_reflection_workflow_lightweight', description: str='Generate-and-critique code reflection (programmatic build).', code_write_config: dict[str, Any] | None=None, code_review_config: dict[str, Any] | None=None)` — Build the code-reflection workflow programmatically.

## `src/apecx_integration/composition/lightweight/tdr_loop_lightweight.py`
_Programmatic (lightweight) variant of tdr_refine_workflow.yml._

- `def build_tdr_refine_workflow(*, workflow_name: str='tdr_refine_workflow_lightweight', description: str='TDR refine loop (programmatic build).')` — Build the TDR refine workflow programmatically.

## `src/apecx_integration/composition/lightweight_validator.py`
_LW — bridge from nanobrain.lightweight.WorkflowBuilder to the_

- `def validate_lightweight_builder(builder: Any, *, catalog_class_paths: set[str] | None=None)` — Run the A1 validator over a WorkflowBuilder's current config.
- `def validate_and_load(builder: Any)` — Validate the builder's config, then call ``builder.load()``.
- `def repair_lightweight_builder_class_paths(builder: Any, catalog_class_paths: set[str])` — Apply CPR (2026-05-11) auto-repairs to a builder's config.

## `src/apecx_integration/composition/novel_python_validation.py`
_Deterministic AST structural validation of LLM-emitted nanobrain Python._

- `def _collect_top_level_names(tree: ast.Module)` — Names defined at module scope (classes + functions + assignments).
- `def _inherits_base_class(cls: ast.ClassDef, name: str)`
- `def _inherits_framework_base(cls: ast.ClassDef)` — True if the class declares any framework base in its bases (direct only).
- `def validate_python_structure(code: str, entry_point: str='', *, strict_imports: bool=True, require_process: bool=False)` — Run AST structural checks. Returns an ordered list of issue strings;

## `src/apecx_integration/composition/reviewer.py`
_REVIEW-AGENT — semantic-fit reviewer for composer-generated workflows._

- class `ReviewVerdict` — One reviewer judgment over a composed workflow.
- class `WorkflowReviewer` — Second-pass semantic reviewer for composer output.
  - `def __init__(self, *, llm_factory, system_prompt: str, model: str='mistral-nemo:latest', base_url: str='http://localhost:11434/v1', temperature: float=0.0, max_tokens: int=1024)`
  - `def from_prompt_dir(cls, prompt_dir: Path, *, llm_factory, model: str='mistral-nemo:latest', base_url: str='http://localhost:11434/v1', temperature: float=0.0, max_tokens: int=1024)` — Build a reviewer when the prompt file exists; return
  - `async def review(self, *, user_prompt: str, yaml_text: str, summary_sentence: str='', candidates_block: str='')` — Ask the LLM to judge the generated workflow.
  - `def _format_user_message(self, *, user_prompt: str, yaml_text: str, summary_sentence: str, candidates_block: str)`
  - `def _parse_verdict(self, raw: str)` — Extract the fenced JSON verdict from the LLM response.

## `src/apecx_integration/composition/rhea_tool_persistence.py`
_Persist an on-demand-synthesized rhea tool step as a committed, portable wrapper YAML._

- `def _slug(descriptor_id: str)` — ``rhea:muscle@3.8.1551+galaxy0`` -> ``muscle`` (filesystem-safe).
- `def persist_rhea_step(spec: RheaStepSpec, *, dest_dir: Path | None=None, tool_slug: str | None=None)` — Write *spec* as a portable, committed wrapper YAML; return its path.
- `async def ensure_tool_step(tool_name: str, *, dest_dir: Path | None=None, mcp_url: str | None=None, **synth_kwargs)` — On-demand resolver: return a committed wrapper for *tool_name*, synthesizing

## `src/apecx_integration/composition/runtime/__init__.py`
_Runtime wiring for the external-orchestration surface (EO-03/40/41/42)._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/runtime/container_admission.py`
_Host-side admission control for per-run code-exec ``docker run`` spawns._

- `def _max_slots()` — Resolve the cap from the env var (fail-loud on garbage / non-positive).
- `def acquire_container_slot()` — Return the process-wide code-exec-container semaphore for ``async with``.
- `def _reset_for_test()` — Drop the cached semaphore so the next acquire re-reads ``ENV_VAR``. Tests only.

## `src/apecx_integration/composition/runtime/design_approval_store.py`
_Design-approval store — server-ISSUED, scoped, fail-closed HITL tokens for the_

- class `DesignApprovalRecord` — One issued design-approval token + its decision state.
  - `def to_dict(self)`
  - `def from_dict(cls, d: dict)`
- class `DesignApprovalStore` — Thread-safe, FIFO-bounded store of design-approval tokens.
  - `def __init__(self, max_tokens: int=_DEFAULT_MAX_TOKENS, *, persist_dir: str | os.PathLike | None=None)`
  - `def _load_from_disk(self)` — Load persisted records oldest-first (by mtime) so dict-insertion order — which the
  - `def _persist(self, rec: DesignApprovalRecord)`
  - `def _evict_file(self, token: str)`
  - `def _scope(query: str | None, protein: str | None)` — Normalize the design-request identity. Whitespace-collapsed + lowercased so
  - `def request(self, *, query: str | None, protein: str | None)` — Issue a fresh PENDING token bound to ``(query, protein)``; return the token.
  - `def approve(self, token: str, *, decided_by: str='operator')` — Mark ``token`` approved. Returns the record, or ``None`` if the token is unknown
  - `def reject(self, token: str, *, decided_by: str='operator')`
  - `def get(self, token: str)`
  - `def validate(self, *, token: str | None, query: str | None, protein: str | None)` — FAIL-CLOSED scope-bound validation. Returns ``(ok, reason)``: ``ok`` is True ONLY
  - `def clear(self)` — Test hook + session reset. Also removes persisted files so a durable store is
- `def get_design_approval_store()` — Process-wide design-approval store singleton.

## `src/apecx_integration/composition/runtime/execution_locus.py`
_execution_locus — the single server flag that selects the orchestration face._

- class `ExecutionLocus(StrEnum)` — The two orchestration faces. ``StrEnum`` so a value compares/serializes as plain
- `def resolve_locus(value: str | None=None)` — Resolve the locus from ``value`` (the ``--locus`` flag) or ``$APECX_EXECUTION_LOCUS``.
- `def set_active_locus(locus: ExecutionLocus)` — Record the resolved locus process-wide (called by ``build_server``).
- `def get_active_locus()` — The locus this process is running under (``desktop`` until a server build sets it).

## `src/apecx_integration/composition/runtime/observed_run.py`
_Observed workflow run (EO-03 core): run a workflow and return the LLM-facing envelope_

- class `WorkflowRunOutcome` — Everything a caller needs after a run: raw output, the LLM-facing envelope, the summary.
- `def _extract_workflow_result(raw: dict[str, Any])` — Find a WorkflowResult-shaped value in the workflow output, if the workflow emitted one.
- `async def run_workflow_observed(workflow: Any, input_data: dict[str, Any], *, redact: list[str] | None=None, **run_kwargs: Any)` — Run a workflow with provenance + events captured; return envelope + run summary.

## `src/apecx_integration/composition/runtime/provenance_wiring.py`
_Provenance + step-event wiring around a workflow run (EO-40/41/42/43)._

- class `MemorySink(ProvenanceSinkBase)` — In-memory provenance sink — collects records for programmatic inspection.
  - `def __init__(self)`
  - `async def write_record(self, record: dict[str, Any])`
  - `async def flush(self)`
- class `ProvenanceRun` — A workflow run plus everything captured about it.
- `async def run_with_provenance(workflow: Any, input_data: dict[str, Any], *, redact: list[str] | None=None, **run_kwargs: Any)` — Run a workflow with G4 provenance + G37 events captured.
- class `StepRunSummary(BaseModel)`
- class `RunSummary(BaseModel)` — Scientist-facing 'what ran' view derived from G4 records + G37 events (EO-42).
- `def summarize_run(prov_run: ProvenanceRun)` — Fold the per-step records + events into a flat run summary.

## `src/apecx_integration/composition/runtime/run_store.py`
_Session run store (EO-04): keep each workflow run's summary + envelope keyed by run_id._

- class `RunRecord` — One recorded workflow run.
- class `RunStore` — In-memory, thread-safe store of workflow runs keyed by ``run_id``.
  - `def __init__(self, max_runs: int=_DEFAULT_MAX_RUNS)`
  - `def record(self, *, workflow_name: str, status: str | None, run_summary: RunSummary, workflow_result: WorkflowResult | None)` — Store a run under a fresh ``run_id`` and return the record.
  - `def get(self, run_id: str)` — Return the record for ``run_id``, or ``None`` if it is unknown.
  - `def session_runs(self)` — All runs recorded this session, oldest first.
  - `def clear(self)` — Drop every recorded run (test hook + session reset).
- `def get_run_store()` — Process-wide run store singleton.

## `src/apecx_integration/composition/sandbox.py`
_T13 Phase 1 — import-whitelist scanner for LLM-generated Python._

- class `Import` — One import as detected by the scanner.
- class `ScanResult`
  - `def ok(self)`
- class `ScanViolation(ValueError)` — Raised when a scan detects a disallowed construct or import.
  - `def __init__(self, result: ScanResult, *, suggestions: tuple[str, ...]=())`
- class `_ImportVisitor(ast.NodeVisitor)`
  - `def __init__(self)`
  - `def visit_Import(self, node: ast.Import)`
  - `def visit_ImportFrom(self, node: ast.ImportFrom)`
  - `def visit_Call(self, node: ast.Call)`
  - `def _dangerous_call(self, node: ast.Call)` — Return the (top_module, attr) DANGEROUS_CALLS hit for this call, else None — resolving
  - `def _has_kwarg_constant(node: ast.Call, name: str, value: object)`
- class `ImportScanner` — Scan Python source for imports and banned dynamic-code constructs.
  - `def __init__(self, *, whitelist: frozenset[str] | None=None)`
  - `def scan(self, source: str)`
- `def load_whitelist(path: str | Path)` — Load a newline-delimited whitelist file.
- `def scan_python_source(source: str, *, whitelist: frozenset[str] | None=None)` — Top-level scan: parse, detect imports + dynamic-code constructs,

## `src/apecx_integration/composition/schemas/__init__.py`
_Pydantic schemas for the external-orchestration surface (EO-* tasks)._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/schemas/control_transfer.py`
_ControlTransfer — the structured "return of control to the frontier LLM" payload (RoC-1b)._

- class `ParamNeed(BaseModel)` — One parameter the frontier LLM must supply (for ``missing_param``).
- class `WorkflowNeed(BaseModel)` — One workflow in a proposed plan (for ``decomposition_choice``).
- class `NextAction(BaseModel)` — The structured action the frontier LLM should take. The populated field depends on the
- class `ControlTransfer(BaseModel)` — The full return-of-control payload attached to a ``needs_input`` WorkflowResult.
- `def missing_param_transfer(params: list[ParamNeed], *, message: str | None=None)`
- `def ambiguous_entity_transfer(candidates: list[dict[str, Any]], *, message: str | None=None)`
- `def needs_prerequisite_transfer(prerequisite: str, *, message: str | None=None)`
- `def decomposition_choice_transfer(workflows: list[WorkflowNeed], *, message: str | None=None)`

## `src/apecx_integration/composition/schemas/data_shapes.py`
_Canonical data shapes stored behind a ``WorkflowResult.data_handle`` (EO-12)._

- class `_ShapeBase(BaseModel)`
- class `RecordSet(_ShapeBase)`
  - `def preview(self, limit: int=3)`
- class `EvidenceItem(_ShapeBase)`
- class `Evidence(_ShapeBase)`
  - `def preview(self, limit: int=3)`
- class `Bundle(_ShapeBase)`
  - `def preview(self, limit: int=10)`
- class `Artifact(_ShapeBase)`
  - `def preview(self, limit: int=0)`
- `def parse_data_shape(payload: dict[str, Any])` — Parse a serialized payload into its typed shape via the ``kind`` discriminator.

## `src/apecx_integration/composition/schemas/workflow_result.py`
_WorkflowResult — the standard envelope a workflow returns through the MCP surface._

- class `WorkflowResult(BaseModel)`
  - `def _check_consistency(self)`
  - `def failed(cls, error: str, *, markdown: str='', run_id: str | None=None)` — Construct a loud-error result. Prefer this over hand-setting ``status``.
  - `def needs_input(cls, control_transfer: ControlTransfer, *, markdown: str='', data_handle: str | None=None, data_preview: dict[str, Any] | None=None, run_id: str | None=None, provenance: dict[str, Any] | None=None)` — Construct a return-of-control result. Prefer this over hand-setting ``status``.

## `src/apecx_integration/composition/skeletons/__init__.py`
_Skeleton library — pre-authored MinimalWorkflowSpec instances._

- class `Skeleton(BaseModel)` — One pre-authored skeleton.
- class `SkeletonLibrary(BaseModel)` — All skeletons known to the composer.
  - `def from_dir(cls, directory: Path)` — Load every ``*.yml`` under ``directory`` as a Skeleton.
  - `def get(self, name: str)`
  - `def names(self)`
  - `def render_prompt_block(self)` — Compact prompt fragment listing available skeletons.

## `src/apecx_integration/composition/steps/__init__.py`
_Nanobrain `BaseStep` subclasses that are specific to the APECx_

_(no module-level classes or functions)_

## `src/apecx_integration/composition/steps/_align_cache.py`
_On-disk cache for the conserved-sites ALIGN step (E3-9, CC-2 / CC-4)._

- `def nocache_enabled()` — True when ``$APECX_CONSERVED_SITES_NOCACHE`` is set to a truthy value (read live).
- `def _cache_root()` — Cache root, read live so a test fixture that sets the env var after import is honored.
- `def compute_sequence_set_hash(fasta_text: str)` — Content-hash the fetched FASTA via the nanobrain G24 helper (sha256 hex, no prefix).
- `def align_cache_key(*, aligner: str, mode: str, amino: bool, executable: str, fasta_text: str)` — A stable sha256 key over the alignment determinants (incl. the G24 sequence-set hash).
- `def _entry_path(key: str)`
- `def read_cached(key: str)` — Return the cached align-output dict, or ``None`` on miss / corrupt / I/O error.
- `def write_cached(key: str, payload: dict[str, Any])` — Atomically write ``payload`` (tmp + replace). Never raises on I/O error (CC-2).

## `src/apecx_integration/composition/steps/_alignment_viz.py`
_Sequence-conservation visualization helpers for the viral_epitope_analysis report._

- `def _artifacts_dir()` — Durable artifact dir (env override or ~/.apecx/artifacts). Inlined per the layering rule
- `def _spark(values: list[float])` — Render a 0..1 value series as a block-glyph sparkline (degrade-loud on empty).
- `def render_conservation_text(per_column: list[dict[str, Any]] | None, conserved_regions: list[dict[str, Any]] | None, *, protein: str='protein', n_sequences: int | None=None)` — Dependency-free inline conservation track (the degrade-loud floor). Returns a markdown
- `def _region_letter_freqs(seqs: list[tuple[str, str]], start: int, end: int)` — Per-column (residue, information-bits height) for a logo over alignment cols [start, end].
- `def render_conservation_png(per_column: list[dict[str, Any]] | None, conserved_regions: list[dict[str, Any]] | None, alignment_fasta: str | None, *, protein: str='protein', n_sequences: int | None=None, dest_dir: Path | None=None, basename: str='conservation')` — Render a conservation plot PNG; return its BASENAME (relative, co-located with the report

## `src/apecx_integration/composition/steps/_bvbrc_cds.py`
_Shared BV-BRC exact-CDS coverage probe for the taxon-resolution fallback steps._

- `def content_range_total(header: str | None)` — Parse the BV-BRC ``Content-Range: items a-b/N`` header, returning ``N`` (0 if absent).
- `def cds_count(api_base: str, taxon_id: int, timeout: float)` — Total ``genome_feature`` CDS rows for a taxon (exact taxon_id match), from the

## `src/apecx_integration/composition/steps/_clade_grouping.py`
_Pure helpers for the per-clade conservation analysis (Req 5 — broad effectiveness)._

- `def pairwise_identity(a: str, b: str)` — Identity over columns where BOTH sequences are non-gap. Returns 0.0 when there is no
- `def identity_distribution(aligned: list[tuple[str, str]], *, max_pairs: int=2000)` — Summary of pairwise identity across the aligned sequences — evidence for the clade verdict.
- `def cluster_by_identity(aligned: list[tuple[str, str]], *, threshold: float=0.95, min_size: int=2)` — Greedy single-pass clustering of aligned sequences by pairwise identity.
- `def _clade_column_consensus(alignment: dict[str, str], clade_member_ids: list[list[str]], length: int, threshold: float)` — For each clade, per-column ``(conserved_within_clade, consensus_residue)``.
- `def clade_conservation_breadth(alignment: list[tuple[str, str]], clade_member_ids: list[list[str]], *, identity_threshold: float=0.9, min_region: int=3)` — Broad-effectiveness across clades, computed on the SHARED alignment (correct coordinates).
- `def _runs(col_class: list[int], target: int, min_region: int, consensus_fn, *, consensus_key: str='consensus')` — Contiguous runs of columns whose class == target, each >= min_region long.

## `src/apecx_integration/composition/steps/_combination_common.py`
_Shared pass-through contract for the decomposed epitope-combination steps._

- `def unwrap_single_key(input_data: dict[str, Any])` — Descend a single-key trigger envelope (``{du_name: payload}``) generically.
- `def is_terminal(payload: dict[str, Any])` — True when ``payload`` already carries a terminal ``EnvelopeStep``-shaped output.

## `src/apecx_integration/composition/steps/_evidence_provenance.py`
_Per-run provenance collection for viral_epitope_analysis (E3-8)._

- `def _stage_report_data(bundle: dict[str, Any], stage: str)` — The ``data`` payload of the named stage report, or ``{}`` when absent.
- `def _sequence_provenance(bundle: dict[str, Any])` — Sequence-conservation determinism params: aligner + version, threshold, counts.
- `def _structural_retrieval_provenance(bundle: dict[str, Any])` — The structural query actually ISSUED (E3-2): resolved organism spellings, the
- `def _analyzed_structures_prov(sr: dict[str, Any])` — The N structures analysed for cross-structure corroboration (E3-13), as
- `def _structural_reasoning_provenance(bundle: dict[str, Any])` — The chosen PDB + ranking rationale, the assembly/SASA settings, exposed/buried
- `def _functional_provenance(bundle: dict[str, Any])` — UniProt accession + release, SIFTS pdb_id, IEDB/UniProt query date, #coincidences
- `def collect_provenance(bundle: dict[str, Any])` — Fold the determinism-relevant params recorded by the science stages into ONE record.

## `src/apecx_integration/composition/steps/_novel_step_container/_novel_step_job.py`
_In-container job harness for a sandboxed novel step (#1c Phase 1)._

- `def _load_target_class(source: str, class_name: str)` — Write the novel source to a temp module and return its ``class_name`` attribute.
- `async def _build_and_run(cls, config: dict[str, Any], input_data: dict[str, Any])`
- `def run(job: dict[str, Any])`
- `def main()`

## `src/apecx_integration/composition/steps/_proceed.py`
_How-to-proceed guidance — turn a degradation into a diagnosis + recommended next action._

- `def append_proceed_note(bundle: dict[str, Any], *, stage: str, what: str, why: str, action: str, severity: str='info')` — Append a how-to-proceed note to ``bundle['proceed_notes']`` and return the bundle.
- `def render_how_to_proceed(bundle: dict[str, Any])` — Render accumulated proceed notes as a ``## How to proceed`` Markdown section.

## `src/apecx_integration/composition/steps/_pubmed_helpers.py`
_Stateless PubMed harvest + term-build helpers._

- `def entity_name(entity: Any)` — Extract a display name from an entity dict or string.
- `def build_term(query: str, entities: list[Any] | None, template: str='{query}', *, owner_name: str='')` — Apply ``template`` to build a PubMed eSearch term.
- `def build_focused_term(query: str, *, owner_name: str='')` — Build an ORGANISM-ANCHORED PubMed eSearch term from a free-text query.
- `def container_to_dict(container: Any)` — Project a ``PubMedContainer`` into the synthesizer's
- `async def harvest(term: str, *, max_papers: int=0)` — Run the PubMed eSearch → eFetch chain for ``term``.

## `src/apecx_integration/composition/steps/_pymol_container/_pymol_job.py`
_Headless PyMOL structural-reasoning job — runs INSIDE the containerized,_

- `def _assembly_context(cmd, raw_obj: str, work_obj: str, cutoff: float)` — Build the SASA-context object for the biological assembly and return
- `def _protein_chains(cmd, base_sel: str, requested: str | None)` — Candidate chains to analyse. With ``requested`` set, ONLY that chain (an explicit
- `def _select_best_chain(cmd, base_sel: str, candidates: list, conserved_regions: list, min_identity, pdb_id)` — R3 (chain-pinning): analyse the chain whose conserved motifs map with the BEST identity.
- `def _chain_sequence(cmd, base_sel: str, chain: str)` — Ordered (resi, resn, 1-letter, ca_xyz) for the chain's CA atoms (original copy).
- `def _per_residue_area(cmd, base_sel: str, chain: str)` — Sum the b-factor-loaded per-atom SASA into per-residue totals for the original
- `def _contact_map(resi_list: list, ca_xyz: list[list[float]], cutoff: float)`
- `def run(job: dict)`
- `def main()`

## `src/apecx_integration/composition/steps/_pymol_sasa.py`
_Pure structural-reasoning helpers for the ``structural_reasoning`` stage (E2-P)._

- `def extract_pdb_id(record: dict[str, Any])` — Extract a 4-character PDB id from a structural record's ``subject``.
- `def select_candidate_pdb_id(records: list[dict[str, Any]])` — Pick the first loadable PDB id from the structural records (deterministic:
- `def map_motif_to_chain(motif: str, chain_seq: str, chain_resis: list[int], *, min_identity: float=0.7)` — Map a conserved-region consensus *motif* onto chain residues (ungapped).
- `def _ungapped_slide(motif: str, chain_seq: str, chain_resis: list[Any], min_identity: float)` — Best UNGAPPED offset of the shorter of motif/chain over the longer (the original mapping). Returns
- `def _gapped_block_map(motif: str, chain_seq: str, chain_resis: list[Any], min_identity: float)` — DF3b gapped fallback — a COLLINEAR-block alignment (stdlib difflib) of the region consensus onto the
- `def map_regions_on_chain(resis: list[Any], chain_seq: str, conserved_regions: list[dict[str, Any]], *, min_identity: float=0.7, chain: str='?', pdb_id: Any=None)` — Map every conserved region's consensus motif onto ONE chain's residues.
- `def select_best_chain(per_chain: list[tuple[Any, list[Any], str]], conserved_regions: list[dict[str, Any]], *, min_identity: float=0.7, pdb_id: Any=None)` — R3 (chain-pinning): from per-chain ``(chain, resis, chain_seq)`` sequences, pick the
- `def relative_sasa(resn: str, sasa: float)` — RSA = SASA / theoretical-max-ASA for the residue. ``None`` for non-standard
- `def classify_sasa(resn: str, sasa: float, *, rsa_threshold: float=0.25)` — Classify one residue EXPOSED vs BURIED from its in-context SASA.
- `def _structure_positions(struct: dict[str, Any])` — Map one structure's analysis onto SHARED conserved-region coordinates.
- `def aggregate_corroboration(per_structure: list[dict[str, Any]], *, threshold: float=0.5)` — Corroborate candidate-epitope positions ACROSS the N analysed structures.
- `def corroborated_residue_list(primary_struct: dict[str, Any], corroboration: list[dict[str, Any]])` — Anchor the cross-structure corroboration onto the PRIMARY structure's residues.
- `def assembly_exposure_flips(au_residues: list[dict[str, Any]], assembly_residues: list[dict[str, Any]])` — Residues whose EXPOSED/BURIED verdict CHANGED between the asymmetric unit and the

## `src/apecx_integration/composition/steps/_stage_report.py`
_Stage-report scaffolding for the viral_epitope_analysis pipeline._

- `def append_stage_report(bundle: dict[str, Any], stage: str, order: int, markdown: str, data: dict[str, Any] | None=None)` — Append a stage report to ``bundle['stage_reports']`` and return the bundle.
- `def render_stage_reports(bundle: dict[str, Any])` — Render the accumulated stage reports as a Markdown fragment.

## `src/apecx_integration/composition/steps/_unlimited_lookup.py`
_Enhanced lookup functions for unlimited VIOLIN + BV-BRC data retrieval._

- `def lookup_violin_unlimited(terms: list[tuple[str, str]], violin_dir: Path, *, max_results: int | None=None, min_relevance_score: float=0.1, owner_name: str='')` — Enhanced VIOLIN lookup with unlimited retrieval capability.
- `def lookup_bvbrc_unlimited(terms: list[tuple[str, str]], bvbrc_dir: Path, *, max_results: int | None=None, min_relevance_score: float=0.1, owner_name: str='')` — Enhanced BV-BRC lookup with unlimited retrieval capability.
- `def _calculate_relevance(query: str, target: str)` — Calculate relevance score between query and target string.
- `def lookup_violin(terms: list[tuple[str, str]], violin_dir: Path, *, max_results: int | None, owner_name: str='')` — Compatibility wrapper that supports both limited and unlimited modes.
- `def lookup_bvbrc(terms: list[tuple[str, str]], bvbrc_dir: Path, *, max_results: int | None, owner_name: str='')` — Compatibility wrapper that supports both limited and unlimited modes.

## `src/apecx_integration/composition/steps/_violin_bvbrc_lookup.py`
_Stateless lookup functions for VIOLIN + BV-BRC tabular data._

- `def lookup_violin(terms: list[tuple[str, str]], violin_dir: Path, *, max_results: int, owner_name: str='')` — Substring-match ``terms`` against VIOLIN's pathogen + vaccine CSVs.
- `def lookup_bvbrc(terms: list[tuple[str, str]], bvbrc_dir: Path, *, max_results: int, owner_name: str='')` — Substring-match ``terms`` against the BV-BRC alphavirus genomes TSV.

## `src/apecx_integration/composition/steps/alignment_report_step.py`
_AlignmentReportStep — the "use the result" leg of the MUSCLE workflow._

- class `AlignmentReportStepConfig(StepConfig)` — Configuration for :class:`AlignmentReportStep`.
  - `def _strip_framework_keys(cls, data: Any)`
- `def _parse_fasta(text: str)` — Parse FASTA text into a list of ``(id, sequence)`` tuples.
- class `AlignmentReportStep(BaseStep)` — Parse a MUSCLE alignment and produce a summary + statistics.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config: AlignmentReportStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: Any, **kwargs)` — Parse the alignment FASTA and compute the report.

## `src/apecx_integration/composition/steps/alignment_viz_step.py`
_AlignmentVizStep — render the sequence-conservation visualization (E-viz)._

- class `AlignmentVizStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load.
  - `def _strip_framework_keys(cls, data: Any)`
- class `AlignmentVizStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/benchmark_drafter_step.py`
_BenchmarkDrafterStep — single LLM call for benchmark code generation._

- `def _resolve_role_model(role: str)` — Resolve ``(model, base_url)`` for ``role`` using the same
- class `BenchmarkDrafterStepConfig(StepConfig)` — Configuration for ``BenchmarkDrafterStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_role(self)`
- class `BenchmarkDrafterStep(BaseStep)` — Single-call LLM drafter for benchmark problems.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: BenchmarkDrafterStepConfig)`
  - `def _init_from_config(self, config: BenchmarkDrafterStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def _resolve_rules_path(configured: str | None, source_path: str | None)` — Mirror of ``_resolve_prompt_path`` for the optional rules file.
  - `def system_prompt(self)` — Loaded system prompt — exposed for tests + LLM-input introspection.
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str, model: str, base_url: str)`
  - `def _build_user_message(*, spec: str, entry_point: str | None, test_hint: str | None, function_signature: str | None, previous_attempt: str | None=None, critique: str | None=None)`
  - `def _extract_code(raw: str)` — Pull the largest ```python ... ``` block; fall back to the

## `src/apecx_integration/composition/steps/benchmark_edge_case_step.py`
_BenchmarkEdgeCaseStep — pre-drafter agent that enumerates edge cases._

- class `BenchmarkEdgeCaseStepConfig(StepConfig)` — Configuration for ``BenchmarkEdgeCaseStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_role(self)`
- class `BenchmarkEdgeCaseStep(BaseStep)` — Edge-case enumerator stage for MBPP-style problems.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: BenchmarkEdgeCaseStepConfig)`
  - `def _init_from_config(self, config: BenchmarkEdgeCaseStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str, model: str, base_url: str)`

## `src/apecx_integration/composition/steps/benchmark_planner_step.py`
_BenchmarkPlannerStep — plan-then-code planner stage._

- class `BenchmarkPlannerStepConfig(StepConfig)` — Configuration for ``BenchmarkPlannerStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_role(self)`
- class `BenchmarkPlannerStep(BaseStep)` — Planner stage of the plan-then-code scaffold.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: BenchmarkPlannerStepConfig)`
  - `def _init_from_config(self, config: BenchmarkPlannerStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str, model: str, base_url: str)`
  - `def _build_user_message(*, spec: str, entry_point: str | None, test_hint: str | None, function_signature: str | None)`
  - `def _extract_plan(raw: str)` — Strip think blocks from planner output.

## `src/apecx_integration/composition/steps/benchmark_reviewer_step.py`
_BenchmarkReviewerStep — middle stage of the review-revise scaffold._

- class `BenchmarkReviewerStepConfig(StepConfig)` — Configuration for ``BenchmarkReviewerStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_role(self)`
- class `BenchmarkReviewerStep(BaseStep)` — Reviewer stage of the review-revise scaffold.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: BenchmarkReviewerStepConfig)`
  - `def _init_from_config(self, config: BenchmarkReviewerStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_path(configured: str | None, source_path: str | None, *, default: Path | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str, model: str, base_url: str)`

## `src/apecx_integration/composition/steps/bvbrc_protein_fasta_step.py`
_BvbrcProteinFastaStep — fetch REAL per-strain protein AA sequences from BV-BRC (EO-51)._

- `def _product_matches_word_boundary(product: str, protein: str)` — True iff ``protein`` occurs in ``product`` at a WORD BOUNDARY — i.e. not as a
- `def _is_informative_product(product: str)` — True iff ``product`` is a SPECIFIC protein name (not a generic catch-all annotation) usable as
- class `BvbrcProteinFastaStepConfig(StepConfig)` — Config for BV-BRC protein-sequence retrieval.
- class `BvbrcProteinFastaStep(BaseStep)` — Fetch real per-strain protein AA sequences for a taxon from the BV-BRC data API.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(self, input_data: dict[str, Any])`
  - `def _fetch(self, taxon_id: int, protein: str, feature_type: str, exact: bool=False)` — Return ``(records, n_fetched, n_dropped_length_outlier)``.
  - `def _select_length_cluster(self, records: list[dict[str, Any]])` — Keep the DOMINANT (most-populous) length cluster; drop outlier-length records.
  - `def _get_json(self, path: str, query: str)`
  - `def _query_available_proteins(self, taxon_id: int, feature_type: str, *, cap: int=5000)` — Reverse lookup: which protein products (and how many features each) exist for this taxon.
  - `def _query_features(self, taxon_id: int, protein: str, feature_type: str, exact: bool=False)`
  - `def _query_sequences(self, md5s: list[str])`

## `src/apecx_integration/composition/steps/bvbrc_taxonomy_search_step.py`
_BvbrcTaxonomySearchStep — DETERMINISTIC BV-BRC taxonomy lookup over candidate names._

- `def _already_resolved(bundle: dict[str, Any])` — True when the deterministic dict resolver already won (the fallback is then skipped).
- `def _as_int(value: Any)`
- `def _species_taxon_id(row: dict[str, Any])` — The SPECIES-rank ancestor taxon_id from a BV-BRC taxonomy row's lineage (``lineage_ids`` zipped
- class `BvbrcTaxonomySearchStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load time.
  - `def _strip_framework_keys(cls, data: Any)`
- class `BvbrcTaxonomySearchStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(self, input_data: dict[str, Any])`
  - `def _get_json(self, path: str, query: str)`
  - `def _cds_count(self, taxon_id: int)` — Exact ``genome_feature`` CDS count for a taxon (shared with the review step).

## `src/apecx_integration/composition/steps/clade_grouping_step.py`
_CladeGroupingStep — group the aligned strains into clades for broad-effectiveness analysis._

- class `CladeGroupingStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule).
  - `def _strip_framework_keys(cls, data: Any)`
- class `CladeGroupingStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _degrade(self, bundle: dict[str, Any], note: str, n_clades: int=0, distribution: dict[str, Any] | None=None, n_subgroups: int | None=None)` — Loud single-group/empty fallback — never raise, never silently empty.

## `src/apecx_integration/composition/steps/code_reflection_step.py`
_CodeReflectionStep — generate→critique reasoning pattern as a step._

- class `CodeReflectionStep(SubworkflowStep)` — Embed the code-reflection sub-workflow (write → review) as a step.
  - `def _default_inner_workflow_path(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — Run the embedded write → review sub-workflow.

## `src/apecx_integration/composition/steps/code_review_step.py`
_CodeReviewStep — LLM-backed structured critique of Python code._

- class `CodeReviewStepConfig(StepConfig)` — Configuration for CodeReviewStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `CodeReviewStep(BaseStep)` — Review Python code against a spec and emit a structured verdict.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: CodeReviewStepConfig)`
  - `def _init_from_config(self, config: CodeReviewStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str)`
  - `def _build_user_message(*, spec: str, code: str, function_name: str | None, signature: str | None)`
  - `def _parse_verdict(self, raw: str)` — Parse a JSON verdict from the LLM output.

## `src/apecx_integration/composition/steps/code_structure_validator_step.py`
_CodeStructureValidatorStep — deterministic AST inspector that gates the_

- class `CodeStructureValidatorStepConfig(StepConfig)` — Configuration for CodeStructureValidatorStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `CodeStructureValidatorStep(BaseStep)` — Deterministic AST-based validator. See module docstring.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: CodeStructureValidatorStepConfig)`
  - `def _init_from_config(self, config: CodeStructureValidatorStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _validate(self, code: str, entry_point: str)` — Run AST structural checks (delegates to the shared validator).

## `src/apecx_integration/composition/steps/code_verification_step.py`
_CodeVerificationStep — runtime verification reasoning pattern as a step._

- class `CodeVerificationStep(SubworkflowStep)` — Embed the code-verification sub-workflow as a step.
  - `def _default_inner_workflow_path(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — Run the embedded isolated-exec sub-workflow.

## `src/apecx_integration/composition/steps/code_with_tests_step.py`
_CodeWithTestsStep — generate-code-AND-tests reasoning pattern._

- class `CodeWithTestsStep(SubworkflowStep)` — Embed the code-with-tests sub-workflow as a step.
  - `def _default_inner_workflow_path(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — Run the embedded code → tests sub-workflow.

## `src/apecx_integration/composition/steps/code_write_step.py`
_CodeWriteStep — LLM-backed Python code authoring with strict gates._

- class `CodeWriteStepConfig(StepConfig)` — Configuration for CodeWriteStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `CodeWriteStep(BaseStep)` — Generate Python code from a natural-language spec.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: CodeWriteStepConfig)`
  - `def _init_from_config(self, config: CodeWriteStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def system_prompt(self)` — The loaded system prompt — exposed for tests.
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str)`
  - `def _build_user_message(*, spec: str, function_name: str | None, signature: str | None, previous_attempt: str | None, critique: str | None)`
  - `def _strip_fences(raw: str)` — Remove leading/trailing ``` ```python fences if present.
  - `def _strip_leading_prose(source: str)` — Recovery for the common LLM drift: emitting a prose
  - `def _validate_ast(self, code_source: str, *, expected_function_name: str | None)` — Parse + check function definition. Return the matched name

## `src/apecx_integration/composition/steps/combination_classification_step.py`
_Classification step for the epitope-combination feasibility workflow._

- `def _class_from_mapping(raw: Any)`
- class `CombinationClassificationStepConfig(StepConfig)`
  - `def _strip_framework_keys(cls, data: Any)`
- class `CombinationClassificationStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _readiness(evidence_parts: dict[str, Any], candidate_parts: dict[str, Any], epitopes: list[dict[str, Any]])`
  - `def _preliminary_assessment(self, evidence_parts: dict[str, Any], epitopes: list[dict[str, Any]])`
  - `def _epitope_support(epitope: dict[str, Any])`
  - `def _structural_placement(evidence_parts: dict[str, Any], epitopes: list[dict[str, Any]])`
  - `def _combination_support(evidence_parts: dict[str, Any], epitope_support: list[dict[str, Any]])`
  - `def _immunodominance(evidence_parts: dict[str, Any], epitopes: list[dict[str, Any]])`

## `src/apecx_integration/composition/steps/combination_intake_step.py`
_Intake step for the epitope-combination feasibility workflow._

- `def _clean_sequence(v: Any)`
- `def _as_int(v: Any)`
- `def _as_label(v: Any, fallback: str)`
- class `CombinationIntakeStepConfig(StepConfig)`
  - `def _strip_framework_keys(cls, data: Any)`
- class `CombinationIntakeStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: CombinationIntakeStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(input_data: dict[str, Any])`
  - `def _load_required_bundle(self, payload: dict[str, Any], *, handle_key: str, inline_key: str, label: str)`
  - `def _load_optional_bundle(self, payload: dict[str, Any], *, handle_key: str, inline_key: str)`
  - `def _bundle_parts_from_source(raw: Any)`
  - `def _candidate_is_released(candidate_parts: dict[str, Any])`
  - `def _additional_epitopes(self, raw: Any)`
  - `def _candidate_epitope(candidate_parts: dict[str, Any])`
  - `def _scope_query(payload: dict[str, Any], evidence_parts: dict[str, Any], candidate_parts: dict[str, Any], epitopes: list[dict[str, Any]])`
  - `def _epitope_scope_fragment(epitope: dict[str, Any])`
  - `def _scope_protein(evidence_parts: dict[str, Any], candidate_parts: dict[str, Any])`
  - `def _needs_candidate(self)`
  - `def _needs_released_candidate(self, candidate_parts: dict[str, Any])`
  - `def _needs_epitopes(self, reason: str)`

## `src/apecx_integration/composition/steps/combination_release_step.py`
_Release step for the epitope-combination feasibility workflow._

- `def _is_blank(v: Any)`
- class `CombinationReleaseStepConfig(StepConfig)`
  - `def _strip_framework_keys(cls, data: Any)`
- class `CombinationReleaseStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _approval_token(approval_id: Any, reason: str, query: str, protein: Any)`
  - `def _withheld_output(self, *, readiness: dict[str, Any], preliminary: dict[str, Any], reason: str, token: str, scope_query: str, protein: Any, epitopes: list[dict[str, Any]])`
  - `def _approved_output(self, *, readiness: dict[str, Any], preliminary: dict[str, Any], evidence_parts: dict[str, Any], candidate_parts: dict[str, Any], epitopes: list[dict[str, Any]], approval_id: str, scope_query: str, protein: Any)`
  - `def _validation_gaps(preliminary: dict[str, Any])`
  - `def _epitope_records(epitopes: list[dict[str, Any]], *, include_sequence: bool)`
  - `def _render_approved(self, *, readiness: dict[str, Any], preliminary: dict[str, Any], validation_gaps: list[dict[str, str]], epitopes: list[dict[str, Any]], evidence_parts: dict[str, Any], candidate_parts: dict[str, Any])`
  - `def _epitope_line(record: dict[str, Any])`
  - `def _source_lines(evidence_parts: dict[str, Any], candidate_parts: dict[str, Any])`

## `src/apecx_integration/composition/steps/consensus_aggregator_step.py`
_ConsensusAggregatorStep — deterministic voter for multi-sample fan-in._

- class `ConsensusAggregatorStepConfig(StepConfig)` — Configuration for ``ConsensusAggregatorStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_strategy(self)`
- class `ConsensusAggregatorStep(BaseStep)` — Deterministic voter that picks one candidate from a fan-out.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: ConsensusAggregatorStepConfig)`
  - `def _init_from_config(self, config: ConsensusAggregatorStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _score_candidates(self, candidates: list[dict[str, Any]], *, entry_point: str)`
- `def _ast_validate(code: str, entry_point: str)` — Inline AST checks (mirrors CodeStructureValidatorStep).
- `def _runtime_validate(code: str, entry_point: str, *, timeout: float)` — Mirror of FrameworkComplianceRunnerStep's compliance probe.

## `src/apecx_integration/composition/steps/conservation_report_step.py`
_ConservationReportStep — render a conservation result as markdown + a handle-able Bundle._

- class `ConservationReportStepConfig(StepConfig)` — Config — a StepConfig subclass, so NO ``extra='forbid'``.
- class `ConservationReportStep(BaseStep)` — Format a conservation result into markdown + a Bundle data payload.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(self, input_data: dict[str, Any])`

## `src/apecx_integration/composition/steps/conservation_score_step.py`
_ConservationScoreStep — per-column conservation over a multiple-sequence alignment (EO-52)._

- class `ConservationScoreStepConfig(StepConfig)` — Config for conservation scoring.
- class `ConservationScoreStep(BaseStep)` — Score per-column conservation over an aligned FASTA; report conserved sites + regions.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(self, input_data: dict[str, Any])`
  - `def _score(self, seqs: list[tuple[str, str]])`
- `def _parse_fasta(text: str)` — Parse FASTA into ``[(header, sequence)]``. Sequence whitespace/newlines stripped.
- `def _contiguous_regions(per_column: list[dict[str, Any]], flags: list[bool], min_length: int)` — Group contiguous conserved columns into regions (≥ ``min_length``).
- `def _append_region(regions: list[dict[str, Any]], per_column: list[dict[str, Any]], start: int, end: int, min_length: int)`

## `src/apecx_integration/composition/steps/cross_clade_aggregate_step.py`
_CrossCladeAggregateStep — broad-effectiveness breadth across clades (Req 5)._

- class `CrossCladeAggregateStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule).
  - `def _strip_framework_keys(cls, data: Any)`
- class `CrossCladeAggregateStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _per_clade_counts(clade_results: list[Any])` — Fold the MapSubworkflowStep per-clade reports into {clade, n_conserved_regions}.

## `src/apecx_integration/composition/steps/data_readiness_step.py`
_DataReadinessStep — C0 coverage summary (spec stage 0)._

- class `DataReadinessStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load.
  - `def _strip_framework_keys(cls, data: Any)`
- class `DataReadinessStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _render_markdown(coverage: list[tuple[str, int]], gaps: list[str], sources_available: int)`

## `src/apecx_integration/composition/steps/data_unit_schemas.py`
_Cross-step DataUnit schemas for the violin_bvbrc T01 vertical slice._

- class `EntityCandidate(TypedDict)` — One LLM-extracted entity candidate from Step 1.
- class `LLMSynonymProposal(TypedDict)` — One Step 3c LLM proposal.
- class `ApprovedMapping(TypedDict)` — One mapping accepted by the human reviewer at Step 4. Used as
- class `Step1Output(TypedDict)` — EntityExtractionStep output. Carries BOTH the rich entity dicts
- class `Step3aOutput(TypedDict)` — SynonymCacheLookupStep output.
- class `Step3cOutput(TypedDict)` — SynonymLLMProposalsStep output.
- class `Step4Output(TypedDict)` — ApprovalStep output. Generic — the framework shallow-merges
- class `Step4pOutput(TypedDict)` — VerifiedSynonymWritebackStep output.
- class `T01FinalOutput(TypedDict)` — ResultCollectionStep output for the T01 slice. The

## `src/apecx_integration/composition/steps/db_integration_wrappers.py`
_Nanobrain Step wrapper around the apecx-db-integration entity_

- class `EntityExtractionStepConfig(StepConfig)` — No extra fields. The wrapped function reads its LLM config from
- class `EntityExtractionStep(BaseStep)` — Workflow spec Step 1 — extract biomedical entity candidates from
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/design_gate_step.py`
_DesignGateStep — terminal approval gate for the evidence-review workflow._

- `def _is_blank(v: Any)`
- class `DesignGateStepConfig(StepConfig)`
  - `def _strip_framework_keys(cls, data: Any)`
- class `DesignGateStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _design_section(self, query: str, evidence_md: str, approval_id: str)` — Generate the approved design-hypotheses section. Phase A: a labelled
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/domain_rag_step.py`
_Nanobrain ``BaseStep`` wrapper around_

- class `DomainRagSearchStepConfig(StepConfig)` — Step config for DomainRagSearchStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `DomainRagSearchStep(BaseStep)` — Retrieval branch — semantic search over the domain RAG index.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: DomainRagSearchStepConfig)`
  - `def _init_from_config(self, config: DomainRagSearchStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/envelope_step.py`
_EnvelopeStep — wrap a workflow step's output into a standard WorkflowResult (EO-13)._

- class `EnvelopeStepConfig(StepConfig)` — EnvelopeStep config — which input keys carry the markdown / data payload.
  - `def _strip_framework_keys(cls, data: Any)`
- class `EnvelopeStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `def _handle_store(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/epitope_resolve_step.py`
_EpitopeResolveStep — resolve a BARE virus name from the query into a canonical_

- class `EpitopeResolveStepConfig(StepConfig)` — Config for EpitopeResolveStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `EpitopeResolveStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs: Any)`

## `src/apecx_integration/composition/steps/evidence_distillation_step.py`
_EvidenceDistillationStep — rank and truncate the high-volume retrieval sources_

- `def _query_terms(query: str)` — Content tokens of the query, lowercased, len >= 3, stoplist-filtered.
- `def _term_overlap(text: str, terms: list[str])` — Fraction of query terms present in ``text``, scaled to [0, 3].
- `def _blob(record: Any)` — Shape-agnostic lowercased text of a record for term-overlap scoring.
- `def _identity(record: Any)` — Stable per-record key for a reproducible sort tiebreaker.
- `def _score_publication(p: dict[str, Any], terms: list[str])` — Deterministic quality score for a PubMed record.
- `def _score_globus(g: dict[str, Any], terms: list[str])` — Deterministic quality score for a Globus/DataCite record.
- `def _score_generic(r: dict[str, Any], terms: list[str])` — Deterministic quality score for tabular rows (BV-BRC genomes, VIOLIN maps).
- `def _rank_truncate(records: Any, scorer, terms: list[str], top_n: int)` — Return (top-N by descending score, original count). Stable + deterministic.
- class `EvidenceDistillationStepConfig(StepConfig)` — Config for EvidenceDistillationStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `EvidenceDistillationStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: EvidenceDistillationStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/evidence_query_normalize_step.py`
_EvidenceQueryNormalizeStep — the PARSE-ONLY entry step of viral_epitope_analysis._

- class `EvidenceQueryNormalizeStepConfig(StepConfig)`
  - `def _strip_framework_keys(cls, data: Any)`
- class `EvidenceQueryNormalizeStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/evidence_review_synthesis_step.py`
_EvidenceReviewSynthesisStep — LLM evidence synthesis + a DETERMINISTIC_

- `def _sanitize_inline(text: Any, cap: int=300)` — Collapse a free/user-supplied string to a SINGLE inline markdown-safe line.
- `def _collapse_ws(text: Any)` — LIGHT inline-safety for EXTERNAL-DB display strings (publication / genome / VIOLIN /
- `def _group_publications_by_theme(pubs: list[dict[str, Any]])` — Group publications into deterministic topical themes (first-match, non-overlapping).
- `def render_evidence_fallback(query: str, publications: list[dict[str, Any]] | None, reason: str='', *, host_synthesizes: bool=False)` — Deterministic narrative body used when LLM synthesis FAILS its gate.
- `def render_structural_section(structural_records: list[dict[str, Any]] | None, structural_note: str | None, reasoning: dict[str, Any] | None=None)` — Render the structural-evidence Markdown section. Pure + LLM-free so the
- `def render_sources_section(bundle: dict[str, Any])` — Deterministic ``## Sources and evidence`` — every retrieved record that
- `def render_provenance_disclosure_section(bundle: dict[str, Any])` — Render the ``## Data actually used`` section — full disclosure of WHICH sequences and
- `def render_cross_reference_section(bundle: dict[str, Any])` — Render ``## Cross-referenced epitope candidates`` — the SYNTHESIZED (not siloed) view.
- `def render_cross_clade_section(bundle: dict[str, Any])` — Render ``## Epitope breadth across strains/clades`` — the broad-effectiveness signal.
- `def render_analysis_steps_section(bundle: dict[str, Any])` — Deterministic ``## Analysis steps`` — the full pipeline progression, ordered.
- `def render_coverage_section(bundle: dict[str, Any])` — Deterministic ``## Evidence coverage`` — what each source contributed.
- `def render_followups_section(query: str, bundle: dict[str, Any])` — Deterministic ``## Follow-up questions`` — 3–5 templated questions seeded from
- `def _ensure_contract_headers(narrative: str)` — Deterministically GUARANTEE the three narrative contract headings exist, in
- `def _insert_scope_caveat_if_unresolved(body: str, bundle: dict[str, Any])` — When NO viral species was resolved (``taxon_id`` is None — neither
- `def collect_structured_output(bundle: dict[str, Any])` — The STRUCTURED scientific result (not prose) — emitted alongside the markdown so the
- `def compose_evidence_markdown(narrative_body: str, query: str, bundle: dict[str, Any])` — Assemble the full contract-shaped evidence document from a narrative body
- class `EvidenceReviewSynthesisStepConfig(StepConfig)` — Config for EvidenceReviewSynthesisStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `EvidenceReviewSynthesisStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: EvidenceReviewSynthesisStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/fasta_collection_step.py`
_FastaCollectionStep — the "collect data" leg of the MUSCLE workflow._

- class `FastaCollectionStepConfig(StepConfig)` — Configuration for :class:`FastaCollectionStep`.
  - `def _strip_framework_keys(cls, data: Any)`
- class `FastaCollectionStep(BaseStep)` — Resolve the input FASTA and emit the staged-file payload.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config: FastaCollectionStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_default_path(config: FastaCollectionStepConfig)` — Resolve ``default_fasta_path`` to an absolute Path.
  - `def _count_sequences(fasta_bytes: bytes)` — Count FASTA records by counting ``>`` header lines.
  - `async def process(self, input_data: Any, **kwargs)` — Resolve the FASTA from the input or the bundled default.

## `src/apecx_integration/composition/steps/framework_compliance_runner_step.py`
_FrameworkComplianceRunnerStep — deterministic *runtime* validator._

- class `FrameworkComplianceRunnerStepConfig(StepConfig)` — Configuration for FrameworkComplianceRunnerStep.
  - `def _strip_framework_keys(cls, data: Any)`
- `def _indent(text: str, n_spaces: int)` — Indent every line of ``text`` by ``n_spaces`` characters.
- class `FrameworkComplianceRunnerStep(BaseStep)` — Deterministic runtime validator. See module docstring.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: FrameworkComplianceRunnerStepConfig)`
  - `def _init_from_config(self, config: FrameworkComplianceRunnerStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _find_target_classes(self, code: str)` — Return list of (class_name, base_module_key) tuples.
  - `def _extract_report(stdout: str)` — Pull the JSON report out of the probe's stdout.
  - `def _make_output(*, decision: str, critique: str, candidate: str, input_data: dict[str, Any])`

## `src/apecx_integration/composition/steps/functional_validation_step.py`
_FunctionalValidationStep — C3 cross-check (mandatory spec stage 3)._

- class `FunctionalValidationStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load.
  - `def _strip_framework_keys(cls, data: Any)`
- class `FunctionalValidationStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: FunctionalValidationStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `async def _fetch_real_annotation(self, bundle: dict[str, Any])` — Fetch SIFTS+UniProt+IEDB residue-level annotation for the analysed structure.
  - `def _validate(self, bundle: dict[str, Any], annotation: dict[str, Any] | None=None)` — Cross-check candidate epitope residues against functional annotation.
  - `def _real_assessment(result: dict[str, Any])` — Assessment for the REAL (UniProt+SIFTS+IEDB) annotation path.
  - `def _scan_residue_annotations(records: Any)` — Best-effort scan for residue-level functional annotation on assembled records.
  - `def _coerce_positions(fields: dict[str, Any])`
  - `def _assessment(result: dict[str, Any], sr: dict[str, Any], annotation_note: str | None=None)`

## `src/apecx_integration/composition/steps/harmonized_bundle_merge_step.py`
_HarmonizedBundleMergeStep — fan-in the per-index harmonized search results_

- `def _records_from_item(item: Any)` — Pull the full record set out of one per-index ``HarmonizedSearchExecuteStep``
- `def _available_from_item(item: Any)` — Total records AVAILABLE in the index for this query (distinct from KEPT/retrieved).
- `def _health_from_item(item: Any)` — The per-index harmonization_health VERDICT ('broken' / 'zero_floor_unclear' /
- class `HarmonizedBundleMergeStepConfig(StepConfig)` — Config for HarmonizedBundleMergeStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `HarmonizedBundleMergeStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs: Any)`

## `src/apecx_integration/composition/steps/harmonized_resolve_step.py`
_HarmonizedResolveStep — resolve a biomedical term to a canonical IRI + classify HITL state._

- `def build_resolution_plan(term: str, index: str, entity_type_str: str='')` — Resolve ``term`` to a canonical entity + classify HITL state, returning the
- class `HarmonizedResolveStep(BaseStep)` — Resolve term → canonical IRI + classify HITL state for downstream gating.
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs: Any)`

## `src/apecx_integration/composition/steps/harmonized_search_execute_step.py`
_HarmonizedSearchExecuteStep — run raw + harmonized Globus queries, emit envelope-shaped output._

- `def _quote_raw_term(term: str)` — Wrap multi-token / non-alphanumeric terms in double quotes.
- `def _iri_to_taxon_id(iri: str)` — Extract integer NCBI Taxonomy ID from an OBO IRI tail.
- `def _fetch_records(client: Any, index_uuid: str, query_body: dict[str, Any])` — Run ONE Globus search pulling every match up to the offset ceiling.
- `def _is_iri_input(term: str)` — True iff ``term`` is an HTTP(S) IRI (a canonical-IRI re-call).
- `def _select_raw_query_term(term: str, canonical_label: Any)` — Pick what to feed Globus as the raw `q=` query string.
- `def _build_filter_values(plan: dict[str, Any])` — Build the harmonized filter values per the index's shape spec.
- `def _compute_harmonization_health(raw_total: int, harm_total: int, filter_field: str, filter_values_count: int, index: str, canonical_label: Any, raw_error: str | None, harm_error: str | None)` — Classify the harmonization outcome into a structured verdict.
- `def _summarize_record(record: dict[str, Any])` — Project a harmonized (DataCite-shaped) Globus record to a small preview dict.
- `def _run_paused_envelope(plan: dict[str, Any])` — Emit a paused envelope when resolution is ambiguous.
- `def _raw_query(index: str, term: str)` — Run the anonymous RAW full-text query (no IRI / no taxon filter needed) against an
- `def _run_miss_envelope(plan: dict[str, Any])` — The term did not resolve to a taxon. DO NOT give up — fall back to a RAW full-text query
- `def _execute_globus_queries(plan: dict[str, Any])` — Run raw + harmonized queries and emit an ok envelope with divergence.
- class `HarmonizedSearchExecuteStep(BaseStep)` — Execute the raw + harmonized Globus queries (or emit a paused envelope).
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs: Any)`

## `src/apecx_integration/composition/steps/isolated_py_exec_step.py`
_IsolatedPyExecStep — run Python source in an isolated subprocess._

- class `IsolatedPyExecStepConfig(StepConfig)` — Configuration for IsolatedPyExecStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `IsolatedPyExecStep(BaseStep)` — Run Python source in a subprocess; capture stdout/stderr/returncode.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: IsolatedPyExecStepConfig)`
  - `def _init_from_config(self, config: IsolatedPyExecStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _compose_script(*, code: str, test_code: str | None, entrypoint: str | None)`
  - `def _run_subprocess(self, *, script: str, env: dict[str, str])`

## `src/apecx_integration/composition/steps/local_mafft_align_step.py`
_LocalMafftAlignStep — multiple-sequence alignment via a local MAFFT binary (EO-53)._

- class `LocalMafftAlignStepConfig(StepConfig)` — Config for local MAFFT alignment. A StepConfig subclass — no ``extra='forbid'``.
- class `LocalMafftAlignStep(BaseStep)` — Align an unaligned FASTA with a local MAFFT binary; emit the aligned FASTA.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _with_live_context(alignment: dict[str, Any], payload: dict[str, Any])` — Return ``alignment`` with the LIVE payload's taxon_id/protein re-applied.
  - `def _unwrap(self, input_data: dict[str, Any])`
  - `async def _run_mafft_container(self, fasta_text: str)` — Align in the SELF-PROVISIONING MAFFT container (no host binary). Build the image if absent,
  - `def _docker_run_mafft(self, workdir: Path, container_name: str)` — Hardened ``docker run`` of MAFFT (network-isolated, cap-dropped, mem/pids-capped, host-uid;
- `def _first_record_length(aligned_fasta: str)` — Length of the first aligned record (all records share it in an MSA).

## `src/apecx_integration/composition/steps/memory_read_step.py`
_MemoryReadStep — read prior reflexion-style lessons for the LLM._

- class `MemoryReadStepConfig(StepConfig)` — Configuration for MemoryReadStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `MemoryReadStep(BaseStep)` — Read prior memory entries and format them as ``critique`` input.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: MemoryReadStepConfig)`
  - `def _init_from_config(self, config: MemoryReadStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_memory_dir(configured: str, source_path: str | None)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _fetch(self, spec_id: str | None, spec_keywords: list[str])`
  - `def _format_lessons(entries: list)` — Format a list of MemoryEntry into a single critique string.

## `src/apecx_integration/composition/steps/memory_store.py`
_Git-tracked file-based memory store for code-writing workflows._

- class `MemoryEntry` — One reflexion-style memory entry.
  - `def to_dict(self)`
  - `def from_dict(cls, d: dict[str, Any])`
- `def _slugify(s: str)` — Filesystem-safe slug for spec_id directory naming.
- `def _now_iso()`
- `def _timestamp_token()` — Filename-safe + chronologically sortable + collision-resistant.
- `def _next_counter()`
- `def _git_short_commit(repo_dir: Path)` — Best-effort short commit hash for the repo containing ``repo_dir``.
- class `MemoryStore` — File-based, git-tracked memory store for verbal lessons.
  - `def __init__(self, root: Path)`
  - `def root(self)`
  - `def reflexions_dir(self)`
  - `def write(self, *, spec_id: str, attempt_n: int, status: Status, lesson: str, failure_keywords: Iterable[str]=(), spec_keywords: Iterable[str]=(), metadata: dict[str, Any] | None=None, skip_if_restatement: bool=True, min_lesson_chars: int=40)` — Write a new memory entry, subject to gates.
  - `def read_for_spec(self, spec_id: str, *, limit: int=3)` — Return the most recent ``limit`` entries for ``spec_id``,
  - `def read_by_keywords(self, *, spec_keywords: Iterable[str], limit: int=3)` — Fall-back retrieval when no spec_id match: return entries
  - `def all_entries(self)` — Return every entry in the store. Useful for tests / audits.
  - `def _load(self, path: Path)`
  - `def _read_newest_for_spec(self, spec_id: str)`
  - `def _normalize_keywords(keywords: Iterable[str])`
  - `def _keyword_jaccard(a: Iterable[str], b: Iterable[str])`
  - `def _lesson_jaccard(a: str, b: str)` — Token-Jaccard on alphanumeric tokens of two lessons.

## `src/apecx_integration/composition/steps/memory_write_step.py`
_MemoryWriteStep — persist a reflexion-style lesson after a cycle._

- class `MemoryWriteStepConfig(StepConfig)` — Configuration for MemoryWriteStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `MemoryWriteStep(BaseStep)` — Write a reflexion-style memory entry from a cycle's result.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: MemoryWriteStepConfig)`
  - `def _init_from_config(self, config: MemoryWriteStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_memory_dir(configured: str, source_path: str | None)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _derive_lesson(input_data: dict[str, Any])`
  - `def _classify_status(input_data: dict[str, Any])`
  - `def _derive_failure_keywords(input_data: dict[str, Any])` — Quick keyword extraction from a failure signal — used when
  - `def _build_metadata(input_data: dict[str, Any])`

## `src/apecx_integration/composition/steps/multi_sample_drafter_step.py`
_MultiSampleDrafterStep — fan-out drafter for structural consensus (SGDe-style)._

- class `MultiSampleDrafterStepConfig(StepConfig)` — Configuration for ``MultiSampleDrafterStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_role_and_temp(self)`
- class `MultiSampleDrafterStep(BaseStep)` — Fan-out drafter producing N candidates at temperature > 0.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: MultiSampleDrafterStepConfig)`
  - `def _init_from_config(self, config: MultiSampleDrafterStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_path(configured: str | None, source_path: str | None, *, default: Path | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str, model: str, base_url: str)`
  - `def _build_user_message(*, spec: str, entry_point: str | None, test_hint: str | None, function_signature: str | None)`
  - `def _extract_code(raw: str)`

## `src/apecx_integration/composition/steps/peptide_candidate_assessment_step.py`
_PeptideCandidateAssessmentStep - deterministic follow-up candidate assessment._

- `def _is_blank(v: Any)`
- `def _clean_sequence(v: Any)`
- `def _as_fraction(v: Any, default: float=0.0)`
- `def _fmt_score(v: Any)`
- `def _region_key(region: dict[str, Any])`
- `def _columns_in(regions: Any)` — Set of alignment-column indices covered by a list of inclusive ``[start, end]`` regions.
- `def _breadth_phrase(b: Any)`
- class `PeptideCandidateAssessmentStepConfig(StepConfig)` — Config for PeptideCandidateAssessmentStep.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_lengths(self)`
- class `PeptideCandidateAssessmentStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: PeptideCandidateAssessmentStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _load_evidence(self, payload: dict[str, Any])`
  - `def _bundle_parts(raw: Any)`
  - `def _readiness(parts: dict[str, Any])`
  - `def _candidate_regions(self, raw_regions: Any)`
  - `def _scope_query(parts: dict[str, Any], payload: dict[str, Any])`
  - `def _approval_token(store: Any, approval_id: Any, reason: str, query: str, protein: Any)`
  - `def _rank_candidates(self, regions: list[dict[str, Any]], parts: dict[str, Any])`
  - `def _length_score(self, length: int)`
  - `def _exposure(region: dict[str, Any], sr: Any)`
  - `def _corroboration(region: dict[str, Any], sr: Any)`
  - `def _recognition(region: dict[str, Any], residues: list[int], fv: Any)`
  - `def _breadth(region: dict[str, Any], breadth: Any)` — Pan-clade breadth for a candidate region (broad-effectiveness signal).
  - `def _needs_evidence(self)`
  - `def _needs_conserved_regions(self, readiness: dict[str, Any], parts: dict[str, Any] | None=None)`
  - `def _withheld_output(self, *, readiness: dict[str, Any], reason: str, token: str, scope_query: str, protein: Any)`
  - `def _approved_output(self, *, parts: dict[str, Any], readiness: dict[str, Any], primary: dict[str, Any], alternates: list[dict[str, Any]], approval_id: str, scope_query: str, protein: Any)`
  - `def _presentation_contexts()`
  - `def _validation_gaps(primary: dict[str, Any], parts: dict[str, Any])`
  - `def _render_approved(*, primary: dict[str, Any], alternates: list[dict[str, Any]], readiness: dict[str, Any], validation_gaps: list[str], context_notes: list[dict[str, str]], approval_id: str)`

## `src/apecx_integration/composition/steps/prompt_perturbing_drafter_step.py`
_PromptPerturbingDrafterStep — strong-form SGDe structural-consensus drafter._

- class `PromptPerturbingDrafterStepConfig(StepConfig)` — Configuration for ``PromptPerturbingDrafterStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_perturbations_and_samples(self)`
- class `PromptPerturbingDrafterStep(BaseStep)` — Fan-out drafter with per-sample prompt perturbation (strong SGDe).
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: PromptPerturbingDrafterStepConfig)`
  - `def _init_from_config(self, config: PromptPerturbingDrafterStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_path(configured: str | None, source_path: str | None, *, default: Path | None)`
  - `def system_prompt(self)`
  - `def perturbations(self)` — Read-only view of the perturbation set used for fan-out.
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str, model: str, base_url: str)`
  - `def _build_user_message(*, stem: str, spec: str, entry_point: str | None, test_hint: str | None, function_signature: str | None)`
  - `def _extract_code(raw: str)`

## `src/apecx_integration/composition/steps/protein_name_normalization_step.py`
_ProteinNameNormalizationStep — resolve a user protein name to BV-BRC's product term._

- `def _token_subset_score(product: str, user_protein: str)` — Fraction of the user's whole-word tokens present in the product's token set (0.0–1.0).
- class `ProteinNameNormalizationStepConfig(StepConfig)` — Config for BV-BRC protein-name normalization.
- class `ProteinNameNormalizationStep(BaseStep)` — Rewrite a user protein name to the BV-BRC product term for a taxon; degrade-loud passthrough.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _emit(taxon_id: Any, protein: Any, feature_type: str | None, original_protein: Any, match_source: str)` — Build the ``{"norm_out": {...}}`` output the fetch step consumes.
  - `def _unwrap(self, input_data: dict[str, Any])`
  - `def _query_catalog(self, taxon_id: int)` — Return ``{(product, feature_type): count}`` for the taxon across the configured feature
  - `def _get_json(self, path: str, query: str)`

## `src/apecx_integration/composition/steps/protein_name_normalization_subworkflow_step.py`
_ProteinNameNormalizationSubworkflowStep — nest protein_name_normalization as a passthrough-degrade step._

- class `ProteinNameNormalizationSubworkflowStep(SubworkflowStep)` — Embed protein_name_normalization; degrade to the ORIGINAL payload (never raise).
  - `def _default_inner_workflow_builder(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _extract_payload(self, input_data: dict[str, Any])` — Unwrap the framework trigger envelope ``{<my_input_du>: payload}`` to recover the original

## `src/apecx_integration/composition/steps/pubmed_harvester_step.py`
_Nanobrain ``BaseStep`` wrapper around the apecx-harvesters PubMed_

- class `PubMedHarvesterStepConfig(StepConfig)` — Step config for PubMedHarvesterStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `PubMedHarvesterStep(BaseStep)` — Retrieval branch — PubMed search + fetch.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: PubMedHarvesterStepConfig)`
  - `def _init_from_config(self, config: PubMedHarvesterStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _entity_name(entity: Any)`
  - `def _build_term(self, query: str, entities: list[Any] | None)`
  - `def _container_to_dict(container: Any)`
  - `async def _harvest(self, term: str)`
  - `def _harvest_sync(self, term: str)` — Drive the async harvest on a fresh event loop. Called from a
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/pymol_sasa_tool.py`
_PyMOL-SASA as a self-provisioning nanobrain tool._

- class `PyMOLToolBackendAdapter(ToolBackendAdapter)` — Self-provisioning PyMOL-SASA tool. Build the image if absent, then run the one-shot job.
  - `def __init__(self, *, image_tag: str=_DEFAULT_IMAGE, dockerfile_path: str=str(_DOCKERFILE), build_context: str=str(_BUILD_CONTEXT), job_script: Path=_JOB_SCRIPT, sasa_helper: Path=_SASA_HELPER)`
  - `def register(cls, **kwargs: Any)` — Build the adapter and register it with the process-global ToolBackendRegistry.
  - `async def ensure_image(self, *, on_progress: Any=None)` — Auto-build the PyMOL image if absent (build-locked, off the event loop). FAIL-LOUD
  - `def _verify_artifacts_present(self)` — FAIL-LOUD with a reinstall hint when the packaged PyMOL container artifacts are missing
  - `async def ensure_established(self, *, on_progress: Any=None)` — Establish PyMOL from its docker source (build the image if absent) — the
  - `def _docker_argv(self, workdir: Path, memory_mb: int, container_name: str)` — Hardened ``docker run`` argv (moved verbatim from structural_reasoning_step): network-isolated,
  - `async def _docker_kill(container_name: str)` — Best-effort ``docker kill <name>`` (a hung container must not outlive its run). Never
  - `async def run_sasa(self, *, pdb_id: str, structure_path: Path, kind: str, regions: list[dict[str, Any]], rsa_threshold: float, min_map_identity: float, contact_cutoff: float, memory_mb: int, timeout: float, artifacts_dir: Path, requested_chain: str | None=None, on_progress: Any=None)` — Ensure the image is built, then run the headless PyMOL SASA job one-shot and return its
  - `async def invoke(self, utd: UnifiedToolDescriptor, inputs: dict[str, Any], *, run_context_namespace: str='', **kwargs: Any)` — Framework dispatch: validate the UTD-required inputs, run SASA, key the result under the

## `src/apecx_integration/composition/steps/rag_synthesis_step.py`
_Nanobrain ``BaseStep`` wrapper around_

- `def render_rag_evidence_fallback(query: str, bundle: dict[str, Any], reason: str='', *, host_synthesizes: bool=False)` — DETERMINISTIC report body when LLM synthesis cannot run (no reachable LLM, or a synth-gate
- class `RagSynthesisStepConfig(StepConfig)` — Step config for RagSynthesisStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `RagSynthesisStep(BaseStep)` — Final step of the synthesis pipeline — assemble retrieved data
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: RagSynthesisStepConfig)`
  - `def _init_from_config(self, config: RagSynthesisStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _load_synthesis_config(path: Path)` — Load a custom synthesis_config.yml. Errors surface here so
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/reasoning_pattern_step.py`
_ReasoningPatternStep — embed an apecx reasoning-pattern workflow BY NAME._

- class `ReasoningPatternStep(SubworkflowStep)` — SubworkflowStep that resolves ``inner_workflow_name`` against apecx's
  - `def _default_workflow_search_paths(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — Run the bound reasoning-pattern inner workflow.

## `src/apecx_integration/composition/steps/rhea_genomic_analysis_step.py`
_RheaGenomicAnalysisStep — large-scale sequence-conservation analysis via RHEA._

- `def _find_nested(d: Any, key: str, depth: int=0)` — Best-effort search for ``key`` anywhere in a nested dict (report shape varies).
- class `RheaGenomicAnalysisStepConfig(StepConfig)` — Config for RheaGenomicAnalysisStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `RheaGenomicAnalysisStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: RheaGenomicAnalysisStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `def _params_unusable(self, bundle: dict[str, Any])` — Return a loud reason when the bundle can't feed the RHEA fetch, else None.
  - `async def _drive_rhea_conservation(self, taxon_id: Any, protein: str)` — Run the RHEA-MUSCLE conserved-sites inner workflow; return its report dict.
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `async def _diagnose_rhea_failure(self, exc: Exception)` — Name the REAL cause of a RHEA-leg failure so the note points to the right fix.
  - `def _unavailable_warning(reason: str)`

## `src/apecx_integration/composition/steps/rhea_muscle_align_step.py`
_RheaMuscleAlignStep — MSA via MUSCLE dispatched through the Rhea MCP server (EO-54b)._

- class `RheaMuscleAlignStepConfig(StepConfig)` — Config for Rhea-backed MUSCLE alignment. A StepConfig subclass — no ``extra='forbid'``.
- class `RheaMuscleAlignStep(BaseStep)` — Align an unaligned FASTA via MUSCLE over Rhea MCP; emit the LocalMafftAlignStep shape.
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `async def _drive_rhea_muscle(self, fasta_text: str)` — Run the verified rhea_muscle_alignment subworkflow on ``fasta_text``.
  - `async def _degrade_to_local_mafft(self, payload: dict[str, Any], cause: Exception)` — EF5 OPT-IN degrade-LOUD: Rhea unreachable + ``degrade_to_local_mafft=True`` → align with local
  - `def _unwrap(self, input_data: dict[str, Any])`

## `src/apecx_integration/composition/steps/sandboxed_novel_step.py`
_SandboxedNovelStep — run composer-generated "novel Python" INSIDE a hardened Docker sandbox._

- class `SandboxedNovelStepConfig(StepConfig)` — Configuration for :class:`SandboxedNovelStep`.
  - `def _strip_framework_keys(cls, data: Any)`
- class `SandboxedNovelStep(BaseStep)` — Run an untrusted composer-generated step inside the hardened Docker sandbox. See module docstring.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: SandboxedNovelStepConfig)`
  - `def _init_from_config(self, config: SandboxedNovelStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `async def _run_container(self, argv: list[str], container_name: str)` — Spawn the hardened container under the process-wide slot and return (returncode, stderr).
  - `async def _docker_kill(container_name: str)` — Best-effort ``docker kill <name>`` (a hung container must not outlive its run). Never

## `src/apecx_integration/composition/steps/sequence_analysis_step.py`
_SequenceAnalysisStep — BV-BRC sequence retrieval and multiple sequence alignment._

- class `SequenceAnalysisStepConfig(StepConfig)` — Config for SequenceAnalysisStep.
- class `SequenceAnalysisStep(BaseStep)` — Sequence analysis step for epitope conservation analysis.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: SequenceAnalysisStepConfig)`
  - `def _init_from_config(self, config: SequenceAnalysisStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — RETIRED — fabricated data; FAIL-LOUD instead of running (2026-06-12).
  - `def _extract_query_data(input_data: dict[str, Any])` — Extract and validate query data from input.
  - `async def _retrieve_bvbrc_sequences(self, virus_name: str)` — Retrieve viral sequences from BV-BRC API.
  - `def _sync_retrieve_bvbrc_sequences(self, virus_name: str)` — Synchronous BV-BRC sequence retrieval.
  - `async def _perform_sequence_alignment(self, sequences: list[dict[str, Any]])` — Perform multiple sequence alignment using MUSCLE.
  - `def _run_muscle_alignment(self, input_file: str, output_file: str)` — Run MUSCLE sequence alignment.
  - `def _create_mock_alignment(self, input_file: str, output_file: str)` — Create mock alignment when MUSCLE is unavailable.
  - `async def _calculate_conservation(self, sequences: list[dict[str, Any]], alignment_file: str)` — Calculate sequence conservation analysis.
  - `def _sync_calculate_conservation(self, sequences: list[dict[str, Any]], alignment_file: str)` — Synchronous conservation analysis.

## `src/apecx_integration/composition/steps/sequence_conservation_subworkflow_step.py`
_SequenceConservationSubworkflowStep — nest viral_conserved_sites as a degrade-loud step._

- class `SequenceConservationSubworkflowStep(SubworkflowStep)` — Embed viral_conserved_sites; degrade loud (never raise) so the evidence run completes.
  - `def _default_inner_workflow_builder(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _params_unusable(self, input_data: dict[str, Any])` — Return a loud reason string when the params can't feed conserved-sites, else None.
  - `def _extract_payload(self, input_data: dict[str, Any])` — Unwrap the framework trigger envelope ``{<my_input_du>: payload}`` for the pre-check.

## `src/apecx_integration/composition/steps/sequence_evidence_merge_step.py`
_SequenceEvidenceMergeStep — fan-in that folds sequence conservation into the evidence bundle._

- class `SequenceEvidenceMergeStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load time.
  - `def _strip_framework_keys(cls, data: Any)`
- class `SequenceEvidenceMergeStep(BaseStep)` — Merge the structural bundle + sequence conservation; emit the sequence stage report.
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _resolve_conservation(self, sequence: Any)` — Resolve the conserved-sites result from the sequence stage output.
  - `def _render_available(cons: dict[str, Any], sites: list[dict[str, Any]], regions: list[dict[str, Any]])`

## `src/apecx_integration/composition/steps/solution_memory_step.py`
_SolutionMemoryStep — file-backed cross-problem memory (MemFlow-style)._

- class `SolutionMemoryStepConfig(StepConfig)` — Configuration for ``SolutionMemoryStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_mode(self)`
- class `SolutionMemoryStep(BaseStep)` — Cross-problem memory of passing solutions.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: SolutionMemoryStepConfig)`
  - `def _init_from_config(self, config: SolutionMemoryStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _process_read(self, input_data: dict[str, Any])`
  - `def _process_similarity_read(self, input_data: dict[str, Any])` — MemFlow tier-2: cross-category top-K embedding-similarity read.
  - `def _fallback_to_exact_read(self, *, spec: str, category: str, input_data: dict[str, Any], reason: str)` — Tier-2 graceful degradation to tier-1 (exact-category) lookup.
  - `def _load_embedding_model(self)` — Lazy-load sentence-transformers model on first similarity_read call.
  - `def _process_record(self, input_data: dict[str, Any])`
  - `def _load_store_safe(self)`
  - `def _save_store_safe(self, store: dict[str, list[str]])`

## `src/apecx_integration/composition/steps/structural_evidence_step.py`
_StructuralEvidenceStep — pull PDB + EMDB structural records for the query._

- `def _species_name_from_dict(taxon_id: int | str)` — Resolve an NCBI taxon_id -> canonical species label via the synonym dictionary (the
- class `StructuralEvidenceStepConfig(StepConfig)` — Config for StructuralEvidenceStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `StructuralEvidenceStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: StructuralEvidenceStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `def _search_source(self, query: str, source: str, publisher: str, taxon_id: int | str | None, species_name: str | None)` — Taxon-precise structural query for one source (E3-2). Delegates to the
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/structural_reasoning_step.py`
_StructuralReasoningStep — map sequence conservation onto 3D structure (E2-P)._

- `def _artifacts_dir()` — The durable artifacts directory (``$APECX_ARTIFACTS_DIR`` or ``~/.apecx/artifacts``),
- `def _record_text(rec: dict[str, Any])` — Lower-cased DataCite title + subjects for a structural record (the relevance
- `def _kw_match(keyword: str, text: str)`
- `def _protein_terms(protein: Any)`
- `def _score_record(rec: dict[str, Any], protein_terms: list[str])`
- `def rank_structural_records(records: list[dict[str, Any]], protein: Any=None)` — Rank structural records for epitope relevance (highest first).
- class `StructuralReasoningStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load.
  - `def _strip_framework_keys(cls, data: Any)`
- class `StructuralReasoningStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: StructuralReasoningStepConfig)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `async def _reason(self, regions: list[dict[str, Any]], records: list[dict[str, Any]], protein: Any=None)` — Run the structural reasoning, returning ``(result_dict, degrade_note)``.
  - `def _selection(entry: dict[str, Any], considered: int)`
  - `def _structure_summary(raw: dict[str, Any], entry: dict[str, Any])` — Per-structure provenance row for an analysed (ok) structure.
  - `async def _analyze_one(self, pdb_id: str, regions: list[dict[str, Any]])` — Run the containerized PyMOL analysis for ONE structure, degrade-loud per structure.
  - `async def _run_container(self, pdb_id: str, regions: list[dict[str, Any]])` — Fetch the biological assembly (host), then run the headless PyMOL job.
  - `async def _run_pymol_on_file(self, pdb_id: str, structure_path: Path, kind: str, regions: list[dict[str, Any]], *, requested_chain: str | None=None)` — Delegate the SASA run to the self-provisioning PyMOL tool
  - `def _selection_prefix(result: dict[str, Any])` — Human-readable structure-selection rationale (P1 relevance ranking).
  - `def _render_markdown(result: dict[str, Any], note: str | None)`
- `def _fetch_au_cif(pdb_id: str)` — Download (and cache) the immutable RCSB asymmetric-unit ``.cif`` for ``pdb_id``.
- `def _fetch_mmcif_assembly(pdb_id: str)` — Download (and cache) RCSB biological assembly 1 in mmCIF (``{pdb}-assembly1.cif.gz``,
- `def _fetch_structure(pdb_id: str, *, prefer_assembly: bool=True)` — Download (and cache) the immutable RCSB structure for ``pdb_id``.

## `src/apecx_integration/composition/steps/synthesis_context_assembly_step.py`
_Nanobrain ``BaseStep`` that assembles all four retrieval branches_

- class `SynthesisContextAssemblyStepConfig(StepConfig)` — Step config for SynthesisContextAssemblyStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `SynthesisContextAssemblyStep(BaseStep)` — Fan-in assembly step — runs three retrieval branches
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: SynthesisContextAssemblyStepConfig)`
  - `def _init_from_config(self, config: SynthesisContextAssemblyStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _extract_search_terms(query: str, entities: list[Any] | None, query_terms: list[Any] | None)` — Build (name, type) pairs for the VIOLIN/BV-BRC lookup.
  - `def _rag_search(self, query: str)`
  - `def _violin_bvbrc_lookup(self, terms: list[tuple[str, str]])`
  - `def _globus_search(self, query: str)` — Query the APECx Globus Search index.
  - `def _pubmed_harvest(self, query: str, entities: list[Any] | None)` — Drive the PubMed harvest synchronously on a fresh loop.
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/composition/steps/task_category_router_step.py`
_TaskCategoryRouterStep — deterministic retrieval-grounded enrichment._

- class `TaskCategoryRouterStepConfig(StepConfig)` — Configuration for TaskCategoryRouterStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `TaskCategoryRouterStep(BaseStep)` — Deterministic category classifier + example enricher.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: TaskCategoryRouterStepConfig)`
  - `def _init_from_config(self, config: TaskCategoryRouterStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_examples_dir(configured: str | None, source_path: str | None)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _classify(*, spec: str, entry_point: str, fallback_mode: str='nanobrain')` — First-match-wins keyword classification.
  - `def _classify_mbpp(spec_lower: str)` — MBPP sub-category classification by keyword (not exhaustive;

## `src/apecx_integration/composition/steps/taxon_candidate_review_step.py`
_TaxonCandidateReviewStep — LLM picks the right taxon, then a deterministic CDS-coverage gate._

- `def _is_underspecified_taxon(name: str)` — True iff a resolved taxon name is a non-specific umbrella (so the analysis would run on a
- `def _most_specific(cands: list[dict[str, Any]])` — Drop candidates that are an ANCESTOR of another candidate (their taxon_id appears in another's
- `def _distinct_species_reps(cands: list[dict[str, Any]])` — One representative candidate per distinct SPECIES (input order preserved, after collapsing
- `def _syndrome_category(query: str)` — The bare disease-category term in a query ("...the hepatitis virus...") that is NOT a specific
- `def _clear_cache()` — Test seam: drop the process-lifetime per-query verdict cache.
- `def _already_resolved(bundle: dict[str, Any])` — True when the deterministic dict resolver already won (the fallback is then skipped).
- class `TaxonCandidateReviewStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load time.
  - `def _strip_framework_keys(cls, data: Any)`
- class `TaxonCandidateReviewStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(self, input_data: dict[str, Any])`
  - `def _find_candidate(bundle: dict[str, Any], taxon_id: int)`
  - `def _parse_matches(text: str, valid_ids: set[int])` — Return the SET of candidate taxon_ids the LLM listed as the same virus as the query.
  - `def _cds_count(self, taxon_id: int)` — Exact ``genome_feature`` CDS count for a taxon (shared with the search step).
  - `def _finalize_winner(self, bundle: dict[str, Any], taxon_id: int, name: str, genomes: int | None, cds: int | None)`
  - `def _needs_clarification(self, bundle: dict[str, Any], name: str, taxon_id: int)` — Emit a needs_input CLARIFICATION: the request resolved only to an under-specified umbrella
  - `def _needs_clarification_multi(self, bundle: dict[str, Any], reps: list[dict[str, Any]])` — Clarification for a query matching MULTIPLE distinct viral species — list them as candidate
  - `def _needs_clarification_syndrome(self, bundle: dict[str, Any], category: str)` — Clarification for a bare DISEASE/SYNDROME query — name the category, give per-syndrome
  - `def _set_miss(bundle: dict[str, Any], note: str)`
  - `def _miss(self, bundle: dict[str, Any], key: str, note: str)`

## `src/apecx_integration/composition/steps/taxon_synonym_generation_step.py`
_TaxonSynonymGenerationStep — LLM-driven candidate virus-name generation (taxon-resolution_

- `def _already_resolved(bundle: dict[str, Any])` — True when the deterministic dict resolver already won (the fallback is then skipped).
- `def _load_system_prompt(filename: str)` — Load the ``system_prompt`` block from a co-located YAML (no hardcoded prompts in code).
- `def _dedup_ci(names: list[str])` — De-duplicate names case-insensitively, dropping blanks, preserving first-seen order.
- `def _parse_name_lines(text: str)` — Parse the LLM's one-name-per-line output: strip bullets/numbering + blanks.
- class `TaxonSynonymGenerationStepConfig(StepConfig)` — Config — ``extra='forbid'`` (workspace rule): YAML typos raise at config-load time.
  - `def _strip_framework_keys(cls, data: Any)`
- class `TaxonSynonymGenerationStep(BaseStep)`
  - `def _get_config_class(cls)`
  - `def _init_from_config(self, config, component_config, dependencies)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _unwrap(self, input_data: dict[str, Any])`

## `src/apecx_integration/composition/steps/tdr_iteration_step.py`
_TdrIterationStep — one iteration of the TDR refine loop, framework-native._

- class `TdrIterationStepConfig(StepConfig)` — Configuration for TdrIterationStep.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate_mode(self)`
- class `TdrIterationStep(BaseStep)` — One iteration of the TDR refine loop.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: TdrIterationStepConfig)`
  - `def _init_from_config(self, config: TdrIterationStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_sibling_path(configured: str, source_path: str | None)`
  - `async def process(self, input_data: Any, **kwargs)`
  - `def _unwrap_envelope(input_data: dict[str, Any])` — Detect initial-envelope vs back-edge-wrapped input.
  - `def _format_critique(exec_output: dict[str, Any])` — Turn an exec result into a critique string for the writer's

## `src/apecx_integration/composition/steps/test_write_step.py`
_TestWriteStep — LLM-backed pytest authoring._

- class `TestWriteStepConfig(StepConfig)` — Configuration for TestWriteStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `TestWriteStep(BaseStep)` — Author pytest tests for a given function.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: TestWriteStepConfig)`
  - `def _init_from_config(self, config: TestWriteStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str)`
  - `def _build_user_message(*, code: str, spec: str, function_name: str | None)`
  - `def _strip_fences(raw: str)`
  - `def _validate_tests(self, test_code: str)`

## `src/apecx_integration/composition/steps/unlimited_synthesis_assembly_step.py`
_Unlimited synthesis context assembly step - removes arbitrary result caps._

- class `UnlimitedSynthesisAssemblyStepConfig(StepConfig)` — Configuration for UnlimitedSynthesisAssemblyStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `UnlimitedSynthesisAssemblyStep(BaseStep)` — Enhanced assembly step that removes arbitrary result limits.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: UnlimitedSynthesisAssemblyStepConfig)`
  - `def _init_from_config(self, config: UnlimitedSynthesisAssemblyStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _unlimited_violin_bvbrc_lookup(self, terms: list[tuple[str, str]])` — Unlimited VIOLIN/BV-BRC lookup - retrieves ALL relevant data.
  - `def _enhanced_rag_search(self, query: str)` — Enhanced RAG search with increased result count.
  - `def _enhanced_globus_search(self, query: str)` — Enhanced Globus search with increased result count.
  - `def _enhanced_pubmed_harvest(self, query: str, entities: list[Any] | None)` — Enhanced PubMed harvest with increased result count.
  - `def _extract_search_terms(query: str, entities: list[Any] | None, query_terms: list[Any] | None)` — Build (name, type) pairs for VIOLIN/BV-BRC lookup.
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — Perform unlimited multi-source data assembly.

## `src/apecx_integration/composition/steps/violin_bvbrc_context_step.py`
_Default data-directory constants for the VIOLIN + BV-BRC tabular_

_(no module-level classes or functions)_

## `src/apecx_integration/composition/steps/viral_ontology_manager.py`
_Viral immunology ontology management utilities._

- class `ViralFamily` — Represents a viral family with associated metadata.
- class `VirusEntry` — Represents a specific virus with detection patterns.
- class `ImmunologyTerm` — Represents an immunology concept with context.
- class `QueryClassification` — Result of viral immunology query classification.
- class `ViralOntologyManager` — Manages viral immunology ontology and query classification.
  - `def __init__(self, ontology_config: dict[str, Any])` — Initialize from ontology configuration dict.
  - `def from_yaml(cls, config_path: Path)` — Load ontology from YAML configuration file.
  - `def _load_ontology(self, config: dict[str, Any])` — Load virus families, viruses, and immunology terms from config.
  - `def classify_query(self, query: str)` — Classify a query for viral immunology research intent.
  - `def _detect_viruses(self, query_lower: str)` — Detect specific virus mentions in query.
  - `def _detect_viral_families(self, query_lower: str)` — Detect viral family mentions in query.
  - `def _detect_proteins(self, query_lower: str)` — Detect protein mentions in query.
  - `def _detect_immunology_concepts(self, query_lower: str)` — Detect immunology concept mentions in query.
  - `def _determine_research_type(self, immunology_terms: list[ImmunologyTerm])` — Determine research type based on detected immunology concepts.
  - `def _calculate_confidence(self, viruses: list[VirusEntry], families: list[ViralFamily], proteins: list[str], immunology: list[ImmunologyTerm])` — Calculate confidence score for viral immunology classification.
- `def load_default_ontology()` — Load the default viral immunology ontology configuration.

## `src/apecx_integration/composition/steps/web_search_context_step.py`
_WebSearchContextStep — inject web-search context into the drafter prompt._

- class `WebSearchContextStepConfig(StepConfig)` — Configuration for ``WebSearchContextStep``.
  - `def _strip_framework_keys(cls, data: Any)`
  - `def _validate(self)`
- class `WebSearchContextStep(BaseStep)` — Enrich the drafter's ``code_spec`` with web search context.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: WebSearchContextStepConfig)`
  - `def _init_from_config(self, config: WebSearchContextStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_tool_config_path(raw: str, source_path: str | None)` — Resolve the tool config path.
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _derive_query(self, spec: str)` — Turn a problem spec into a search query.
  - `def _format_context(self, results: list[dict[str, str]])` — Format search results into a compact, LLM-readable block.

## `src/apecx_integration/composition/steps/workflow_analysis_step.py`
_WorkflowAnalysisStep — pure-Python structural analyzer for workflow YAMLs._

- class `WorkflowAnalysisStepConfig(StepConfig)` — Configuration for WorkflowAnalysisStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `WorkflowAnalysisStep(BaseStep)` — Analyze a workflow YAML and emit a structured report.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: WorkflowAnalysisStepConfig)`
  - `def _init_from_config(self, config: WorkflowAnalysisStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _load_workflow_dict(self, input_data: dict[str, Any])`
  - `def _analyze(self, wf: dict[str, Any])`
  - `def _collect_data_unit_names(node: Any)`
  - `def _analyze_step(name: str, entry: dict[str, Any])`
  - `def _analyze_link(name: str, entry: dict[str, Any])`
  - `def _summarize_topology(steps: list[dict[str, Any]], links: list[dict[str, Any]])`
  - `def _detect_issues(self, *, steps_report: list[dict[str, Any]], links_report: list[dict[str, Any]], workflow_input_dus: list[str], workflow_output_dus: list[str], config_version: int)`

## `src/apecx_integration/composition/steps/workflow_summarizer_step.py`
_WorkflowSummarizerStep — LLM-backed plain-English explainer._

- class `WorkflowSummarizerStepConfig(StepConfig)` — Configuration for WorkflowSummarizerStep.
  - `def _strip_framework_keys(cls, data: Any)`
- class `WorkflowSummarizerStep(BaseStep)` — Explain a workflow in plain English to a domain expert.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: WorkflowSummarizerStepConfig)`
  - `def _init_from_config(self, config: WorkflowSummarizerStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `def _resolve_prompt_path(configured: str | None, source_path: str | None)`
  - `def system_prompt(self)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _invoke_llm(self, *, user_message: str)`
  - `def _build_user_message(analysis: dict[str, Any])`

## `src/apecx_integration/composition/tools/__init__.py`
_Project-specific Tool adapters that wrap nanobrain tools for the_

_(no module-level classes or functions)_

## `src/apecx_integration/composition/tools/bv_brc_snapshot_tool.py`
_``BVBRCSnapshotTool`` — reads from local BV-BRC TSV/FASTA snapshots_

- `def _resolve_snapshot_dir()` — Resolve the snapshot directory from env + default.
- `def _read_tsv(path: Path)`
- `def _extract_md5_from_header(line: str)` — Extract a 32-char-hex md5 from a FASTA header line.
- `def _read_fasta_by_md5(path: Path)` — Parse the annotated FASTA into ``{md5_hex: aa_sequence}``.
- class `BVBRCSnapshotTool(BVBRCTool)` — Snapshot-aware variant of ``BVBRCTool``. Overrides the three
  - `def snapshot_dir(self)`
  - `async def download_alphavirus_genomes(self, limit: int | None=None)` — Load genomes from ``<snapshot>/alphavirus_genomes.tsv``.
  - `async def filter_genomes_by_size(self, genomes: list[GenomeData])` — Snapshot-variant pass-through.
  - `async def get_unique_protein_md5s(self, genome_ids: list[str])` — Load protein rows from ``<snapshot>/alphavirus_proteins.tsv``
  - `async def get_feature_sequences(self, md5_list: list[str])` — Populate aa_sequences by reading the annotated FASTA.
  - `async def execute_command(self, command: list[str], **kwargs: Any)` — Block any attempt to shell out to the BV-BRC CLI from a

## `src/apecx_integration/composition/transforms.py`
_Sample data-unit transforms for nanobrain TransformLink._

- `def entities_to_query_terms(input_data: dict[str, Any])` — Reshape ``EntityExtractionStep`` output for ``SynonymCacheLookupStep``.
- `def llm_proposals_to_approved_mappings(input_data: dict[str, Any])` — Reshape ``ApprovalStep`` passthrough output for

## `src/apecx_integration/composition/workflow_requires_llm.py`
_V4 — compute whether a workflow needs an LLM (the loud-disclosure contract)._

- class `RequiresLlmResult`
- `def _class_is_llm_bearing(class_path: str, _depth: int=0)` — True/False if determinable from the class's module source; None if the
- `def _is_subworkflow_class(class_path: str)`
- `def compute_requires_llm(workflow_dict: dict[str, Any])` — Compute whether ``workflow_dict`` is LLM-bearing.
- `def loaded_workflow_llm_steps(workflow: Any)` — ``[(step_name, llm_role)]`` for each LLM-bearing step of a LOADED workflow.

## `src/apecx_integration/composition/workflow_spec.py`
_Minimal workflow spec + deterministic template expander._

- class `WorkflowStepSpec(BaseModel)` — One step in the minimal spec.
- class `WorkflowLinkSpec(BaseModel)` — One link in the minimal spec.
- class `MinimalWorkflowSpec(BaseModel)` — The full minimal spec the LLM emits.
- class `SpecExpansionError(ValueError)` — A spec referenced a class that the catalog doesn't have, or
- `def expand_spec(spec: MinimalWorkflowSpec, catalog: list[CatalogComponent])` — Expand a minimal spec into a full workflow YAML dict.
- `def _resolve_step_class(name: str, by_full: dict[str, CatalogComponent], by_leaf: dict[str, list[CatalogComponent]], warnings: list[str], step_id: str)` — Map a leaf class name OR a full path to a catalog component.
- `def _make_link_id(source: str, target: str, idx: int)` — Stable, readable link id derived from source/target.
- `def _workflow_du_block(name: str)` — The shape of a workflow-level DataUnit block.

## `src/apecx_integration/composition/workflow_validator.py`
_Framework-native pre-execution validator for composed workflow YAML._

- class `WorkflowViolation` — A single framework-rule violation in a composed workflow.
- class `WorkflowValidationError(ComposerResponseError)` — The LLM emitted a workflow that violates framework rules.
  - `def __init__(self, violations: tuple[WorkflowViolation, ...], *, yaml_text: str | None=None)`
  - `def _format_message(self)`
  - `def to_feedback_payload(self)` — Format violations as an LLM-facing correction prompt.
- `def _import_class(class_path: str)` — Best-effort import of a dotted class path.
- `def _is_inline_config_supported(target_class: type)` — Delegate to the framework's authoritative classifier.
- `def _is_step_subclass(target_class: type)` — True iff ``target_class`` is a ``BaseStep`` (or alias) subclass.
- `def _is_link_subclass(target_class: type)`
- `def _ref_target_is_workflow_level(ref: str, workflow_dict: dict[str, Any])` — True iff ``ref`` is a bare name matching a workflow-level data unit.
- `def _ref_target_is_step_qualified(ref: str, workflow_dict: dict[str, Any])` — True iff ``ref`` is ``<step_id>.<du_name>`` with a known step_id.
- `def validate_workflow_against_framework(workflow_dict: dict[str, Any], *, catalog_yaml_paths: dict[str, str] | None=None, catalog_class_paths: set[str] | None=None)` — Run the framework-rule pass over a parsed composed workflow.
- `def _validate_steps_block(workflow_dict: dict[str, Any], catalog_yaml_paths: dict[str, str] | None, catalog_class_paths: set[str] | None=None)`
- `def _validate_links_block(workflow_dict: dict[str, Any])`

## `src/apecx_integration/composition/workflows/benchmark_max_power_websearch_lightweight_builder.py`
_Lightweight WorkflowBuilder variant of benchmark_max_power_websearch._

- `def build_max_power_websearch_workflow_lightweight()` — Programmatic builder for benchmark_max_power_websearch.
- `def _nest_link_configs(wf_config: dict)` — Repair the builder's flat-link shape into the framework's nested shape.

## `src/apecx_integration/composition/workflows/benchmark_structural_consensus_lightweight_builder.py`
_Lightweight nanobrain builder for the structural-consensus scaffold._

- `def build_structural_consensus_workflow_lightweight()` — Programmatic builder for the structural-consensus scaffold.
- `def _nest_link_configs(wf_config: dict)` — Repair the builder's flat-link shape into the framework's nested shape.

## `src/apecx_integration/composition/workflows/conserved_epitope_candidate_assessment/__init__.py`
_conserved_epitope_candidate_assessment - approved follow-up candidate assessment._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/workflows/conserved_epitope_candidate_assessment/builder.py`
_conserved_epitope_candidate_assessment - lightweight nanobrain builder._

- `def _du(name: str)`
- `def _trig(du: str)`
- `def _candidate_assessment_workflow_builder()`
- `def build_conserved_epitope_candidate_assessment_workflow()` — Construct and load the conserved_epitope_candidate_assessment workflow.

## `src/apecx_integration/composition/workflows/epitope_combination_feasibility_assessment/__init__.py`
_epitope_combination_feasibility_assessment - approved follow-up grouping assessment._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/workflows/epitope_combination_feasibility_assessment/builder.py`
_epitope_combination_feasibility_assessment - lightweight nanobrain builder._

- `def _du(name: str)`
- `def _trig(du: str)`
- `def _combination_feasibility_workflow_builder()`
- `def build_epitope_combination_feasibility_assessment_workflow()` — Construct and load the epitope_combination_feasibility_assessment workflow.

## `src/apecx_integration/composition/workflows/harmonized_index_search/builder.py`
_harmonized_index_search — a ONE-step inner workflow: run HarmonizedSearchExecuteStep_

- `def _du(name: str)`
- `def build_harmonized_index_search_workflow()` — Build + load the 1-step harmonized-search-for-one-index inner workflow.

## `src/apecx_integration/composition/workflows/open_rosalind_rhea_lightweight_builder.py`
_Lightweight WorkflowBuilder variant of the open_rosalind_rhea workflow._

- `def build_open_rosalind_rhea_workflow_lightweight()` — Programmatic builder for the open_rosalind_rhea workflow.
- `def _nest_link_configs(wf_config: dict)` — Repair the builder's flat-link shape into the framework's nested shape.

## `src/apecx_integration/composition/workflows/protein_name_normalization/__init__.py`
_protein_name_normalization — resolve a user protein name to the BV-BRC product term._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/workflows/protein_name_normalization/builder.py`
_protein_name_normalization — resolve a user protein name to the BV-BRC product term._

- `def _du(name: str)`
- `def _trig(du: str)`
- `def _normalization_builder()` — Shared workflow_input → normalize cascade (no terminal step / output bound).
- `def build_protein_name_normalization_core_workflow()` — No-arg builder: ``workflow_output`` is the ``normalize`` step's dict directly (no EnvelopeStep).

## `src/apecx_integration/composition/workflows/viral_conserved_sites/__init__.py`
_viral_conserved_sites — find conserved protein sites across virus strains._

_(no module-level classes or functions)_

## `src/apecx_integration/composition/workflows/viral_conserved_sites/builder.py`
_viral_conserved_sites — the conserved-sites workflow, built the LIGHTWEIGHT way (EO-53)._

- `def _du(name: str)`
- `def _trig(du: str)`
- `def _conserved_sites_core_builder(aligner: str, max_sequences: int=25)` — Build the shared fetch→align→conserve→report cascade (NO terminal step / output bound).
- `def build_viral_conserved_sites_workflow(aligner: str='mafft')` — Construct + load the viral_conserved_sites workflow (catalog lightweight entry-point).
- `def build_viral_conserved_sites_core_workflow(aligner: str='mafft')` — No-arg NESTING variant (E2-C1): the same fetch→align→conserve→report cascade, but the
- `def build_viral_conserved_sites_rhea_core_workflow()` — No-arg NESTING variant for the RHEA-MUSCLE path (large-scale genomic analysis leg).
- `def build_viral_conserved_sites_muscle_workflow()` — No-arg catalog entry-point for the MUSCLE-via-Rhea variant (EO-54b).
- `def build_clade_conservation_core_workflow(aligner: str='mafft')` — No-arg NESTING variant for the PER-CLADE re-analysis loop (Req 5 looped execution).

## `src/apecx_integration/composition/workflows/viral_epitope_analysis/__init__.py`
_viral_epitope_analysis — identify and analyze B-cell epitopes / antigenic sites on a_

_(no module-level classes or functions)_

## `src/apecx_integration/composition/workflows/viral_epitope_analysis/builder.py`
_viral_epitope_analysis — lightweight builder (Track D)._

- `def _du(name: str)`
- `def _trig(du: str)`
- `def _evidence_workflow_builder()` — Build (but do NOT load) the WorkflowBuilder. Exposed so tests can exercise
- `def build_viral_epitope_analysis_workflow()` — Construct + load the viral_epitope_analysis workflow (catalog entry-point).

## `src/apecx_integration/config/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/_serve_lifecycle.py`
_Serve-process lifecycle — PID file + stale-bind detection + stop._

- `def pid_file()`
- `def _alive(pid: int)` — True iff a process with ``pid`` currently exists.
- `def _read_record()` — Parse the pid file into ``(pid, host, port)``; None if absent/malformed.
- `def read_running_pid()` — The live server's pid from the pid file, or None if absent/stale.
- `def write_pid(host: str, port: int)` — Atomically record this process + the address it is serving (tmp + os.replace).
- `def remove_pid()`
- `def port_in_use(host: str, port: int)` — True iff something is already LISTENING on ``host:port``.
- `def stop_running(*, timeout: float=10.0)` — Stop the pid-file server: SIGTERM, wait up to ``timeout``, then SIGKILL.
- `def wait_port_free(host: str, port: int, *, timeout: float=10.0)` — Poll until ``host:port`` is free (post-stop) or ``timeout`` elapses.

## `src/apecx_integration/control_plane/accounting/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/accounting/cost_estimator.py`
_T07 pre-submission allocation estimator (minimal implementation)._

- `def _per_step_core_hours(step_id: str, step_config: dict[str, Any])` — Pick a core-hour estimate for a single step.
- `def _endpoint_factor(endpoint: str)` — Per-endpoint multiplier on the total.
- `def estimate_workflow_cost(workflow_config: dict[str, Any], endpoint: str='local')` — Estimate the compute cost of running a workflow.
- `def estimated_wall_time_seconds(total_core_hours: float)` — Convert core-hours to wall-time seconds under the dumb

## `src/apecx_integration/control_plane/app.py`
_FastAPI application entrypoint for the Control Plane (Tier 2)._

- `def _sanitize_request_id(inbound: str | None)` — Sanitize a caller-supplied ``X-Request-ID`` (ASCII alnum/-/_ only, ≤64 chars) so it can't
- `def _resolve_sweep_interval()` — Seconds between stuck-run sweeps. Overridable via ``APECX_RUN_SWEEP_INTERVAL_SECONDS``
- `async def _run_sweep_loop(sweeper, *, interval_seconds: float, stale_after)` — Periodically run the RunStateSweeper so a run whose executor died mid-flight is reaped to
- `def create_app(engine: Engine | None=None, *, composer=None, approval_policy=None, local_executor=None, recorder: ProvenanceRecorder | None=None, start_monitor: bool=False)` — Build the FastAPI app.
- `def _build_components_from_env(engine: Engine, *, recorder: ProvenanceRecorder | None=None)` — Build composer + approval policy + local executor from env vars.
- `def _serve(args: argparse.Namespace)`
- `def _teardown(args: argparse.Namespace)`
- `def _stop(args: argparse.Namespace)`
- `def _restart(args: argparse.Namespace)`
- `def main(argv: list[str] | None=None)`

## `src/apecx_integration/control_plane/db.py`
_Control Plane SQLAlchemy engine + session factory (T09)._

- `def cp_home_dir()` — The apecx-cp home directory (holds ``cp.db`` and ``cp.pid``); created if missing.
- `def get_db_url()` — Resolve the Control Plane DB URL.
- `def make_engine(url: str | None=None, *, echo: bool=False)` — Create an engine with SQLite WAL mode wired in on connect.
- `def _install_sqlite_pragmas(engine: Engine)` — Enable WAL journal mode and NORMAL sync on every new SQLite connection.
- `def make_session_factory(engine: Engine)`

## `src/apecx_integration/control_plane/dependencies.py`
_FastAPI dependencies for the Control Plane._

- `def get_session(request: Request)`
- `def get_session_factory(request: Request)` — Return the raw factory so routes that ``await`` external work
- `def get_recorder(request: Request)`
- `def get_composer(request: Request)` — Return the Composer the app was built with, or raise 503.
- `def get_composer_or_none(request: Request)` — Non-raising variant for routes that want to keep body
- `def require_composer(composer: Composer | None)` — Raise the canonical 503 if composer is None. Use after body
- `def get_approval_policy(request: Request)`
- `def get_approval_policy_or_none(request: Request)`
- `def require_approval_policy(policy: ApprovalPolicy | None)`
- `def get_local_executor(request: Request)`
- `def get_local_executor_or_none(request: Request)`
- `def require_local_executor(executor: LocalExecutor | None)`

## `src/apecx_integration/control_plane/executors/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/executors/local.py`
_T01 P2 — local executor for composed workflows._

- `def _format_step_diagnostics(started: list[str], done: set[str], failures: list[tuple[str, str, str]])` — Build a short suffix naming the in-flight and/or failed step(s) from captured G37 events.
- class `ExecutionResult`
- class `LocalExecutor` — Local Tier-4 executor. Synchronous-looking API, async under the
  - `def __init__(self, *, session_factory: sessionmaker[Session], artifact_store: ArtifactStore, recorder: ProvenanceRecorder, workflow_base_dir: Path, actor: str='local_executor', cascade_timeout_seconds: float=600.0, cascade_settle_ms: int=200, default_payload: dict[str, Any] | None=None, allow_empty_input: bool=False, allow_empty_output: bool=False, config_search_paths: list[str] | None=None)`
  - `async def execute(self, run_id: UUID)`
  - `def _terminal_result(self, *, run_id: UUID, intended_status: RunStatus, transitioned: bool, intended_reason: str | None, output_artifact_id: UUID | None)` — Construct the ExecutionResult, honoring the ACTUAL DB state.
  - `def _validate_and_fetch(self, run_id: UUID)`
  - `def _stage_workflow(self, yaml_path: Path, run_root: Path)` — Build a staging directory that nanobrain can load from.
  - `def _persist_output(self, run_id: UUID, raw_result: object)`
  - `def _mark_completed(self, run_id: UUID, output_artifact_id: UUID)` — Transition run to COMPLETED + emit RUN_COMPLETED.
  - `def _mark_failed(self, run_id: UUID, reason: str, *, failure_class: str, in_session: Session | None=None)` — Transition run to FAILED + emit RUN_FAILED.
  - `def _read_actual_status(self, run_id: UUID)` — Fetch the run's current status from the DB. Used by
  - `def _classify_runtime_violation(cls, exc: BaseException)` — Map an exception's text onto an A1-style rule_id.
  - `def _record_runtime_violation(self, *, run_id: UUID, exc: BaseException, failure_class: str)` — Persist a structured runtime violation onto the run's
- `def run_sync(executor: LocalExecutor, run_id: UUID)` — Sync wrapper for callers outside an async context (e.g. CLI,

## `src/apecx_integration/control_plane/gates/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/infra/__init__.py`
_Infrastructure governance for the Control Plane._

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/infra/apptainer_runtime.py`
_Apptainer-backed implementation of :class:`ContainerRuntime`._

- class `_CommandResult`
- class `_SubprocessRunner` — Indirection so command construction is unit-testable without
  - `def run(self, cmd: list[str], *, check: bool=True, timeout: float | None=300.0)`
- `def _managed_data_path(config: PostgresConfig)` — Return the actual on-disk path the runtime manages.
- class `ApptainerRuntime`
  - `def __init__(self, *, binary: str='apptainer', runner: _SubprocessRunner | None=None)`
  - `def build_ensure_commands(self, config: PostgresConfig)` — The exact commands we would invoke to bring Postgres up.
  - `def build_teardown_commands(self, config: PostgresConfig, *, remove_data: bool)`
  - `def build_is_running_command(self)`
  - `def ensure_postgres_running(self, config: PostgresConfig)`
  - `def is_postgres_running(self)`
  - `def teardown(self, config: PostgresConfig, *, remove_data: bool)`
  - `def _wait_for_healthy(self, config: PostgresConfig, *, timeout_seconds: int)` — Poll Postgres until it accepts connections, or timeout.
  - `def _psycopg_probe(self, config: PostgresConfig, psycopg_module)`
  - `def _pg_isready_probe(self, config: PostgresConfig)` — Shell out to ``apptainer exec <instance> pg_isready`` as a

## `src/apecx_integration/control_plane/infra/docker_runtime.py`
_Docker-backed implementation of :class:`ContainerRuntime`._

- `def _repo_root()`
- class `DockerRuntime`
  - `def __init__(self, *, repo_root: Path | None=None)`
  - `def _compose(self, *args: str, check: bool=True)`
  - `def ensure_postgres_running(self, config: PostgresConfig)`
  - `def is_postgres_running(self)`
  - `def teardown(self, config: PostgresConfig, *, remove_data: bool)`
  - `def _wait_for_healthy(self, *, timeout_seconds: int)` — Poll ``docker compose ps`` until the postgres service reports

## `src/apecx_integration/control_plane/infra/lifecycle.py`
_High-level "does whatever the URL needs" entry point._

- `def default_data_dir()` — Where Apptainer / non-named-volume runtimes should persist PG data.
- `def _alembic_cfg_for(db_url: str, *, alembic_root: Path)`
- `def _find_alembic_root()` — Locate the alembic.ini + migrations/ pair.
- `def _current_db_revision(db_url: str)` — Revision the DB is stamped at, or ``None`` for a fresh / un-stamped DB.
- `def _revision_known_to_scripts(cfg: Config, revision: str)` — Is ``revision`` present in our bundled migration scripts?
- `def _quarantine_incompatible_sqlite(db_url: str, revision: str)` — Move an incompatible SQLite DB aside so a fresh one can be created.
- `def ensure_infra_ready(db_url: str, *, runtime: ContainerRuntime | None=None, data_dir: Path | None=None)`
- `def teardown_infra(db_url: str, *, remove_data: bool=False, runtime: ContainerRuntime | None=None, data_dir: Path | None=None)`

## `src/apecx_integration/control_plane/infra/runtime.py`
_ContainerRuntime protocol + detection._

- class `RuntimeKind(Enum)`
- class `PostgresConfig` — Parameters the runtime needs to stand up a Postgres instance.
- class `ContainerRuntimeUnavailable(RuntimeError)` — Raised when no supported container runtime is installed.
- class `ContainerRuntime(Protocol)`
  - `def ensure_postgres_running(self, config: PostgresConfig)` — Idempotent: start Postgres if not running, no-op if already up.
  - `def is_postgres_running(self)`
  - `def teardown(self, config: PostgresConfig, *, remove_data: bool)` — Stop the container. When ``remove_data=True`` also remove the
- `def _have_command(name: str)`
- `def _docker_daemon_is_up()` — Cheap liveness check for the Docker daemon.
- `def detect_runtime()` — Pick a runtime. Docker first if its daemon is up, Apptainer next.

## `src/apecx_integration/control_plane/infra/urls.py`
_Decide from a DB URL whether the app should self-provision infra._

- class `InfraMode(Enum)` — What the lifespan hook should do with this URL at startup.
- class `InfraDecision`
- `def decide_infra_mode(db_url: str)` — Classify a DB URL into an :class:`InfraMode`.

## `src/apecx_integration/control_plane/models/__init__.py`
_SQLAlchemy ORM models for the Control Plane._

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/models/base.py`
_Declarative base and shared column types for Control Plane ORM models (T09)._

- class `UUIDString(TypeDecorator[UUID])` — Store UUIDs as 36-char strings for cross-DB portability.
  - `def process_bind_param(self, value: Any, dialect: Any)`
  - `def process_result_value(self, value: Any, dialect: Any)`
- class `Base(DeclarativeBase)` — Declarative base for all Control Plane ORM models.

## `src/apecx_integration/control_plane/models/entities.py`
_SQLAlchemy 2.0 ORM models mirroring schemas/entities.py (T09)._

- `def _enum_col(py_enum: type, length: int=48)` — Shorthand for a portable enum-as-VARCHAR column.
- class `Run(Base)`
- class `Step(Base)`
- class `Approval(Base)`
- class `Artifact(Base)`
- class `GeneratedArtifact(Base)`
- class `ProvenanceEvent(Base)`
- class `Component(Base)`
- class `AllocationEstimate(Base)`
- class `VerifiedSynonym(Base)`

## `src/apecx_integration/control_plane/notifications/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/notifications/email.py`
_SMTP notifier for run-state transitions (T08 AP §5.8)._

- class `SMTPConfig`
- `def _bool_env(name: str, default: bool)`
- `def load_smtp_config_from_env()` — Read SMTP params from the ``APECX_SMTP_*`` env vars. Returns
- class `EmailNotifier` — Sends run-state-transition emails via SMTP.
  - `def __init__(self, config: SMTPConfig | None)`
  - `def enabled(self)`
  - `def send_state_transition(self, *, run_id: UUID, old_status: RunStatus, new_status: RunStatus, user_id: str, to_addr: str | None=None, run_summary: str='')` — Send an email for a run-state transition.
  - `def _build_message(self, *, run_id: UUID, old_status: RunStatus, new_status: RunStatus, user_id: str, to_addr: str, run_summary: str)`
  - `def _send(self, msg: EmailMessage)`

## `src/apecx_integration/control_plane/notifications/sweeper.py`
_Stale-run sweeper (T08 AP §5.8 "queue sweeper")._

- class `SweepResult`
- class `RunStateSweeper` — Sweeps for stale runs and reconciles their state.
  - `def __init__(self, session_factory: sessionmaker[Session], recorder: ProvenanceRecorder)`
  - `def sweep(self, *, stale_after: timedelta=DEFAULT_STALE_AFTER, now: datetime | None=None)` — Find and reconcile stale runs.

## `src/apecx_integration/control_plane/provenance/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/provenance/recorder.py`
_ProvenanceRecorder — hash-chained append-only provenance log (T09 AC3)._

- class `ChainBroken(Exception)` — Raised when ProvenanceRecorder.validate finds a chain violation.
- `def _canonical_json(payload: dict[str, Any])`
- `def _canonical_timestamp(ts: datetime)` — Render a timestamp in a form that survives SQLite round-trip.
- `def _compute_event_hash(*, prev_event_hash: str | None, run_id: UUID, event_type: ProvenanceEventType, actor: str, timestamp: datetime, payload: dict[str, Any])`
- class `ProvenanceRecorder`
  - `def __init__(self, session_factory: sessionmaker[Session])`
  - `def record(self, run_id: UUID, event_type: ProvenanceEventType, actor: str, payload: dict[str, Any], *, now: datetime | None=None)`
  - `def validate(self, run_id: UUID)` — Walk the chain for ``run_id`` by following ``prev_event_hash``
  - `def _last_event_for_run(session: Session, run_id: UUID)` — Identify the chain TAIL for ``run_id`` — the unique event

## `src/apecx_integration/control_plane/routes/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/routes/approval.py`
_HITL approval routes (TX1)._

- `def get_approval(approval_id: UUID, session: Annotated[Session, Depends(get_session)])` — Return the current state of an approval.
- `def _load_approval_or_404(session: Session, approval_id: UUID)`
- `def _require_pending(approval: ApprovalORM)`
- `def _run_id_for_step(session: Session, step_id: UUID)`
- `def create_approval(body: CreateApprovalRequest, session: Annotated[Session, Depends(get_session)], recorder: Annotated[ProvenanceRecorder, Depends(get_recorder)])`
- `def _decide(session: Session, recorder: ProvenanceRecorder, approval_id: UUID, new_status: ApprovalStatus, *, decided_by: str, comment: str | None, extra_policy: dict[str, object] | None=None)`
- `def approve(body: ApproveRequest, session: Annotated[Session, Depends(get_session)], recorder: Annotated[ProvenanceRecorder, Depends(get_recorder)])`
- `def reject(body: RejectRequest, session: Annotated[Session, Depends(get_session)], recorder: Annotated[ProvenanceRecorder, Depends(get_recorder)])`
- `def correct(body: CorrectRequest, session: Annotated[Session, Depends(get_session)], recorder: Annotated[ProvenanceRecorder, Depends(get_recorder)])`
- `def list_pending_approvals(body: ListPendingApprovalsRequest, session: Annotated[Session, Depends(get_session)])`

## `src/apecx_integration/control_plane/routes/dashboard.py`
_Dashboard routes — live infrastructure health + recent failures (W3.2)._

- `async def status()`
- `async def dashboard()`
- `def _dot(reachable: bool, state: str)`
- `def _render_html(data: dict[str, Any])`

## `src/apecx_integration/control_plane/routes/hpc.py`
_Optional HPC-export routes._

- `def _not_implemented(task_ref: str)`
- `async def estimate_cost(body: EstimateCostRequest, session: Annotated[Session, Depends(get_session)])` — Pre-submission allocation estimate for a run's workflow.
- `async def confirm_allocation(body: ConfirmAllocationRequest, session: Annotated[Session, Depends(get_session)])` — User acknowledges the estimated core-hours for a run.
- `async def submit_hpc(body: SubmitHpcRequest)`
- `async def export_hpc_bundle(body: ExportHpcBundleRequest, session: Annotated[Session, Depends(get_session)])` — T05 — produce a qsub-able PBS bundle on disk for a Run.
- `async def ingest_hpc_bundle(body: IngestHpcBundleRequest, session: Annotated[Session, Depends(get_session)], recorder: Annotated[ProvenanceRecorder, Depends(get_recorder)])` — T05 AC3 — reconcile a completed bundle back into Tier 2.

## `src/apecx_integration/control_plane/routes/metrics.py`
_Review-UX telemetry (TX3)._

- `def _parse_since(since: str)` — Parse an ISO-8601 timestamp; normalize to aware UTC.
- `def _median(values: list[float])`
- `def _percentile(values: list[float], p: float)` — Nearest-rank percentile, 0 < p < 100.
- `def approval_metrics(session: Annotated[Session, Depends(get_session)], since: Annotated[str, Query(description='ISO-8601 window start; approvals decided at or after this time are included. Example: 2026-04-15T00:00:00Z')])`
- `def __import_uuid(s: str)` — Small indirection so the main query can pass strings through
- `def run_metrics(session: Annotated[Session, Depends(get_session)])` — Run counts by status + total — deployment run-health in one call, instead of

## `src/apecx_integration/control_plane/routes/status.py`
_Run, step, and artifact inspection routes (TX1)._

- `def list_runs(body: ListRunsRequest, session: Annotated[Session, Depends(get_session)])`
- `def get_status(body: GetStatusRequest, session: Annotated[Session, Depends(get_session)])`
- `def get_artifact(body: GetArtifactRequest, session: Annotated[Session, Depends(get_session)])`

## `src/apecx_integration/control_plane/routes/verified_synonyms.py`
_Verified-synonym cache (T02 workflow-spec Steps 3a and 4p)._

- `def _load_or_404(session: Session, synonym_id: UUID)`
- `def lookup(body: VerifiedSynonymLookupRequest, session: Annotated[Session, Depends(get_session)])` — Batched cache lookup. Returns one match per input term, in
- `def create_verified_synonym(body: CreateVerifiedSynonymRequest, session: Annotated[Session, Depends(get_session)])` — Record a novel approved mapping. Called by the workflow's
- `def revoke_verified_synonym(synonym_id: UUID, body: RevokeVerifiedSynonymRequest, session: Annotated[Session, Depends(get_session)])` — Soft-delete: set ``is_active=false``, record revocation metadata.

## `src/apecx_integration/control_plane/routes/workflow.py`
_Workflow-creation and plan-inspection routes._

- `def _not_implemented(task_ref: str)`
- `async def start_workflow(body: StartWorkflowRequest, session_factory: Annotated[sessionmaker[Session], Depends(get_session_factory)], composer: Annotated[Composer | None, Depends(get_composer_or_none)]=None, policy: Annotated[ApprovalPolicy | None, Depends(get_approval_policy_or_none)]=None)` — T01 P1: compose a workflow, persist it, return the Run.
- `async def generate_plan(body: GeneratePlanRequest, session_factory: Annotated[sessionmaker[Session], Depends(get_session_factory)], composer: Annotated[Composer | None, Depends(get_composer_or_none)]=None)` — Preview-mode composition — same composer flow as
- `async def execute_workflow(body: ExecuteWorkflowRequest, session_factory: Annotated[sessionmaker[Session], Depends(get_session_factory)], executor: Annotated[LocalExecutor | None, Depends(get_local_executor_or_none)]=None)` — T01 P2 HTTP surface — run a composed workflow to terminal state.
- `async def show_yaml_diff(body: ShowYamlDiffRequest, session: Annotated[Session, Depends(get_session)])` — T06 / AP §5.6 — surface the diff payload for a run's workflow.

## `src/apecx_integration/control_plane/schemas/__init__.py`
_Pydantic schemas for the Control Plane._

_(no module-level classes or functions)_

## `src/apecx_integration/control_plane/schemas/api.py`
_Request / response envelopes for the Tier 1 ↔ Tier 2 API contract (TX1)._

- class `_APIBase(BaseModel)`
- class `StartWorkflowRequest(_APIBase)`
- class `StartWorkflowResponse(_APIBase)`
- class `GeneratePlanRequest(_APIBase)`
- class `StepPlan(_APIBase)`
- class `GeneratePlanResponse(_APIBase)`
- class `ShowYamlDiffRequest(_APIBase)`
- class `ShowYamlDiffResponse(_APIBase)`
- class `ExecuteWorkflowRequest(_APIBase)`
- class `ExecuteWorkflowResponse(_APIBase)` — Mirror of ``LocalExecutor.ExecutionResult`` as a Pydantic shape.
- class `ApproveRequest(_APIBase)`
- class `RejectRequest(_APIBase)`
- class `CorrectRequest(_APIBase)`
- class `ApprovalResponse(_APIBase)`
- class `ListPendingApprovalsRequest(_APIBase)`
- class `ListPendingApprovalsResponse(_APIBase)`
- class `ListRunsRequest(_APIBase)`
- class `ListRunsResponse(_APIBase)`
- class `GetStatusRequest(_APIBase)`
- class `GetStatusResponse(_APIBase)`
- class `GetArtifactRequest(_APIBase)`
- class `GetArtifactResponse(_APIBase)`
- class `EstimateCostRequest(_APIBase)`
- class `EstimateCostResponse(_APIBase)`
- class `ConfirmAllocationRequest(_APIBase)`
  - `def _reject_non_finite(cls, v)`
- class `ConfirmAllocationResponse(_APIBase)`
- class `SubmitHpcRequest(_APIBase)`
- class `SubmitHpcResponse(_APIBase)`
- class `ExportHpcBundleRequest(_APIBase)`
- class `ExportHpcBundleResponse(_APIBase)`
- class `IngestHpcBundleRequest(_APIBase)` — Point at a completed bundle on disk (post-qsub, post-transfer-back).
- class `IngestHpcBundleResponse(_APIBase)`
- class `CreateApprovalRequest(_APIBase)`
- class `CreateApprovalResponse(_APIBase)`
- class `ApprovalMetricsResponse(_APIBase)` — Aggregate telemetry over approval decisions in a time window.
- class `RunMetricsResponse(_APIBase)` — Aggregate run-health telemetry: run counts by status + total. A server-side
- class `VerifiedSynonymLookupRequest(_APIBase)` — Batched cache lookup. Every workflow run starts with a lookup
- class `VerifiedSynonymMatch(_APIBase)` — One cache-lookup result. ``result`` is the active
- class `VerifiedSynonymLookupResponse(_APIBase)`
- class `CreateVerifiedSynonymRequest(_APIBase)` — Write-back after ApprovalStep approves a novel mapping.
- class `VerifiedSynonymResponse(_APIBase)`
- class `RevokeVerifiedSynonymRequest(_APIBase)` — Soft-delete a previously-approved mapping. Row is preserved for

## `src/apecx_integration/control_plane/schemas/entities.py`
_Pydantic entity schemas mirroring architectural_plan.md §4._

- class `_EntityBase(BaseModel)`
- class `Run(_EntityBase)`
- class `Step(_EntityBase)`
- class `Approval(_EntityBase)`
- class `Artifact(_EntityBase)`
- class `GeneratedArtifact(_EntityBase)` — Subtype of Artifact with LLM-generation provenance.
- class `ProvenanceEvent(_EntityBase)` — Hash-chained, append-only provenance record.
- class `Component(_EntityBase)` — A library component available for composition.
- class `VerifiedSynonym(_EntityBase)` — A user-verified synonym mapping persisted across runs.
- class `AllocationEstimate(_EntityBase)`

## `src/apecx_integration/control_plane/schemas/enums.py`
_String-valued enums shared across Control Plane entities._

- class `RunStatus(StrEnum)`
- class `StepStatus(StrEnum)`
- class `ExecutorKind(StrEnum)`
- class `ApprovalKind(StrEnum)`
- class `ApprovalStatus(StrEnum)`
- class `ArtifactKind(StrEnum)`
- class `ProvenanceEventType(StrEnum)`
- class `ComponentTestStatus(StrEnum)`
- class `StepCategory(StrEnum)` — Disposition category from the differential-review UX (T06).

## `src/apecx_integration/execution/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/execution/pbs_bundle.py`
_T05 — PBS bundle generator._

- class `BundleRequest` — What the generator needs to know about a run.
- class `BundleResult`
- class `UnsupportedSystem(ValueError)` — Raised when ``target_system`` isn't in ``SUPPORTED_SYSTEMS``.
- `def generate_bundle(request: BundleRequest)` — Write the bundle to disk and return the paths.
- `def _render_submit_pbs(request: BundleRequest, workflow_path: Path)` — Minimal-viable PBS script. Defaults are Polaris/Aurora-shaped.
- `def _render_run_sh(request: BundleRequest)` — The inner script the PBS job runs.
- `def _render_staging_plan(request: BundleRequest)`
- `def _render_readme(request: BundleRequest)`

## `src/apecx_integration/infrastructure/__init__.py`
_Startup-time infrastructure orchestrator for ``apecx-mcp``._

_(no module-level classes or functions)_

## `src/apecx_integration/infrastructure/backends.py`
_Dataclasses + state enum that describe a managed backend._

- class `BackendState(StrEnum)` — The state machine values reported via :meth:`InfraOrchestrator.status`.
- class `ProbeResult` — The output of a backend health probe.
- class `Probe` — A named async health probe.
  - `async def run(self)`
- class `ContainerSpec` — A Docker container specification.
- class `BackendSpec` — A complete description of one managed backend.
  - `def __post_init__(self)`
- class `BackendRuntime` — Mutable per-backend runtime state held by the orchestrator.
  - `def snapshot(self)` — Return a JSON-safe dict for the status tool.

## `src/apecx_integration/infrastructure/containers.py`
_Canonical Docker container specs for the apecx-mcp stack._

- `def all_container_specs()` — Return every container spec in a deterministic order.
- `def container_run_args(spec: ContainerSpec, *, bind_host: str='127.0.0.1')` — Build the ``docker run`` argv (without the leading ``docker run``).

## `src/apecx_integration/infrastructure/failure_log.py`
_Durable, size-capped JSONL sink for infrastructure failure events (dashboard / W3)._

- class `FailureEvent` — One recorded infrastructure failure (optionally, its auto-reload outcome).
- class `InfraFailureLog`
  - `def __init__(self, path: Path | str | None=None, *, max_records: int=_MAX_RECORDS)`
  - `def path(self)`
  - `def record(self, event: FailureEvent)`
  - `def _enforce_cap(self)`
  - `def recent(self, limit: int=50)`

## `src/apecx_integration/infrastructure/monitor.py`
_Always-on infrastructure monitor daemon (dashboard / W3)._

- `def _utc_now_iso()`
- class `InfraMonitor`
  - `def __init__(self, *, orchestrator: Any=None, failure_log: InfraFailureLog | None=None, backoff_s: float=_DEFAULT_BACKOFF_S, recent_maxlen: int=_RECENT_MAXLEN)`
  - `def _orchestrator(self)`
  - `def latest(self)`
  - `def recent_failures(self)`
  - `async def snapshot(self)`
  - `async def tick(self, *, monotonic: float | None=None, now_iso: str | None=None)`
  - `async def run_forever(self, *, interval_s: float=_DEFAULT_INTERVAL_S, stop_event: asyncio.Event | None=None)`
- `def get_monitor()` — Process-singleton monitor (the CLI + web views + the control-plane daemon share it).

## `src/apecx_integration/infrastructure/orchestrator.py`
_The process-singleton orchestrator that brings the stack up._

- `def _autostart_enabled()`
- `def _make_postgres_spec()`
- `def _make_redis_spec()`
- `def _make_minio_spec()`
- `def _make_ollama_spec()`
- `def _compose_rhea_env(*, postgres: ContainerSpec, redis_c: ContainerSpec, minio: ContainerSpec, ollama_base_url: str, infra_host: str='localhost')` — Derive the env vars Rhea needs, from the orchestrator's own specs.
- `def _compose_rhea_container_env(*, postgres: ContainerSpec, redis_c: ContainerSpec, minio: ContainerSpec, ollama_base_url: str)` — Rhea env for the CONTAINER backend.
- `def _make_rhea_container_spec(*, postgres_container: ContainerSpec, redis_container: ContainerSpec, minio_container: ContainerSpec, ollama_base_url: str)` — Rhea backend that runs the rhea-server DOCKER IMAGE (host-conda-independent).
- `def _default_backend_specs()` — Build the default 5-backend roster.
- class `InfraOrchestrator` — Process-singleton infra orchestrator.
  - `def __init__(self, specs: list[BackendSpec] | None=None, *, autostart_enabled: bool | None=None, docker_binary: str | None=None)`
  - `def autostart_enabled(self)`
  - `def uptime_seconds(self)`
  - `def backend_names(self)`
  - `def get_runtime(self, name: str)`
  - `async def start_all(self)` — Bring up (or probe-only) every backend in parallel.
  - `async def reload_backend(self, name: str)` — Re-establish ONE backend by name (probe → reuse-if-healthy → else spawn/restart) — the
  - `async def reconcile(self)` — Self-heal docker-dependent backends when Docker comes up AFTER startup.
  - `async def status(self)` — Snapshot of every backend's current state.
  - `async def ensure_catalog_seeded(self, *, timeout_s: float=600.0)` — Auto-run the Rhea tool-catalog ingestion when the catalog is empty.
  - `async def _ensure_embedding_model(self)` — Ensure the Rhea catalog-ingestion embedding model is pulled in Ollama.
  - `async def _catalog_row_count(self)` — Row count of the rhea ``galaxytools`` catalog table — the seeded-state ground truth.
  - `async def prewarm_workflow_tools(self)` — Pre-install every Rhea tool conda env declared by the catalog.
  - `async def shutdown(self)` — Tear down ONLY containers we spawned.
  - `def _atexit_shutdown(self)` — atexit hook — sync-only wrapper around ``shutdown``.
  - `async def _bring_up(self, rt: BackendRuntime)` — Bring up one backend (probe → optional spawn → poll).
  - `async def _bring_up_container(self, rt: BackendRuntime)`
  - `async def _poll_until_healthy(self, rt: BackendRuntime, timeout_s: float, *, poll_interval_s: float=0.5)` — Probe until healthy or timeout. Returns True on success.
  - `async def _reprobe(self, rt: BackendRuntime)` — Re-probe a backend and update its state in place.
  - `def _compute_overall_state(self)` — Aggregate per-backend states into a single overall string.
  - `def _actionable_messages(self)`
- `def get_orchestrator()` — Return the process-singleton orchestrator, constructing on first call.
- `def _log_bringup_verdict(snapshot: dict[str, Any])` — Log the aggregate backend bring-up verdict LOUDLY so a required-backend
- `def start_orchestrator_in_background_thread()` — Kick off ``orchestrator.start_all()`` in a dedicated daemon thread.
- `def stop_orchestrator_in_background_thread(timeout: float=5.0)` — Cancel the background drive task and join its thread.
- `def reset_orchestrator_for_testing()` — Clear the singleton. Tests use this between test functions.

## `src/apecx_integration/infrastructure/prewarm_workflow/__init__.py`
_Rhea tool-env pre-warm phase, expressed as a nanobrain workflow._

_(no module-level classes or functions)_

## `src/apecx_integration/infrastructure/prewarm_workflow/aggregate_report_step.py`
_AggregateReportStep — final stage of the pre-warm workflow._

- class `AggregateReportStepConfig(StepConfig)` — Config for :class:`AggregateReportStep`.
- class `AggregateReportStep(BaseStep)` — Builds the final :class:`PrewarmReport` from per-tool results.
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/infrastructure/prewarm_workflow/builder.py`
_Programmatic construction of the pre-warm workflow._

- `def build_prewarm_workflow_via_builder()` — Construct the pre-warm workflow programmatically.
- `def builder_workflow_config()` — Return the WorkflowBuilder-generated dict WITHOUT loading.

## `src/apecx_integration/infrastructure/prewarm_workflow/collect_tools_step.py`
_CollectToolsStep — first stage of the pre-warm workflow._

- class `CollectToolsStepConfig(StepConfig)` — Config for :class:`CollectToolsStep`.
- class `CollectToolsStep(BaseStep)` — Catalog walker that emits the deduped Rhea tool-name list.
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/infrastructure/prewarm_workflow/install_tools_step.py`
_InstallToolsStep — second stage of the pre-warm workflow._

- class `InstallToolsStepConfig(StepConfig)` — Config for :class:`InstallToolsStep`.
- class `InstallToolsStep(BaseStep)` — Per-tool serial installer using :func:`prewarm_tool`.
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/infrastructure/probes.py`
_Health probes for the five managed backends._

- `async def _measure(coro_factory)` — Run ``coro_factory()`` and return (latency_ms, value, exc).
- `async def postgres_probe(*, host: str, port: int, user: str, db: str, password: str, timeout_s: float=_DEFAULT_TIMEOUT_S)` — Probe Postgres via psycopg.
- `async def redis_probe(*, host: str, port: int, timeout_s: float=_DEFAULT_TIMEOUT_S)` — Probe Redis via the sync redis-py client + ``PING``.
- `async def minio_probe(*, host: str, port: int, timeout_s: float=_DEFAULT_TIMEOUT_S)` — Probe MinIO via ``GET /minio/health/live``.
- `def _model_present(names: list[str], required: str)` — True if ``required`` matches an installed Ollama model, tolerant of the ``:tag`` suffix
- `async def ollama_probe(*, base_url: str, required_model: str | None=None, timeout_s: float=_DEFAULT_TIMEOUT_S)` — Probe Ollama via ``GET /api/tags`` and report MODEL-AWARE readiness (#7).
- `async def rhea_mcp_probe(*, mcp_url: str, timeout_s: float=5.0)` — Probe Rhea MCP via the canonical ``tools/list`` round-trip.

## `src/apecx_integration/infrastructure/rhea_env_autodiscovery.py`
_Auto-discover the Rhea checkout + set RHEA_* env vars (G88, 2026-05-16)._

- `def autodiscover_rhea_env(*, dry_run: bool=False)` — Probe the filesystem + apply platform defaults to populate any
- `def _is_set(name: str)` — True iff the operator has set this env var to a non-empty value.
- `def _find_rhea_repo()` — Search known locations for a Rhea checkout.
- `def _is_rhea_repo(path: Path)` — Validate a candidate directory as a Rhea checkout.
- `def autodiscovery_enabled()` — ``APECX_RHEA_AUTODISCOVER=0`` disables the discovery.

## `src/apecx_integration/infrastructure/rhea_prewarm.py`
_Per-tool pre-warm helpers — the install + report-shape primitives._

- class `ToolPrewarmResult` — Per-tool pre-warm outcome — fed to the status tool.
- class `PrewarmReport` — Aggregate report across all pre-warmed tools.
  - `def all_ready(self)` — True iff every declared tool is ready/reused.
  - `def snapshot(self)`
- `def _collect_tools_from_catalog(catalog: Any)` — Walk ``WorkflowCatalog.workflows`` and dedupe their prewarm lists.
- `async def _fetch_tool_requirements(database_url: str, tool_id: str)` — Query Rhea's ``galaxytools`` table for one tool's requirements.
- `async def prewarm_tool(tool_id: str, *, database_url: str, redis_host: str, redis_port: int, rhea_python: str | None=None)` — Pre-install a single Rhea tool's conda env.

## `src/apecx_integration/infrastructure/rhea_server_provisioner.py`
_Auto-build the rhea-server Docker image from local rhea source (P4-B autodeploy)._

- `def resolve_rhea_image_tag()` — The image tag we BUILD and the orchestrator RUNs — single source of truth.
- `def _resolve_rhea_repo()` — Locate the rhea source checkout (the docker build context). FAIL-LOUD if absent.
- `async def ensure_rhea_image_built(*, on_progress: Callable[[str], None] | None=None)` — Build (idempotently) the rhea-server image from the local rhea checkout; return its tag.

## `src/apecx_integration/mcp_surface/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/mcp_surface/control_plane_client.py`
_Thin HTTP client the MCP surface uses to call the Control Plane (Tier 2)._

- class `ControlPlaneDependencyError(RuntimeError)` — Raised when the Control Plane returns 503 because a backend
- class `WorkflowCompositionError(RuntimeError)` — Raised when the Control Plane returns 422 because it could not
- class `ControlPlaneNotFoundError(RuntimeError)` — Raised when the Control Plane returns 404 — a referenced resource does not
- class `ControlPlaneClient` — HTTP client to the Control Plane.
  - `def __init__(self, base_url: str, *, timeout: float=30.0)`
  - `async def __aenter__(self)`
  - `async def __aexit__(self, *_: object)`
  - `async def close(self)`
  - `async def _post(self, path: str, body: BaseModel, response_model: type[R])`
  - `def _extract_detail(payload: Mapping[str, Any])`
  - `async def healthz(self)`
  - `async def start_workflow(self, body: StartWorkflowRequest)`
  - `async def generate_plan(self, body: GeneratePlanRequest)`
  - `async def show_yaml_diff(self, body: ShowYamlDiffRequest)`
  - `async def execute_workflow(self, body: ExecuteWorkflowRequest)`
  - `async def estimate_cost(self, body: EstimateCostRequest)`
  - `async def confirm_allocation(self, body: ConfirmAllocationRequest)`
  - `async def export_hpc_bundle(self, body: ExportHpcBundleRequest)`
  - `async def ingest_hpc_bundle(self, body: IngestHpcBundleRequest)`
  - `async def create_approval(self, body: CreateApprovalRequest)`
  - `async def get_approval(self, approval_id: UUID)` — Poll the current state of an approval.
  - `async def approve(self, body: ApproveRequest)`
  - `async def reject(self, body: RejectRequest)`
  - `async def correct(self, body: CorrectRequest)`
  - `async def list_pending_approvals(self, body: ListPendingApprovalsRequest)`
  - `async def list_runs(self, body: ListRunsRequest)`
  - `async def get_status(self, body: GetStatusRequest)`
  - `async def get_artifact(self, body: GetArtifactRequest)`
  - `async def lookup_verified_synonyms(self, body: VerifiedSynonymLookupRequest)`
  - `async def create_verified_synonym(self, body: CreateVerifiedSynonymRequest)`
  - `async def revoke_verified_synonym(self, synonym_id: UUID, body: RevokeVerifiedSynonymRequest)`

## `src/apecx_integration/mcp_surface/data/__init__.py`
_Vendored DB-query layer for the integration MCP server._

_(no module-level classes or functions)_

## `src/apecx_integration/mcp_surface/data/database.py`
_VIOLIN and BV-BRC database query layer._

- class `DatabaseStore` — Holds VIOLIN + BV-BRC DataFrames in memory. Loaded once, read-only.
  - `def __init__(self, violin_csv_paths: dict[str, Path], bvbrc_csv_path: Path | None, bvbrc_cache_dir: Path | None, virus_resolution_cache_dir: Path | None)`
  - `def is_loaded(self)`
- `def _resolve_data_root()` — Return the data dir from APECX_DATA_ROOT, or APECX_ROOT/data, else None.
- `def get_store()` — Lazily load the store from env. Returns ``(store, error_msg)``.
- `def reset_store()` — Test hook — clear the cached singleton + error.
- `def set_store_for_tests(store: DatabaseStore | None)` — Test hook — install a pre-built store as the singleton.
- `def _safe_str_contains(df: pd.DataFrame, columns: list[str], term: str)` — Case-insensitive substring search across multiple columns. NaN-safe.
- `def _df_to_records(df: pd.DataFrame, limit: int)` — Convert DataFrame slice to list of dicts, replacing NaN with None.
- `def query_vaccines(store: DatabaseStore, search_term: str | None=None, vaccine_type: str | None=None, status: str | None=None, pathogen: str | None=None, limit: int=25, vo_id: str | None=None)` — ``vo_id``: bare VO id string (e.g. ``"VO_0000122"``) for precision
- `def query_pathogens(store: DatabaseStore, search_term: str | None=None, disease: str | None=None, limit: int=25, ncbi_taxonomy_id: int | None=None, ncbi_taxonomy_ids: list[int] | None=None)` — ``ncbi_taxonomy_id``: single taxon ID for leaf-level precision.
- `def query_genes(store: DatabaseStore, search_term: str | None=None, organism: str | None=None, limit: int=25, ncbi_gene_id: int | None=None)` — ``ncbi_gene_id``: integer gene ID for precision filtering.
- `def query_bvbrc_genomes(store: DatabaseStore, search_term: str | None=None, species: str | None=None, host: str | None=None, country: str | None=None, min_year: int | None=None, max_year: int | None=None, limit: int=25, ncbi_taxonomy_id: int | None=None)` — ``ncbi_taxonomy_id``: integer taxon ID for precision filtering on the
- `def get_vaccine_pathogen_genes(store: DatabaseStore, pathogen_name: str, ncbi_taxonomy_id: int | None=None)` — ``ncbi_taxonomy_id``: integer taxon ID for precision pathogen matching.
- `def database_statistics(store: DatabaseStore)`
- `def resolve_entity(store: DatabaseStore, name: str)` — Resolve a biomedical entity name across all loaded databases.

## `src/apecx_integration/mcp_surface/llm_policy.py`
_LLM-access resolution policy (design §9) — announce the resolved LLM, or LOUDLY REFUSE._

- class `LlmResolution` — The outcome of resolving an LLM for an LLM-needing workflow.
- `def _provider(base_url: str)`
- `def _probe_reachable(base_url: str, timeout: float=2.0)` — Best-effort reachability probe (a real HTTP GET). Any failure → unreachable.
- `def resolve_llm(locus: ExecutionLocus, *, reachable: Callable[[str], bool] | None=None)` — Resolve the LLM for an LLM-needing workflow under ``locus``.
- `def workflow_needs_llm_at_run(workflow: object, locus: ExecutionLocus)` — Does this LOADED workflow need an LLM to run UNDER ``locus``?

## `src/apecx_integration/mcp_surface/locus.py`
_execution_locus — re-export of the canonical locus module._

_(no module-level classes or functions)_

## `src/apecx_integration/mcp_surface/network_config.py`
_Centralized network configuration — the single source for apecx ports + hosts._

- `def _port_field(default: int)`
- class `MCPConfig(BaseModel)` — The MCP server's HTTP bind (streamable-http / sse transports).
- class `ControlPlaneConfig(BaseModel)` — The Control Plane (run-store + state backend) the MCP server health-checks + POSTs to.
- class `RheaConfig(BaseModel)` — The Rhea MCP worker (Galaxy bio-tools).
- class `BackendsConfig(BaseModel)` — Container backend HOST ports for the deploy/ stack. ``install-server.sh`` reads these to
- class `NetworkConfig(BaseModel)` — Top-level apecx network config. Sections default to their own built-in defaults, so a
  - `def control_plane_url(self)`
  - `def rhea_mcp_url(self)`
- `def resolve_config_path(explicit: str | Path | None=None)` — Resolve the config-file path: explicit arg > $APECX_CONFIG > ~/.apecx/config.yml.
- `def load_network_config(path: str | Path | None=None)` — Load the network config. Missing file → built-in defaults. A malformed file or an unknown
- `def set_network_config(config: NetworkConfig | None)` — Set the process-wide network config. ``None`` resets to a lazy default (used by tests).
- `def get_network_config()` — The process-wide network config; lazy-loads the default file on first use if unset.
- `def deploy_env_lines(config: NetworkConfig)` — The deploy-stack env lines docker compose interpolates, derived from the config.

## `src/apecx_integration/mcp_surface/prompts/__init__.py`
_MCP-surface prompt assets for the desktop reasoning host (design §11, D7)._

- `def load_prompt(name: str)` — Read a prompt asset by filename (e.g. ``rules_core.md``). FAIL-LOUD if absent —

## `src/apecx_integration/mcp_surface/server.py`
_MCP surface entry point — FastMCP-based server._

- `def build_server(locus: ExecutionLocus | None=None)` — Construct the FastMCP server with every tool registered.
- `def _register_logging_capability(server: FastMCP)` — Make ``logging/setLevel`` a real, advertised MCP method.
- `def _register_reasoning_prompts(server: FastMCP)` — Serve the desktop reasoning host's operating rules as live MCP prompts.
- `async def _ping_control_plane(base_url: str)` — Hit /healthz — return True iff the backend answers.
- `def _autostart_backend(base_url: str)` — Spawn ``apecx-cp serve`` as a child process.
- `def _terminate_child_backend()` — atexit handler — SIGTERM the child backend with a 5s grace,
- `async def _wait_for_backend_ready(base_url: str, *, timeout_s: float=60.0)` — Poll /healthz until it answers or timeout. Returns True on
- `async def _verify_control_plane_reachable()` — Ensure the Control Plane is reachable, autostarting it if
- `def _check_data_root_or_warn()` — Log a loud banner when the VIOLIN/BV-BRC data dir is missing.
- `def _check_rag_index_or_warn()` — Log a loud banner when the domain RAG index is missing (G81).
- `def _check_rhea_status_or_warn()` — Log a banner reporting the Rhea SOURCE-readiness state at MCP startup (G89; P4-B).
- `def _try_public_download(sqlite_path: Path)` — Anonymous bootstrap of the dictionary from the public Globus path.
- `def _ensure_synonym_dict_or_warn()` — Make the synonym dictionary available at startup — DOWNLOAD it from Globus.
- `def _resolve_package_version()` — Return the installed package version, or 'unknown' if unresolvable.
- `def _build_arg_parser()` — Argparse for ``apecx-mcp``.
- `def _resolve_http_host_port(args: argparse.Namespace, config: NetworkConfig | None=None)` — Resolve the HTTP-transport bind (host, port) with precedence **CLI flag > config > built-in**.
- `def _assert_mcp_port_distinct_from_control_plane(http_host: str, http_port: int, config: NetworkConfig | None=None)` — FAIL-LOUD (before the boot) when the MCP HTTP bind collides with the local control-plane port.
- `def _configure_logging()` — Configure server logging. apecx's own INFO startup lines are useful, but nanobrain emits
- `def main(argv: list[str] | None=None)` — Entry point for the ``apecx-mcp`` console script.

## `src/apecx_integration/mcp_surface/tools/__init__.py`

_(no module-level classes or functions)_

## `src/apecx_integration/mcp_surface/tools/_hitl_gate.py`
_Shared HITL gate for MCP tools that resolve user-supplied entity names._

- `def _ncbi_taxon_id(iri: str | None)` — Extract the integer NCBI taxon ID from an OBO IRI tail.
- `def _build_paused_envelope(*, term: str, candidates: list[dict[str, Any]], param_name: str, tool_name: str)` — Build the paused-envelope shape — identical to harmonized_search's.
- `def resolve_with_hitl_gate(*, term: str, entity_type: EntityType | None, param_name: str, tool_name: str)` — Resolve ``term`` with structural HITL gating.
- `def _lookup_result_to_meta(lr: LookupResult)` — Project the LookupResult to a serializable resolution-metadata dict.

## `src/apecx_integration/mcp_surface/tools/_shared.py`
_Shared plumbing for the MCP tool modules._

- `def set_client(client: ControlPlaneClient | None)` — Inject a client (or clear it). Used by tests + the server's
- `def get_client()` — Return the injected client, or build one lazily from the centralized network config.
- class `InvalidRunIdError(ValueError)` — Raised when a tool argument that should be a UUID is not.
  - `def __init__(self, raw: str, field: str='run_id')`
- `def parse_run_id(raw: str, *, field: str='run_id')` — Parse a UUID from a string with a friendly error.

## `src/apecx_integration/mcp_surface/tools/approvals.py`
_MCP tools for the HITL approval lifecycle._

- `async def list_pending_approvals(user_id: str)` — List pending approvals for a given ``user_id``.
- `async def approve(approval_id: str, comment: str='', decided_by: str='api_user')`
- `async def reject(approval_id: str, reason: str, decided_by: str='api_user')` — Reject a pending approval. ``reason`` is required (min length 1)
- `async def correct(approval_id: str, modifications: dict, decided_by: str='api_user')` — Approve with reviewer-supplied modifications. ``modifications``

## `src/apecx_integration/mcp_surface/tools/canonical_entity.py`
_MCP tool: resolve_canonical_entity._

- `def _result_to_dict(result: LookupResult)`
- `async def resolve_canonical_entity(name: str, entity_type: str='')` — Resolve a biomedical entity name to its canonical ontology IRI.

## `src/apecx_integration/mcp_surface/tools/database_tools.py`
_MCP tools for direct VIOLIN + BV-BRC database queries._

- `def _ncbi_taxon_id(iri: str | None)` — Extract integer taxon ID from an NCBITaxon OBO IRI.
- `def _vo_local_id(iri: str | None)` — Extract the local VO identifier (e.g. ``VO_0000001``) from an OBO IRI.
- `def _ncbi_gene_id(iri: str | None)` — Extract integer gene ID from an identifiers.org ncbigene IRI.
- `def _resolution_meta(lr: LookupResult)`
- `async def query_vaccines(search_term: str='', vaccine_type: str='', status: str='', pathogen: str='', limit: int=25)` — Search the VIOLIN vaccine database (~3,500 vaccines).
- `async def query_pathogens(search_term: str='', disease: str='', limit: int=25)` — Search the VIOLIN pathogen database (~220 pathogens).
- `async def query_genes(search_term: str='', organism: str='', limit: int=25)` — Search the VIOLIN gene/protein database (~4,000 genes).
- `async def query_bvbrc_genomes(search_term: str='', species: str='', host: str='', country: str='', min_year: int=0, max_year: int=0, limit: int=25)` — Search the BV-BRC alphavirus genome database (~17,000 genomes).
- `async def get_vaccine_pathogen_genes(pathogen_name: str)` — Get the full vaccine-pathogen-gene relationship chain from VIOLIN.
- `async def resolve_entity(name: str)` — Resolve a biomedical entity name across all APECx databases.
- `async def database_statistics()` — Get row counts and column lists for all loaded VIOLIN and BV-BRC tables.

## `src/apecx_integration/mcp_surface/tools/discovery.py`
_MCP tools that expose the workflow catalog to the model._

- class `_ManifestSummary` — Internal: one parsed manifest reduced to a dict-friendly shape.
- `def _resolve_composer_config_path()` — Find the composer config the manifest paths are relative to.
- `def _load_manifest_paths(config_path: Path)` — Return the list of manifest YAMLs the composer is configured to
- `def _parse_manifest(path: Path)` — Parse one manifest YAML into the internal summary shape.
- `def _strip(s: Any)`
- `def _load_all_manifests()`
- `def _load_runnable_catalog()` — Rows for the directly-runnable workflows (``run_workflow`` targets), DYNAMICALLY.
- `async def list_workflows()` — Find a specialized apecx workflow for a science question BEFORE answering from memory.
- `async def describe_workflow(name: str)` — Return the full per-component view of one workflow.

## `src/apecx_integration/mcp_surface/tools/eo_primitives.py`
_EO thin-surface primitives (EO-03/04/05) — workflows as first-class objects._

- `def _flagged_step_failures(top_level_steps: set[str], failed_steps: list[str], has_workflow_output: bool)` — G127 (refined): which ``step_failed`` events make a 'completed' run actually DISHONEST.
- `def _top_level_step_names(workflow: Any)` — The TOP-LEVEL step identities to match against ``run_summary`` step_names. Includes BOTH the
- `def _write_tool_outputs(tool_dir: Any, data: Any, base: Any)` — Split the resolved structured DataShape into per-part NATIVE files under ``tool_outputs/``.
- `def _attach_artifact(result: dict[str, Any], run_id: str | None)` — Gather a completed run's deliverables into a DURABLE per-run folder and set
- `def prune_artifacts(base: Any=None, max_runs: int | None=None)` — Keep only the N newest run dirs under the artifacts base (FIFO by mtime). NEVER raises.
- `async def run_workflow(name: str, params: dict[str, Any] | None=None, ctx: Context | None=None)` — Run the catalog workflow ``name`` with ``params``; return its result envelope.
- `def _structured_subset(result: dict[str, Any])` — Bounded structured-data block: precise values + a LEAN artifact manifest. The per-tool_output
- `def _desktop_host_payload(result: dict[str, Any])` — Transform a completed result dict into MCP content (text + Image blocks + structured json).
- `def maybe_desktop_payload(result: Any, ctx: Context | None)` — Apply the desktop re-ingestion adapter at the MCP tool boundary, or pass the result through.
- `async def run_workflow_tool(name: str, params: dict[str, Any] | None=None, ctx: Context | None=None)`
- `async def _run_resolved_entry(entry: Any, params: dict[str, Any] | None=None)` — Guarded execution of an ALREADY-RESOLVED catalog entry — the SINGLE execution core.
- `def _make_stage_streamer(on_stage: OnStage, on_heartbeat: OnHeartbeat | None=None)` — Build a G37 step-event subscriber that extracts NEWLY-added stage reports and (when
- `async def run_workflow_streamed(name: str, params: dict[str, Any] | None=None, on_stage: OnStage | None=None, on_heartbeat: OnHeartbeat | None=None)` — Run the catalog workflow ``name`` EXACTLY like ``run_workflow``, but push each
- `async def _run_workflow_streaming_impl(name: str, params: dict[str, Any] | None, ctx: Context)` — STREAM each reasoning stage of workflow ``name`` to the desktop MCP client as it
- `async def inspect_run(run_id: str, detail: bool=False)` — Return the per-step 'what ran' summary (status/timing/tool+LLM call counts) for a run.
- `async def inspect_workflow(name: str, max_depth: int=3)` — Return the recursive static structure (steps, links, nested workflows) of a workflow.
- `async def apecx_context()` — Re-orient: the workflow runs made this session (run_id, name, status), oldest first.
- `async def apecx_capabilities()` — The COMPLETE "what can I do" view — call this before answering a science question from memory.
- `def approve_design(token: str, decided_by: str='operator')` — Operator approval for a design/optimization output request (HITL gate).

## `src/apecx_integration/mcp_surface/tools/globus_search.py`
_MCP tool — query the APECx Globus Search index._

- `async def query_globus_search(query: str, max_results: int=20, offset: int=0)` — Query the APECx Globus Search index for harvested records.

## `src/apecx_integration/mcp_surface/tools/harmonized_search.py`
_MCP tool — drive the harmonized_search nanobrain workflow._

- `def _workflow_dir()` — Locate the harmonized_search workflow directory.
- `def _load_workflow()` — Lazy-load and cache the harmonized_search workflow.
- `async def _aggregate_served_search(term: str, index: str)` — Taxon-precise structural search for an aggregate-served index (pdb/emdb).
- `async def harmonized_search(term: str, index: str, entity_type: str='')` — Search any APECx Globus index (BV-BRC genomes / proteins / protein structures /

## `src/apecx_integration/mcp_surface/tools/hpc.py`
_MCP tools for the optional HPC-export lane._

- `async def estimate_cost(run_id: str)`
- `async def confirm_allocation(run_id: str, confirmed_core_hours: float)`
- `async def export_hpc_bundle(run_id: str, target_system: str, output_directory: str)`
- `async def ingest_hpc_bundle(bundle_path: str)`

## `src/apecx_integration/mcp_surface/tools/infrastructure_status.py`
_``infrastructure_status`` MCP tool._

- `async def infrastructure_status()` — Returns the current health of every backend apecx-mcp depends on.

## `src/apecx_integration/mcp_surface/tools/workflows.py`
_MCP tools for the scientist-facing workflow lifecycle._

- `async def compose_workflow(description: str='', user_id: str='', preferred_executor: str='local', run_id: str='', execute: bool=False)` — Compose (and optionally run) a NEW workflow from a natural-language description.

## `src/apecx_integration/mcp_surface/workflow_discovery.py`
_Dynamic, registration-free discovery of nanobrain workflows on disk._

- class `DiscoveredWorkflow` — One workflow found on disk, with the metadata read from its own files.
- `def _workflows_dir()` — Absolute path to ``apecx_integration/composition/workflows``.
- `def _first_line(text: str | None)` — First non-empty, whitespace-collapsed line of a doc/description block.
- `def _strip_name_prefix(desc: str, name: str)` — ``"viral_conserved_sites — find ..."`` → ``"find ..."`` (drop a leading ``name —``).
- `def category_for(name: str)` — Transparent category LABEL for a workflow name (never a filter).
- `def _module_docstring(py_path: Path)` — AST-read a .py file's module docstring WITHOUT importing it.
- `def _public_build_function(builder_py: Path, dir_name: str)` — Name of the public ``build_*`` callable in ``builder.py`` (AST, no import).
- `def _discover_yaml_workflow(d: Path)` — A directory whose workflow is defined by a YAML file.
- `def _discover_builder_workflow(d: Path)` — A directory whose workflow is built by a Python ``builder.py``.
- `def discover_workflows()` — Scan ``composition/workflows/`` and return every workflow found, sorted by name.
- `def discover_by_name(name: str)` — The discovered workflow whose name == ``name``, or None.

## `src/apecx_integration/mcp_surface/workflow_inputs.py`
_Derive a workflow's required inputs from its OWN schema (RoC-2b)._

- `def derive_required_inputs(workflow: Any, entry_du_name: str | None)` — Return ``{required, properties, obtain_via}`` from the entry step's ``step_input_schema``.
- `def find_param_gaps(params: dict[str, Any], derived: dict[str, Any])` — Return a list of ``ParamNeed`` for required params that are missing or ill-typed.
- `def _find_entry_step(workflow: Any, entry_du_name: str | None)`
- `def _resolve_g6_input_schema(step: Any)`
- `def _unwrap_param_schema(schema: dict[str, Any], entry_du_name: str | None)` — The G6 schema validates the trigger-WRAPPED input ({<entry_du>: {params}}). Unwrap that one
- `def _is_blank(value: Any)`
- `def _type_ok(value: Any, prop_schema: dict[str, Any])`

## `src/apecx_integration/mcp_surface/workflow_registry.py`
_Workflow catalog → MCP tool registrar._

- class `WorkflowSourceYAML(BaseModel)` — A workflow loaded via ``Workflow.from_config(path)``.
- class `WorkflowSourceLightweight(BaseModel)` — A workflow loaded via a Python callable that returns a Workflow.
- class `WorkflowRequirements(BaseModel)` — Prerequisites checked before EACH tool call (env can change).
- class `WorkflowCatalogEntry(BaseModel)` — One pre-made workflow exposed as one MCP tool.
- class `WorkflowCatalog(BaseModel)` — Top-level catalog file: a list of entries.
- class `RegistrationReport` — Outcome of ``register_workflows``.
  - `def summary_line(self)`
- `def _apecx_package_root()` — The apecx_integration package directory.
- `def _packaged_default_catalog_path()`
- `def load_catalog(path: str | Path | None=None)` — Load + validate a workflow catalog.
- `def resolve_catalog_entry(name: str, catalog: WorkflowCatalog | None=None)` — The runnable entry for ``name`` — catalog override if present, else SYNTHESIZED
- `def sole_data_unit_name(data_units: Any)` — The single DU name in a ``{name: cfg}`` mapping or ``[name]`` list, else ``None``.
- `def check_prerequisites(reqs: WorkflowRequirements)` — Return ``(all_met, missing_reasons)``.
- `def _resolve_yaml_path(rel_or_abs: str)` — Resolve a YAML path against the apecx_integration package root.
- `def _load_workflow_for_entry(entry: WorkflowCatalogEntry)` — Load (or return cached) Workflow instance for one catalog entry.
- `def _clear_workflow_cache()` — Test hook — drop every cached Workflow.
- `def _json_type_to_python_annotation(prop_schema: dict[str, Any])` — Map a JSON-Schema property to a Python type-annotation string.
- `def _synthesize_tool_function(entry: WorkflowCatalogEntry, runner: Callable[..., Any])` — Build an async function whose signature mirrors ``entry.input_schema``.
- `def register_workflows(server: Any, catalog: WorkflowCatalog, *, logger: logging.Logger | None=None)` — Register one MCP tool per catalog entry on ``server``.
- `async def _run_via_run_workflow(entry: WorkflowCatalogEntry, *, ctx: Context | None=None, **kwargs: Any)` — The SINGLE workflow-tool runner: the shared guarded execution core.
- `def _register_one_entry(server: Any, entry: WorkflowCatalogEntry, lg: logging.Logger, report: RegistrationReport, *, identifier: str, runner: Callable[..., Any] | None=None)` — Register ONE catalog entry as an MCP tool (live or UNAVAILABLE), recording the outcome.
- `def _resolve_promoted_entries(catalog: WorkflowCatalog, already_registered: set[str], lg: logging.Logger)` — Build registrable entries for ``catalog.promote_discovered`` names.

## `src/apecx_integration/regression_metrics.py`
_Regression-metric queries over the control-plane DB._

- class `ComposeRetryStats` — C1 (compose_retries) distribution.
  - `def retry_rate(self)`
- class `RuntimeViolationStats` — C2 (runtime_violations) breakdown.
- class `RetrievalGapStats` — A2 (retrieval_gap) rate.
  - `def rate(self)`
- class `ClassPathRepairStats` — CPR (2026-05-11) auto-repair frequency.
- class `RegressionMetricsReport` — The full picture an operator wants at a glance.
  - `def to_dict(self)` — JSON-friendly dict for the CLI's ``--json`` output.
  - `def to_table(self)` — Human-readable text. Stable column widths so eyeballs
- `def compute_regression_metrics(session_factory: sessionmaker[Session], *, since: datetime | None=None)` — Compute all three regression metrics from one DB pass.
- `def _select_artifacts(session: Session, *, since: datetime | None)` — Return every GeneratedArtifact row honoring the ``since`` filter.
- `def report_as_json(report: RegressionMetricsReport, *, indent: int=2)`

## `src/apecx_integration/synonym_dictionary/__init__.py`
_Synonym dictionary — local-first build of canonical-IRI resolution._

_(no module-level classes or functions)_

## `src/apecx_integration/synonym_dictionary/build.py`
_Stage 1 build pipeline — orchestrates resolvers + writer to produce_

- class `TableSpec` — One input table to enrich during a build.
- class `_IRIAggregate` — Mutable accumulator for one canonical IRI across multiple rows.
- `async def build_dictionary(*, table_specs: list[TableSpec], output_dictionary: Path, dictionary_version: str, ontology_versions: dict[OntologyName, str], writer_factory: Any, ols_client: OLSClient | None=None, nodes_dmp_path: Path | None=None, merged_dmp_path: Path | None=None, names_dmp_path: Path | None=None, delnodes_dmp_path: Path | None=None, taxonomy_subtree_root: int | None=None)` — Run a full Stage 1 build.
- `async def _process_table(*, spec: TableSpec, resolver: _ResolverBase, aggregates: dict[tuple[EntityType, str], _IRIAggregate])` — Resolve every row of ``spec.input_path`` and write the enriched
- `def _synthesize_subtree_entries(*, aggregates: dict[tuple[EntityType, str], _IRIAggregate], nodes_dmp_path: Path, names_dmp_path: Path, taxonomy_subtree_root: int, ontology_version: str)` — Expand ``aggregates`` with one (PATHOGEN, NCBITaxon-IRI) entry per
- `def run_build_sync(**kwargs: Any)` — Synchronous wrapper around :func:`build_dictionary` for CLI usage.

## `src/apecx_integration/synonym_dictionary/disambiguation_overrides.py`
_Consumer-side disambiguation overrides for the synonym dictionary._

- `def _t(taxid: str)`

## `src/apecx_integration/synonym_dictionary/enums.py`
_Enumerations for the synonym dictionary._

- class `EntityType(StrEnum)` — The kind of entity a dictionary entry refers to.
- class `ResolutionStatus(StrEnum)` — Provenance tag describing how a row's canonical IRI was determined.
- class `OntologyName(StrEnum)` — Authoritative-source identifier; pinned per dictionary build via the

## `src/apecx_integration/synonym_dictionary/harvester_adapter.py`
_Adapter that wraps an :class:`EntityResolutionTransform` for use as an_

- `def adapt_to_harvester_transform(entity_transform: EntityResolutionTransform, *, extension_field: str)` — Wrap an ``EntityResolutionTransform`` for use as an apecx-harvesters
- `def adapt_workflow_to_harvester_transform(workflow: Workflow, *, extension_field: str, entity_type_field: str='entity_type', surface_form_field: str='surface_form')` — Wrap a nanobrain :class:`Workflow` as an apecx-harvesters ``Transform``.

## `src/apecx_integration/synonym_dictionary/hierarchy_loader.py`
_Parse NCBI Taxonomy dump files for the synonym dictionary build._

- `def parse_nodes_dmp(path: Path)` — Yield (child_taxon_id, parent_taxon_id) from ``nodes.dmp``.
- `def parse_nodes_dmp_with_rank(path: Path)` — Yield (taxon_id, parent_taxon_id, rank) from ``nodes.dmp``.
- `def compute_species_ancestors(nodes_dmp_path: Path, *, species_rank: str='species')` — Map every taxon at-or-below species rank to its species ancestor.
- `def parse_merged_dmp(path: Path)` — Yield (old_taxon_id, new_taxon_id) from ``merged.dmp``.
- `def count_lines(path: Path)` — Return approximate row count for progress logging (reads the file once).
- class `NameRecord` — One row of ``names.dmp``.
- `def parse_names_dmp(path: Path)` — Yield :class:`NameRecord` for every line of ``names.dmp``.
- `def parse_delnodes_dmp(path: Path)` — Yield deleted taxon ids from ``delnodes.dmp``.
- `def compute_subtree_descendants(nodes_dmp_path: Path, root_taxon_id: int)` — Return the set of taxon ids in the subtree rooted at
- `def compute_subtree_descendants_union(nodes_dmp_path: Path, root_taxon_ids: list[int])` — Union of the subtrees rooted at each id in ``root_taxon_ids``.

## `src/apecx_integration/synonym_dictionary/io.py`
_Abstract IO contracts for the synonym dictionary._

- class `DictionaryWriter(ABC)` — Build-time interface for emitting a dictionary artifact.
  - `def write_entry(self, entry: DictionaryEntry)` — Persist one dictionary entry.  Idempotent on
  - `def write_manifest(self, manifest: BuildManifest)` — Persist the build manifest.  Should be called exactly once
  - `def close(self)` — Flush + finalize the artifact.  After calling this, the
  - `def __enter__(self)`
  - `def __exit__(self, exc_type: object, exc: object, tb: object)`
- class `DictionaryReader(ABC)` — Runtime interface for querying a built dictionary artifact.
  - `def read_manifest(self)` — Return the build manifest.  Raises if the artifact's
  - `def lookup_by_surface_form(self, entity_type: EntityType, surface_form: str)` — Resolve a free-text user term to its canonical entry.
  - `def lookup_by_iri(self, canonical_iri: str)` — Direct lookup by canonical IRI.  Used for the cross-database
  - `def all_entries(self)` — Iterate every entry in the dictionary.  Used for build
  - `def __enter__(self)`
  - `def __exit__(self, exc_type: object, exc: object, tb: object)`

## `src/apecx_integration/synonym_dictionary/loader.py`
_Stage 2 runtime dictionary loader._

- `def default_dictionary_path()` — Resolve the canonical dictionary location WITHOUT requiring an env var.
- class `DictionaryIndex` — In-memory index loaded from a SQLite dictionary artifact.
  - `def __init__(self, *, inverse: dict[tuple[str, str], tuple[str, ...]], entries: dict[str, DictionaryEntry], manifest: BuildManifest, db_path: Path | None=None, has_hierarchy: bool=False)`
  - `def manifest(self)`
  - `def lookup(self, entity_type: EntityType, surface_form: str)` — Fast-path lookup: normalized surface form -> single canonical entry.
  - `def lookup_all(self, entity_type: EntityType, surface_form: str)` — SC-A4b: return ALL candidate entries for a surface form.
  - `def lookup_any_type(self, surface_form: str)` — Search across all entity types.
  - `def lookup_by_iri(self, canonical_iri: str)` — Reverse lookup: canonical IRI -> dictionary entry.
  - `def has_hierarchy(self)` — True if this index was built with an embedded NCBITaxon hierarchy.
  - `def lookup_ancestor(self, iri: str)` — Walk the NCBITaxon hierarchy upward to find the nearest ancestor in this dictionary.
  - `def lookup_descendant_taxon_ids(self, iri: str)` — Return all descendant NCBITaxon integer IDs for ``iri``.
  - `def entry_count(self)`
  - `def index_entry_count(self)`
  - `def lookup_ambiguous_surface_forms(self, *, surface_form: str | None=None, entity_type: str | None=None, limit: int=100)` — Specialized query: ambiguous-synonym catalog.
  - `def load(cls, path: Path | str)` — Load a SQLite dictionary artifact into memory.
- class `_ProcessSingleton` — Lazy process-singleton holder for the DictionaryIndex.
  - `def __init__(self)`
  - `def configure(self, path: Path | str)` — Set the dictionary artifact path.  Call before ``get``.
  - `def get(self)` — Return (index, None) on success or (None, error_message) on failure.
- `def configure_dictionary_path(path: Path | str)` — Set the process-wide dictionary artifact path.
- `def get_dictionary_index()` — Return the process-wide DictionaryIndex or an error message.

## `src/apecx_integration/synonym_dictionary/lookup.py`
_Stage 2 runtime lookup API._

- class `LookupCandidate` — One candidate for an ambiguous surface form (SC-A4b, 2026-06-08).
- class `LookupResult` — The outcome of a single Stage 2 entity lookup.
- `def fast_miss(surface_form: str, *, reason: str='')` — Return a LookupResult representing a complete miss on all paths.
- `def lookup_entity(surface_form: str, *, entity_type: EntityType | None=None)` — Look up a user-supplied term against the synonym dictionary.
- `def detect_ambiguity(surface_form: str, *, entity_type: EntityType | None=None)` — Resolve a surface form AND detect multi-IRI ambiguity.
- `def _entry_to_result(surface_form: str, entry: DictionaryEntry, *, path: Literal['override', 'fast', 'ancestor', 'slow'])`
- `def _ambiguous_to_result(surface_form: str, candidates: tuple[DictionaryEntry, ...])` — SC-A4b: build a LookupResult for an ambiguous surface form.
- `def _ancestor_to_result(surface_form: str, ancestor: DictionaryEntry)` — Build a LookupResult for a taxon-hierarchy ancestor match.
- `def _try_slow_path(surface_form: str)` — Fall back to the existing database substring resolver.

## `src/apecx_integration/synonym_dictionary/metrics.py`
_Accuracy metrics for synonym-dictionary lookups against ground-truth datasets._

- class `Mismatch` — A single record where the dictionary disagrees with ground truth.
  - `def __str__(self)`
- class `AccuracyMetrics` — Aggregated accuracy report for a dictionary against a ground-truth dataset.
  - `def resolved_count(self)` — Records the dictionary returned any IRI for (fast/ancestor/slow).
  - `def recall(self)` — Fraction of ground-truth rows where lookup returned any IRI.
  - `def precision(self)` — Fraction of resolved rows where IRI matches ground truth.
  - `def f1(self)` — Harmonic mean of precision and recall (single-score summary).
  - `def summary(self)` — Human-readable report. Use in CLI / test logs.
- `def _build_expected_pathogen_iri(taxon_id: float | int | str | None)` — Convert a VIOLIN ``NCBI_Taxonomy_ID`` cell to an OBO IRI, or None.
- `def _build_expected_vaccine_iri(vo_id: float | int | str | None)` — Convert a VIOLIN ``Vaccine_Ontology_ID`` cell to an OBO IRI, or None.
- `def _build_expected_gene_iri(gene_id: float | int | str | None)` — Convert a VIOLIN ``NCBI_Gene_ID`` cell to an identifiers.org IRI.
- `def _confidence_bucket(confidence: float)` — Human-readable confidence band.
- `def _measure_accuracy(dictionary_path: Path | str, csv_path: Path | str, *, entity_type: EntityType, surface_form_column: str, ground_truth_column: str | None, expected_iri_builder, max_rows: int | None=None, surface_form_fallback_columns: tuple[str, ...]=())` — Generic accuracy measurement core. Use the public wrappers.
- `def measure_pathogen_accuracy(dictionary_path: Path | str, violin_pathogens_csv: Path | str, *, max_rows: int | None=None)` — Pathogen IRI accuracy against VIOLIN ``Pathogen_Information``.
- `def measure_vaccine_accuracy(dictionary_path: Path | str, violin_vaccines_csv: Path | str, *, max_rows: int | None=None)` — Vaccine IRI accuracy against VIOLIN ``Vaccine_Information``.
- `def measure_gene_accuracy(dictionary_path: Path | str, violin_genes_csv: Path | str, *, max_rows: int | None=None)` — Gene IRI accuracy against VIOLIN ``Gene_Information``.
- `def measure_disease_path_distribution(dictionary_path: Path | str, violin_pathogens_csv: Path | str, *, max_rows: int | None=None)` — Disease lookup metrics from the ``Disease`` column of Pathogen rows.

## `src/apecx_integration/synonym_dictionary/mined_ingest.py`
_SC-B4 (2026-06-08) — ingest corpus-mined synonym observations into an_

- class `MinedRow` — One row of the mined_observations.jsonl sidecar.
- `def _iter_mined_rows(path: Path)` — Yield MinedRow objects from the sidecar JSONL.
- class `IngestSummary` — Result of ingesting a mined-observations sidecar.
- `def ingest_mined_observations(*, dict_path: Path, mined_jsonl: Path, entity_type: str='pathogen')` — Apply a mined-observations JSONL sidecar against an existing dictionary.

## `src/apecx_integration/synonym_dictionary/normalization.py`
_Surface-form normalization shared by Stage 1 (build-time inverse index)_

- `def normalize_surface_form(s: str)` — Canonicalize a surface form for lookup-key purposes.

## `src/apecx_integration/synonym_dictionary/ols_client.py`
_EBI OLS (Ontology Lookup Service) HTTP client._

- class `OLSError(RuntimeError)` — Raised when an OLS request fails after all retries.
- class `OLSClient` — Async client for the EBI OLS v4 API.
  - `def __init__(self, *, base_url: str=DEFAULT_BASE_URL, timeout: float=DEFAULT_TIMEOUT_SECONDS, retry_attempts: int=DEFAULT_RETRY_ATTEMPTS, backoff_base: float=DEFAULT_BACKOFF_BASE_SECONDS, inter_request_delay: float=DEFAULT_INTER_REQUEST_DELAY_SECONDS, client: httpx.AsyncClient | None=None)`
  - `async def __aenter__(self)`
  - `async def __aexit__(self, *args: object)`
  - `async def close(self)`
  - `async def search(self, query: str, ontology: OntologyName, *, rows: int=5, exact: bool=False)` — Search ``ontology`` for term matches against ``query``.
  - `async def get_term(self, ontology: OntologyName, iri: str)` — Fetch a term by IRI from the named ontology.
  - `def extract_synonyms(term: dict[str, Any] | None)` — Best-effort synonyms-array extraction from an OLS term payload.
  - `def extract_label(term: dict[str, Any] | None)`
  - `async def _get_json(self, path: str, *, params: dict[str, Any])`
  - `async def _backoff(self, attempt: int)`
  - `def _parse_retry_after(resp: httpx.Response)`

## `src/apecx_integration/synonym_dictionary/resolvers.py`
_Per-entity-type resolvers backed by OLS (or, for genes, source-row data)._

- `def normalize_iri(value: str | int | float | None, *, prefix: str)` — Convert a column value (which might be a bare numeric ID, a bare
- class `_ResolverBase` — Common resolver behaviour: build a :class:`ResolutionResult` from
  - `def __init__(self, ols: OLSClient, *, dictionary_version: str)`
  - `async def _resolve_by_iri(self, iri: str, *, fallback_label: str | None=None, fallback_synonyms: tuple[str, ...]=())` — Anchor-mode lookup for an existing IRI.
  - `def _merge_synonyms(primary: tuple[str, ...] | list, extra: tuple[str, ...] | list)` — Concat two synonym lists, dedupe, preserve order.
  - `async def _resolve_by_search(self, query: str, *, context_terms: Iterable[str]=())` — Free-text search fallback.  ``context_terms`` is currently used
  - `def _disambiguate(self, query: str, docs: list[dict[str, Any]], context_terms: Iterable[str])` — Pick the best OLS doc out of ``docs`` for the given query.
  - `def _unresolved(self, *, reason: str='')`
  - `async def resolve(self, record: EntityRecord)` — Override in subclasses.
- class `PathogenResolver(_ResolverBase)` — VIOLIN ``Pathogen_Information`` rows (and BV-BRC genome rows).
  - `async def resolve(self, record: EntityRecord)`
- class `VaccineResolver(_ResolverBase)` — VIOLIN ``Vaccine_Information`` rows.
  - `async def resolve(self, record: EntityRecord)`
- class `DiseaseResolver(_ResolverBase)` — VIOLIN ``Disease`` column on Pathogen_Information rows.
  - `async def resolve(self, record: EntityRecord)`
- `def _build_ncbigene_iri(value: str | int | float | None)` — Construct an identifiers.org NCBI Gene IRI from a gene ID value.
- `def _gene_label_from_name(gene_name: str)` — Extract the primary label (gene symbol) from a Gene_Name column value.
- `def _extract_gene_synonyms(gene_name: str)` — Extract all useful synonym forms from a Gene_Name column value.
- class `GeneResolver(_ResolverBase)` — VIOLIN ``Gene_Information`` rows.
  - `async def resolve(self, record: EntityRecord)`

## `src/apecx_integration/synonym_dictionary/schema.py`
_Pydantic schemas for the synonym dictionary artifact._

- class `ResolutionResult(BaseModel)` — Per-record output of a :class:`EntityResolutionTransform`.
- class `DictionaryEntry(BaseModel)` — One entry per ``(entity_type, canonical_iri)`` tuple in the dictionary.
- class `ProvisionalSynonym(BaseModel)` — User-supplied synonym candidate, not yet promoted into the dictionary.
- class `BuildManifest(BaseModel)` — Metadata accompanying a dictionary artifact.

## `src/apecx_integration/synonym_dictionary/sqlite_writer.py`
_SQLite-backed implementation of :class:`DictionaryWriter`._

- `def _batched(it: Iterator, n: int)` — Yield successive lists of up to n items from iterator.
- class `SQLiteDictionaryWriter(DictionaryWriter)` — Writer that persists to a SQLite file.
  - `def __init__(self, path: Path | str)`
  - `def write_entry(self, entry: DictionaryEntry)`
  - `def write_manifest(self, manifest: BuildManifest)`
  - `def write_taxon_hierarchy(self, nodes_iter: Iterator[tuple[int, int]])` — Persist the full NCBITaxon parent-child mapping from ``nodes.dmp``.
  - `def write_taxon_species(self, species_iter: Iterator[tuple[int, int]])` — Persist the strain→species map.
  - `def write_merged_taxons(self, merged_iter: Iterator[tuple[int, int]])` — Persist the NCBITaxon merged-ID table from ``merged.dmp``.
  - `def has_taxon_hierarchy(self)` — Return True if the hierarchy table was written and is non-empty.
  - `def write_deleted_taxons(self, deleted_iter: Iterator[int])` — Persist deleted-taxon IDs from ``delnodes.dmp`` (SC-A4 / SC-A5).
  - `def close(self)`
- class `SQLiteDictionaryReader(DictionaryReader)` — Reader that loads dictionary entries from a SQLite file built by
  - `def __init__(self, path: Path | str)`
  - `def read_manifest(self)`
  - `def lookup_by_surface_form(self, entity_type: EntityType, surface_form: str)`
  - `def lookup_by_iri(self, canonical_iri: str)`
  - `def has_taxon_hierarchy(self)` — Return True if the SQLite file contains a non-empty taxon_hierarchy table.
  - `def all_entries(self)`
  - `def __exit__(self, exc_type: object, exc: object, tb: object)`
  - `def _row_to_entry(row: sqlite3.Row)`

## `src/apecx_integration/synonym_dictionary/taxdump_fetcher.py`
_NCBI Taxonomy dump downloader._

- `def fetch_taxdump(dest_dir: Path | str, *, url: str=TAXDUMP_URL, force: bool=False, show_progress: bool=False)` — Download NCBI taxdump and extract the four required dump files.
- `def _download(url: str, dest: Path, *, show_progress: bool)`
- `def _extract(archive: Path, dest_dir: Path)`

## `src/apecx_integration/synonym_dictionary/transform.py`
_Entity-resolution transform contract._

_(no module-level classes or functions)_

## `src/apecx_integration/synonym_dictionary/workflow/__init__.py`
_Nanobrain-wrapped end-to-end IRI resolution workflow._

_(no module-level classes or functions)_

## `src/apecx_integration/synonym_dictionary/workflow/bootstrap.py`
_Bootstrap entry point — drive the dictionary-build workflow once._

- `def _env_path(name: str, default: Path)`
- class `EnsureDictionaryConfig` — Inputs to :func:`ensure_dictionary`.
  - `def resolve(self)` — Fill in env-var-derived defaults; return a fully-resolved copy.
- `def _required_data_files(data_root: Path)` — At least one of these must exist for the build to be meaningful.
- `def _data_present(data_root: Path)`
- `def ensure_dictionary(config: EnsureDictionaryConfig | None=None)` — Synchronous wrapper around :func:`ensure_dictionary_async`.
- `async def ensure_dictionary_async(config: EnsureDictionaryConfig | None=None)` — Build the synonym dictionary if it isn't already present.
- `async def _drive_workflow(cfg: EnsureDictionaryConfig)` — Load the dictionary-build workflow YAML and drive the cascade.

## `src/apecx_integration/synonym_dictionary/workflow/dictionary_build_step.py`
_DictionaryBuildStep — nanobrain wrapper around ``build_dictionary``._

- `def _default_dict_output_dir()` — Resolve the default dictionary output directory.
- `def _default_dictionary_version()` — Default dictionary_version is the current ISO-8601 UTC timestamp.
- `def _default_data_file_path(relative_path: str)` — Resolve data file path from APECX_DATA_ROOT environment variable.
- `def _default_violin_pathogens_path()`
- `def _default_violin_vaccines_path()`
- `def _default_violin_genes_path()`
- `def _default_bvbrc_genomes_path()`
- class `DictionaryBuildStepConfig(StepConfig)` — Config for :class:`DictionaryBuildStep`.
- class `DictionaryBuildStep(BaseStep)` — Build the synonym-dictionary artifact from the configured input
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: DictionaryBuildStepConfig)`
  - `def _init_from_config(self, config: DictionaryBuildStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _extract_taxdump_paths(input_data: dict[str, Any])` — Pull the four taxdump file paths out of the input.
  - `def _make_table_specs(self, *, enriched_dir: Path)` — Equivalent of cli._make_table_specs but driven by step config.
  - `def _truncate_inputs(specs: list[TableSpec], max_rows: int)` — Mirror cli._truncate_inputs — write a truncated copy of any

## `src/apecx_integration/synonym_dictionary/workflow/normalize_step.py`
_Step 1 of the IRI-resolution workflow — surface-form normalization._

- class `NormalizeEntityRecordsStepConfig(StepConfig)` — No extra fields beyond StepConfig — class-level constants suffice.
- class `NormalizeEntityRecordsStep(BaseStep)` — Normalize each record's ``surface_form`` to a lookup-friendly key.
  - `def _get_config_class(cls)`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/synonym_dictionary/workflow/resolve_step.py`
_Step 2 of the IRI-resolution workflow — canonical IRI lookup._

- class `ResolveIRIStepConfig(StepConfig)` — Config for :class:`ResolveIRIStep`.
- class `ResolveIRIStep(BaseStep)` — Look up each normalized record's surface form against the synonym
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: ResolveIRIStepConfig)`
  - `def _init_from_config(self, config: ResolveIRIStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _merge_result_into_record(record: dict[str, Any], result: LookupResult)` — Write the LookupResult fields onto ``record`` using the
- `def _extract_dictionary_version(evidence: str)` — Pull ``dictionary_version=<x>`` out of LookupResult.evidence.

## `src/apecx_integration/synonym_dictionary/workflow/taxdump_fetch_step.py`
_TaxdumpFetchStep — nanobrain wrapper around ``fetch_taxdump``._

- `def _default_taxdump_dir()` — Resolve the default output directory at config-validation time.
- class `TaxdumpFetchStepConfig(StepConfig)` — Config for :class:`TaxdumpFetchStep`.
- class `TaxdumpFetchStep(BaseStep)` — Download NCBI taxdump and emit the extracted file paths.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: TaxdumpFetchStepConfig)`
  - `def _init_from_config(self, config: TaxdumpFetchStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`

## `src/apecx_integration/synonym_dictionary/workflow/taxon_species_map_step.py`
_TaxonSpeciesMapStep — build the strain→species map into the dictionary._

- class `TaxonSpeciesMapStepConfig(StepConfig)` — Config for :class:`TaxonSpeciesMapStep`.
- class `TaxonSpeciesMapStep(BaseStep)` — Compute + persist the strain→species map into the dictionary SQLite.
  - `def _get_config_class(cls)`
  - `def extract_component_config(cls, config: TaxonSpeciesMapStepConfig)`
  - `def _init_from_config(self, config: TaxonSpeciesMapStepConfig, component_config: dict[str, Any], dependencies: dict[str, Any])`
  - `async def process(self, input_data: dict[str, Any], **kwargs)`
  - `def _compute(self, nodes_path: Path)`
  - `def _write(self, dict_path: Path, species_map: dict[int, int])`
  - `def _unwrap(self, input_data: dict[str, Any])` — Unwrap the framework trigger envelope ``{du_name: payload}``.
  - `def species_for_taxon(con: sqlite3.Connection, taxon_id: int)` — Read helper: the species ancestor of ``taxon_id`` (or None).

## `src/apecx_integration/synonym_dictionary/workflow/workflow.py`
_IRIResolutionWorkflow — nanobrain Workflow subclass with synchronous orchestration._

- class `IRIResolutionWorkflow(Workflow)` — Two-step IRI resolution workflow with explicit step orchestration.
  - `async def process(self, input_data: dict[str, Any], **kwargs)` — Drive the two-step cascade explicitly.
  - `def _extract_entity_records(input_data: Any)` — Pull the records list out of ``input_data``, accepting two shapes.
