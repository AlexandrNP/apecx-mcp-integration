class: "apecx_integration.composition.steps.consensus_aggregator_step.ConsensusAggregatorStep"
name: aggregator
description: "AST-validator voter over the perturbing drafter's candidate list."

voting_strategy: ast_validator

input_data_units:
  aggregator_input:
    class: "nanobrain.core.data_unit.DataUnitMemory"
    name: aggregator_input
    persistent: false
    contract:
      kind: record

output_data_units:
  aggregator_output:
    class: "nanobrain.core.data_unit.DataUnitMemory"
    name: aggregator_output
    persistent: false
    contract:
      kind: record
      required:
        code_source: {kind: text}

triggers:
  - class: "nanobrain.core.trigger.DataUnitChangeTrigger"
    data_unit: "aggregator_input"
