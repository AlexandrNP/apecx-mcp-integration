<!--
T-COMP Phase 1 — PLACEHOLDER system prompt.

This file exists now so ComposerConfig.prompt_dir has a real directory
to point at during Phase-1 loadability tests. The TEXT below is a
placeholder; Phase 2 will replace it with the actual composition prompt
after prompt-engineering work.

AC6 from docs/composer_task_spec.md says: every prompt string used by
composer.py must live in a file under composer_prompts/. This file is
the first one — claiming the slot BEFORE any real LLM call is wired,
so nobody is tempted to inline prompt text in composer.py "just for
Phase 2."
-->

# System prompt (placeholder)

You are a workflow-composition assistant. Do not use this prompt
verbatim — replace during T-COMP Phase 2 prompt engineering.
